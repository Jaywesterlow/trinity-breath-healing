---
phase: 00-foundation-seo-scaffolding
plan: 02
type: execute
wave: 1
depends_on: ["00-01"]
files_modified:
  - src/lib/seo/types.ts
  - src/lib/seo/defaults.ts
  - src/lib/constants/brand.ts
  - src/lib/components/Head.svelte
  - src/lib/components/PageTitle.svelte
  - src/lib/components/SiteNav.svelte
  - src/lib/components/SiteFooter.svelte
  - src/routes/+layout.svelte
  - tests/unit/head.test.ts
  - tests/unit/page-title.test.ts
  - tests/unit/canonical.test.ts
  - tests/unit/landmarks.test.ts
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04
  - SEO-05
  - SEO-06

must_haves:
  truths:
    - "<Head meta={pageMeta}> emits <title>, <meta name='description'>, <link rel='canonical'>, <link rel='alternate' hreflang='nl'>, <link rel='alternate' hreflang='x-default'>, og:title, og:description, og:url, og:image, og:type, og:locale=nl_NL, twitter:card, twitter:title, twitter:description, twitter:image inside <svelte:head>"
    - "<PageTitle>{title}</PageTitle> renders exactly one <h1>"
    - "Every prerendered page (landing + 14 stubs) contains <nav>, <main>, AND <footer> in initial HTML — SiteNav + SiteFooter mount in root +layout.svelte (SEO-03 semantic landmarks)"
    - "All <Head> output derives from typed PageMeta and SITE_URL env — never from $state or $effect (Pitfall #2)"
    - "Unit tests prove: title length validator, description length validator, canonical absolute-URL builder, hreflang pair construction, landmark presence, og:locale=nl_NL emission"
  artifacts:
    - path: "src/lib/seo/types.ts"
      provides: "PageMeta interface: { title: string, description: string, path: string, og?: { image?: string, type?: 'website'|'article' }, noindex?: boolean, dateModified?: string }"
      contains: "export type PageMeta"
    - path: "src/lib/seo/defaults.ts"
      provides: "SITE_URL constant (reads $env/dynamic/public.PUBLIC_SITE_URL, throws if unset, strips trailing slash); defaults object with title + description + path: '/' + og.image + og.type: 'website'"
      contains: "PUBLIC_SITE_URL"
    - path: "src/lib/constants/brand.ts"
      provides: "BRAND constant: legalName, shortName, practitionerFullName (TODO placeholder), email (info@trinitybnh.nl), phone (TODO), socials.{instagram,facebook,x}, areaServed: ['Amsterdam','Zaandam','Almere','Weesp','Oostzaan','Hoofddorp','Badhoevedorp'], services: [{slug:'mahatma-healing',name:'Mahatma Healing'},{slug:'goldhealing',name:'Goldhealing'},{slug:'raster-energie',name:'Raster Energie'},{slug:'spinal-touch',name:'Spinal Touch'}]"
      contains: "areaServed"
    - path: "src/lib/components/Head.svelte"
      provides: "Reusable head emitter taking `meta: PageMeta` prop via $props; uses $derived for titleFull/canonical/ogImage; emits all SEO tags via <svelte:head> INCLUDING og:locale=nl_NL"
      contains: "og:locale"
    - path: "src/lib/components/PageTitle.svelte"
      provides: "Wraps a snippet in a single <h1>; styling via scoped <style> block referencing var(--font-display)"
      contains: "<h1>"
    - path: "src/lib/components/SiteNav.svelte"
      provides: "Skeleton semantic <nav aria-label=\"Hoofdnavigatie\"> landmark. No nav items in Phase 0 — Phase 1 LND-XX populates the link list. Phase 0 ships an empty nav element (single <ul> with no <li> children OR a single visually-hidden 'Skip to main content' link) so SEO-03 lands and every prerendered HTML has the <nav> landmark in initial HTML."
      contains: "<nav"
    - path: "src/lib/components/SiteFooter.svelte"
      provides: "Skeleton semantic <footer aria-labelledby=\"site-footer-heading\"> landmark. Contains <h2 id=\"site-footer-heading\" class=\"sr-only\">Site footer</h2> as the labelling heading. Phase 0 ships no NAP / social block — Phase 1 LND-08 lands the footer content (BRAND-driven). Phase 0's job is purely to ensure the <footer> landmark exists in initial HTML on every prerendered route."
      contains: "<footer"
    - path: "src/routes/+layout.svelte"
      provides: "Mounts <Head meta={page.data.meta} />, then <SiteNav />, then <main>{@render children()}</main>, then <SiteFooter />. (JsonLd is mounted by Plan 03.) The <html lang=\"nl\"> attribute is set on src/app.html (Plan 01) — NOT here."
      contains: "<SiteNav"
  key_links:
    - from: "src/lib/components/Head.svelte"
      to: "src/lib/seo/defaults.ts"
      via: "import SITE_URL"
      pattern: "SITE_URL"
    - from: "src/lib/components/Head.svelte"
      to: "PageMeta type"
      via: "import type PageMeta"
      pattern: "PageMeta"
    - from: "src/routes/+layout.svelte"
      to: "<Head meta={$page.data.meta} />"
      via: "$page store"
      pattern: "<Head meta="
    - from: "src/routes/+layout.svelte"
      to: "<SiteNav /> + <SiteFooter />"
      via: "direct mount, no props in Phase 0"
      pattern: "<SiteNav"
---

<objective>
Land the head-emission primitives (`<Head>`, `<PageTitle>`) and the typed SEO contract (`PageMeta`, `SITE_URL`, defaults) that every route — landing + 14 stubs — will compose with. Additionally land the semantic-landmark skeletons (`SiteNav`, `SiteFooter`) that SEO-03 requires on every prerendered page (Phase 1 fills the link list / NAP block; Phase 0 ships the empty landmarks). This is the SEO-01..06 foundation. Mount the primitives + landmarks into `src/routes/+layout.svelte` so EVERY prerendered page emits title + meta description + canonical + hreflang + OG (including og:locale=nl_NL) + Twitter + `<nav>` + `<main>` + `<footer>` in initial HTML by default.

Purpose: AI crawlers do not execute JS. The only way SEO meta + semantic landmarks get into initial HTML is via `<svelte:head>` + statically rendered layout markup driven by `load()` data (Pitfall #2). This plan ships the discipline. SEO-03 (semantic HTML5 landmarks: main, nav, article, section, footer) cannot wait for Phase 1 — Plan 08's audit gate asserts <nav>+<main>+<footer> on EVERY prerendered page, so the skeleton landmarks must land here in Wave 2.
Output: Two Svelte head primitives + two landmark skeletons + typed PageMeta contract + BRAND/SITE_URL source-of-truth that Plan 03 (JSON-LD) and Plan 05 (stubs) consume.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/00-foundation-seo-scaffolding/00-CONTEXT.md
@.planning/phases/00-foundation-seo-scaffolding/00-RESEARCH.md
@.planning/phases/00-foundation-seo-scaffolding/00-VALIDATION.md
@seo-aeo-samenvatting-checklist.md
@.planning/phases/00-foundation-seo-scaffolding/00-01-SUMMARY.md
@src/routes/+layout.svelte
@src/routes/+layout.ts
@src/app.html

<interfaces>
From 00-RESEARCH.md §11 (canonical code excerpts the executor implements verbatim — types only, prose action describes behavior):

PageMeta:
  title: string (50-60 chars enforced by audit)
  description: string (150-160 chars enforced by audit)
  path: string (absolute path, leading slash, no trailing slash)
  og?: { image?: string, type?: 'website' | 'article' }
  noindex?: boolean (default false; true only for 404)
  dateModified?: string (YYYY-MM-DD; passed through to JSON-LD WebPage in Plan 03; also rendered visibly by Plan 05 landing — SEO-09)

SITE_URL contract:
  - Source: $env/dynamic/public.PUBLIC_SITE_URL (Pattern 8, Pitfall #13)
  - Throws at module load when unset (no localhost default)
  - Strips trailing slash

defaults contract:
  title: 'TRINITY Breath & Healing'
  description: Dutch sentence describing the practice (150-160 chars)
  path: '/'
  og.image: `${SITE_URL}/og-default.jpg`
  og.type: 'website'

Landmark skeletons (Phase 0 ships empty, Phase 1 LND populates):
  SiteNav: <nav aria-label="Hoofdnavigatie"> ... </nav> — no link items in Phase 0
  SiteFooter: <footer aria-labelledby="site-footer-heading"><h2 id="site-footer-heading" class="sr-only">Site footer</h2></footer> — no NAP/social block in Phase 0
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PageMeta type + SITE_URL + defaults + BRAND constants</name>
  <files>src/lib/seo/types.ts, src/lib/seo/defaults.ts, src/lib/constants/brand.ts, tests/unit/canonical.test.ts</files>
  <read_first>
    - .planning/phases/00-foundation-seo-scaffolding/00-RESEARCH.md (§11 `src/lib/seo/types.ts`, §11 `src/lib/seo/defaults.ts`, §3 Pattern 8 SITE_URL strategy, Pitfall #13)
    - .planning/phases/00-foundation-seo-scaffolding/00-CONTEXT.md (Schema strategy → BRAND fields)
    - .planning/REQUIREMENTS.md (SEO-01, SEO-04, SEO-05, FND-07, LGL-09 — BRAND NAP source-of-truth)
    - seo-aeo-samenvatting-checklist.md Deel 1 §4 (title 50-60, meta 150-160)
    - src/routes/+layout.ts (current placeholder defaults import from Plan 01)
  </read_first>
  <behavior>
    - Test 1: importing `SITE_URL` from `$lib/seo/defaults` with `PUBLIC_SITE_URL` set returns the env value without trailing slash
    - Test 2: importing `SITE_URL` with `PUBLIC_SITE_URL` unset throws an Error mentioning 'PUBLIC_SITE_URL is required'
    - Test 3: `defaults.description.length` is between 150 and 160 inclusive
    - Test 4: `BRAND.services` contains exactly 4 entries with slugs ['mahatma-healing','goldhealing','raster-energie','spinal-touch']
    - Test 5: `BRAND.areaServed` contains 'Amsterdam' and 'Almere' (locks Schema.org Service.areaServed source)
    - Test 6: helper `buildCanonical(siteUrl, path)` produces `${siteUrl}${path}` with no double slash for path='/'
    - Test 7: `PageMeta` type includes optional `dateModified?: string` field (compile-only — verified by `pnpm check` on a typed test fixture)
  </behavior>
  <action>
    Write `src/lib/seo/types.ts` exporting `PageMeta` type matching the interface block above. Include the optional `dateModified?: string` field so Plan 05 landing can pass it through to Plan 03's `buildWebPage()` (SEO-09 plumbing). No runtime code.

    Write `src/lib/seo/defaults.ts`: import `env` from `$env/dynamic/public`; at module top-level, if `env.PUBLIC_SITE_URL` is empty/undefined throw `new Error('PUBLIC_SITE_URL is required (set in Vercel env per environment).')` — Pitfall #15 enforcement. Export `SITE_URL = env.PUBLIC_SITE_URL.replace(/\/$/, '')`. Export `defaults: PageMeta` with title='TRINITY Breath & Healing', description=a 150-160 char Dutch sentence describing the practice (use the RESEARCH §11 example: 'Trinity Breath & Healing begeleidt vanuit eigen ervaring bij lichaamsgerichte therapie, ademwerk en energetische behandelingen in Almere en omgeving.' — confirm length 150-160 with a runtime check or hand-count; pad if needed to reach 150 without changing meaning). path='/', og.image=`${SITE_URL}/og-default.jpg`, og.type='website'. Export `buildCanonical(siteUrl: string, path: string): string` helper that joins with no double slash. **Delete the temporary defaults stub created by Plan 01.**

    Write `src/lib/constants/brand.ts` exporting `BRAND` constant typed `as const`. Fields exactly per the artifact spec above. NAP source-of-truth (LGL-09): comment block at the top: `// NAP SOURCE OF TRUTH (LGL-09). Any change must propagate to footer + Organization/ProfessionalService JSON-LD + Instagram bio + KvK record. NEVER hand-edit downstream copies.` Use `practitionerFullName: 'TODO_PRACTITIONER_NAME'`, `phone: 'TODO_PHONE'`, `legalName: 'Trinity Breath & Healing'`, `shortName: 'TRINITY Breath & Healing'`. Use `email: 'info@trinitybnh.nl'` (locked in REQUIREMENTS LND-08 / 00-CONTEXT.md footer block). Socials.instagram TODO, facebook null, x null. areaServed and services exactly as specified in the artifact spec.

    Write `tests/unit/canonical.test.ts` covering all 7 behaviors above using vitest. For env var tests, use `vi.stubEnv` or import-mocking; for the throw test, dynamic import inside a try/catch. For Test 7, write a `const m: PageMeta = { title, description, path: '/', dateModified: '2026-06-15' }` fixture and rely on `pnpm check` to catch type regressions.
  </action>
  <verify>
    <automated>pnpm test:unit -- --run canonical &amp;&amp; pnpm check</automated>
  </verify>
  <done>
    - All 7 vitest tests pass
    - src/lib/seo/defaults.ts throws on unset PUBLIC_SITE_URL (no localhost default — FND-07)
    - src/lib/constants/brand.ts has TODO markers ONLY for unknowns from UNKNOWNS.md (practitioner name, phone, instagram handle) — every other field is real
    - PageMeta type includes optional dateModified (BLOCKER-3 plumbing for SEO-09)
    - Plan 01's temporary defaults stub is removed (no duplicate `defaults` export)
    - per D-04, D-08 (BRAND areaServed matches Schema.org Service.areaServed locked decision)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: <Head>, <PageTitle>, <SiteNav>, <SiteFooter> primitives + mount in root layout</name>
  <files>src/lib/components/Head.svelte, src/lib/components/PageTitle.svelte, src/lib/components/SiteNav.svelte, src/lib/components/SiteFooter.svelte, src/routes/+layout.svelte, tests/unit/head.test.ts, tests/unit/page-title.test.ts, tests/unit/landmarks.test.ts</files>
  <read_first>
    - .planning/phases/00-foundation-seo-scaffolding/00-RESEARCH.md (§11 `src/lib/components/Head.svelte`, §11 `src/lib/components/PageTitle.svelte`, §3 Pattern 4 Head/PageTitle/JsonLd primitives, Pitfall #2)
    - seo-aeo-samenvatting-checklist.md §A (canonical per pagina, hreflang)
    - src/lib/seo/types.ts (just created)
    - src/lib/seo/defaults.ts (just created)
    - src/routes/+layout.svelte (current minimal version from Plan 01)
    - .planning/REQUIREMENTS.md (SEO-03 semantic landmarks; SEO-06 OG + Twitter)
  </read_first>
  <behavior>
    - Test 1: rendering <Head meta={...}> produces a <title> string of form `${meta.title}` for root or `${meta.title} | TRINITY Breath & Healing` for non-root
    - Test 2: rendering <Head meta={...}> produces <link rel='canonical' href='${SITE_URL}${meta.path}'>
    - Test 3: rendering <Head meta={...}> produces <link rel='alternate' hreflang='nl'> AND <link rel='alternate' hreflang='x-default'> with same href as canonical
    - Test 4: rendering <Head meta={..., og: { type: 'article' }}> emits og:type=article (default is 'website')
    - Test 5: rendering <Head meta={..., noindex: true}> emits <meta name='robots' content='noindex,nofollow'> (used for Vercel preview env later, per RESEARCH security domain)
    - Test 6: rendering <PageTitle>Hello</PageTitle> produces exactly one <h1> with text 'Hello'
    - Test 7: <Head> reads meta strictly from $props() — does not depend on $state or $effect (Pitfall #2)
    - Test 8: rendering <Head meta={...}> emits <meta property='og:locale' content='nl_NL'> (WARNING-3 — assert og:locale is in output, not just action prose)
    - Test 9: rendering <SiteNav /> produces a single <nav> with attribute aria-label='Hoofdnavigatie' and no <a> link children (empty in Phase 0)
    - Test 10: rendering <SiteFooter /> produces a single <footer> with aria-labelledby='site-footer-heading' AND contains an <h2 id='site-footer-heading'> labelling element
    - Test 11: after rendering the root layout slot with a child page, the DOM contains exactly one <nav>, exactly one <main>, exactly one <footer> in that source order
  </behavior>
  <action>
    Write `src/lib/components/Head.svelte` per RESEARCH §11 example, adjusted for Svelte 5 runes: `<script lang="ts">` block imports `PageMeta` type from `$lib/seo/types` and `SITE_URL` from `$lib/seo/defaults`; declares `let { meta }: { meta: PageMeta } = $props();`. Compute `titleFull = $derived(meta.path === '/' ? meta.title : meta.title + ' | TRINITY Breath & Healing')`; `canonical = $derived(SITE_URL + meta.path)`; `ogImage = $derived(meta.og?.image ?? SITE_URL + '/og-default.jpg')`. Emit via `<svelte:head>`: `<title>{titleFull}</title>`, `<meta name='description' content={meta.description}>`, `<link rel='canonical' href={canonical}>`, `<link rel='alternate' hreflang='nl' href={canonical}>`, `<link rel='alternate' hreflang='x-default' href={canonical}>`, OG block (og:title=titleFull, og:description, og:url=canonical, og:image, og:type=meta.og?.type ?? 'website', og:locale='nl_NL' — MUST be emitted; covered by Test 8 / WARNING-3), Twitter block (twitter:card=summary_large_image, twitter:title=titleFull, twitter:description, twitter:image=ogImage). Conditional `{#if meta.noindex}<meta name='robots' content='noindex,nofollow'>{/if}`.

    Write `src/lib/components/PageTitle.svelte` per RESEARCH §11 example: `<script lang="ts"> let { children }: { children: import('svelte').Snippet } = $props();</script> <h1>{@render children()}</h1>` followed by a scoped `<style>` block setting `font-family: var(--font-display); font-size: var(--font-size-3xl); line-height: var(--line-height-tight); color: var(--color-fg-forest);` (the tokens land in Plan 06; consume them as forward references — Svelte scoped CSS does not error on undefined vars).

    Write `src/lib/components/SiteNav.svelte` (BLOCKER-5 — SEO-03 landmark skeleton):
      - No script logic in Phase 0 (no props yet).
      - Markup: `<nav aria-label="Hoofdnavigatie"><!-- Phase 1 LND-XX populates the link list. Phase 0 ships the landmark only so initial HTML has <nav> for SEO-03 + Plan 08 audit gate. --></nav>`
      - Scoped `<style>` block reserved for Phase 1; can be empty.
      - Do NOT inline the link list — Phase 1 owns it. Do NOT use any BRAND constants here (those land in SiteFooter when Phase 1 lands LND-08 NAP block; Phase 0's SiteFooter is also empty of content).

    Write `src/lib/components/SiteFooter.svelte` (BLOCKER-5 — SEO-03 landmark skeleton):
      - Markup: `<footer aria-labelledby="site-footer-heading"><h2 id="site-footer-heading" class="sr-only">Site footer</h2><!-- Phase 1 LND-08 lands NAP block, KvK, social links. Phase 0 ships the labelled landmark only. --></footer>`
      - Scoped `<style>` block: include `.sr-only { position: absolute; width: 1px; height: 1px; overflow: hidden; clip: rect(0 0 0 0); white-space: nowrap; border: 0; }` OR rely on a global `.sr-only` utility class shipped in Plan 06's `static/global.css`. Since Plan 02 ships before Plan 06, scope the rule locally to be safe.

    Modify `src/routes/+layout.svelte` to import and mount in this exact source order:
      1. `<Head meta={page.data.meta} />` (in <svelte:head> via the primitive)
      2. `<SiteNav />` — must render BEFORE <main> in the DOM, so the prerendered initial HTML has <nav> before <main>
      3. `<main>{@render children()}</main>` (unchanged)
      4. `<SiteFooter />` — must render AFTER </main>
      Use `$app/state` (Svelte 5 pattern) — `import { page } from '$app/state';` then `<Head meta={page.data.meta} />`. If `$app/state` is unavailable in the installed SvelteKit version, fall back to `import { page } from '$app/stores';` with `<Head meta={$page.data.meta} />`. Confirm which pattern works at implementation time. The `<html lang="nl">` attribute is NOT set here — Plan 01 sets it on `src/app.html`.

    Write `tests/unit/head.test.ts` using `@testing-library/svelte` (install if not already in Plan 01 deps — Wave 0 should have it; if not, install now via pnpm and update Plan 01 SUMMARY). Test behaviors 1-5, 7, AND 8 (og:locale=nl_NL emission). Mock `PUBLIC_SITE_URL=https://test.example.com` via `vi.stubEnv`.

    Write `tests/unit/page-title.test.ts` covering Test 6.

    Write `tests/unit/landmarks.test.ts` covering Tests 9, 10, 11. For Test 11, render the layout component with a minimal child page slot fixture (or render +layout.svelte directly with a stub `page.data`) and assert via `@testing-library/svelte` that exactly one <nav>, <main>, <footer> exist in that order. Document order check via `container.querySelector('nav').compareDocumentPosition(container.querySelector('main'))` returning `Node.DOCUMENT_POSITION_FOLLOWING`.
  </action>
  <verify>
    <automated>pnpm test:unit -- --run head page-title landmarks &amp;&amp; pnpm check &amp;&amp; PUBLIC_SITE_URL=https://trinity-breath-healing.vercel.app pnpm build &amp;&amp; node -e "const h=require('node-html-parser').parse(require('fs').readFileSync('.svelte-kit/output/prerendered/pages/index.html','utf8'));const t=h.querySelector('title')?.text||'';const d=h.querySelector('meta[name=description]')?.getAttribute('content')||'';const c=h.querySelector('link[rel=canonical]')?.getAttribute('href')||'';const hl=h.querySelectorAll('link[rel=alternate][hreflang]').length;const ogl=h.querySelector('meta[property=\"og:locale\"]')?.getAttribute('content')||'';const navs=h.querySelectorAll('nav').length;const mains=h.querySelectorAll('main').length;const foots=h.querySelectorAll('footer').length;if(!t)throw new Error('no title');if(!d)throw new Error('no description');if(!c.startsWith('https://'))throw new Error('canonical not absolute');if(hl<2)throw new Error('hreflang pair missing');if(ogl!=='nl_NL')throw new Error('og:locale not nl_NL, got '+ogl);if(navs<1)throw new Error('no <nav> landmark');if(mains!==1)throw new Error('expected 1 <main>, got '+mains);if(foots<1)throw new Error('no <footer> landmark');console.log('OK',{titleLen:t.length,descLen:d.length,canonical:c,hreflangCount:hl,ogLocale:ogl,navs,mains,foots});"</automated>
  </verify>
  <done>
    - All Svelte unit tests pass (head.test.ts + page-title.test.ts + landmarks.test.ts)
    - After `pnpm build`, the prerendered `.svelte-kit/output/prerendered/pages/index.html` contains: <title>, <meta name='description'>, <link rel='canonical' href='https://...'>, two hreflang links (nl + x-default), og:* block including og:locale=nl_NL (WARNING-3 closed), twitter:* block, AND <nav>, <main>, <footer> landmarks (BLOCKER-5 closed)
    - No `$state` / `$effect` reactivity inside <Head> (Pitfall #2)
    - SiteNav.svelte + SiteFooter.svelte exist as empty-content skeleton landmarks; Phase 1 LND populates them
    - per SEO-01..06 + SEO-03 + D-01 (Svelte 5 runes mode locked)
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route load() → <Head> prop | Typed PageMeta only; no path-derived or query-derived strings injected raw |
| <svelte:head> emission → prerendered HTML | Bytes go to public crawlers — must not contain injected scripts or unescaped user content |
| SiteNav / SiteFooter markup → prerendered HTML | Static skeleton — no user input until Phase 1 LND-08 lands NAP/footer content (which will be sourced from BRAND constants, not request data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-meta | Tampering | <Head> emission via <svelte:head> | mitigate | All meta values come from typed `PageMeta` populated by `+page.ts` load() — never from `url.searchParams`, never from path-pattern interpolation of user input. Svelte's built-in attribute escaping handles any embedded quotes. Plan 05 enforces the same discipline on stub pages. |
| T-00-noindex-preview | Information Disclosure | Vercel preview URLs indexed by search engines | accept (deferred to Plan 08) | The `meta.noindex` plumbing lands here; Plan 08 wires the env-conditional check (`PUBLIC_VERCEL_ENV !== 'production'` → noindex=true). Plan 02 only ships the plumbing. |
| T-00-landmark-skeleton | Tampering | empty SiteNav / SiteFooter shipping to production | accept | Phase 0 ships intentionally empty landmark skeletons so SEO-03 + Plan 08 audit gate land in Phase 0. Phase 1 fills them. Acceptable because empty `<nav>` and `<footer>` are valid HTML and do not display visible text to users beyond the visually-hidden footer heading. |

</threat_model>

<verification>

Phase-level checks:
- `pnpm test:unit -- --run` includes head.test.ts + page-title.test.ts + canonical.test.ts + landmarks.test.ts and all pass
- After `pnpm build`, the prerendered index.html contains all SEO tags AND <nav>+<main>+<footer> landmarks AND og:locale=nl_NL (asserted via the inline node-html-parser check in Task 2 verify command)
- `pnpm check` passes (TypeScript strict + svelte-check)
- No second `<title>` or duplicate canonical in the prerendered HTML

</verification>

<success_criteria>

- `<Head>` primitive emits all 13 SEO tag categories per SEO-01..06, including og:locale=nl_NL (WARNING-3 closed)
- `<PageTitle>` primitive renders exactly one `<h1>` per page (defense in depth alongside SEO-11 audit gate from Plan 08)
- `<SiteNav>` + `<SiteFooter>` skeleton landmarks render exactly one `<nav>` and one `<footer>` per page; Plan 08's audit gate asserts presence on every prerendered route (BLOCKER-5 closed)
- `PageMeta` type is the single TypeScript contract every route uses; non-conforming meta becomes a compile error
- `SITE_URL` propagation works at build time via `$env/dynamic/public.PUBLIC_SITE_URL`; preview vs production URLs differ correctly (Pitfall #13)

</success_criteria>

<output>
Create `.planning/phases/00-foundation-seo-scaffolding/00-02-SUMMARY.md` when done. Record: exact title length / description length emitted by the landing prerender; whether `$app/state` or `$app/stores` was used (note SvelteKit version); any deviations from RESEARCH §11 examples; confirm og:locale=nl_NL is present in initial HTML (WARNING-3); confirm the layout source order is <Head> → <SiteNav> → <main> → <SiteFooter> (BLOCKER-5).
</output>
