---
phase: 00-foundation-seo-scaffolding
plan: 04
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - static/robots.txt
  - src/lib/constants/routes.ts
  - src/routes/sitemap.xml/+server.ts
  - tests/unit/robots.test.ts
  - tests/unit/sitemap.test.ts
  - tests/unit/routes-manifest.test.ts
  - scripts/check-robots.sh
autonomous: true
requirements:
  - SEO-07
  - SEO-08

must_haves:
  truths:
    - "/robots.txt is served (via static/) and contains AI-crawler Allow blocks for OAI-SearchBot, ChatGPT-User, PerplexityBot, Perplexity-User, ClaudeBot, Claude-User, Google-Extended, Applebot-Extended placed BEFORE the User-agent: * line"
    - "/robots.txt contains Disallow: /api/ inside the wildcard block"
    - "/robots.txt contains 'Sitemap: https://trinity-breath-healing.vercel.app/sitemap.xml' line"
    - "/sitemap.xml is generated by src/routes/sitemap.xml/+server.ts at build time (export const prerender = true) and contains one <url><loc>...</loc></url> entry per route in src/lib/constants/routes.ts"
    - "Sitemap loc URLs are absolute and derived from SITE_URL env (Pitfall #15)"
    - "scripts/check-robots.sh asserts the order: every named AI/training bot's User-agent line index < the wildcard 'User-agent: *' line index"
    - "tests/unit/routes-manifest.test.ts asserts ALL_ROUTES contains exactly 15 entries with the exact paths from CONTEXT.md D-locked list"
  artifacts:
    - path: "static/robots.txt"
      provides: "8 named bot Allow blocks BEFORE wildcard; inline DECISION comment per Pitfall #10; Sitemap reference"
      contains: "OAI-SearchBot"
    - path: "src/lib/constants/routes.ts"
      provides: "Single source-of-truth ALL_ROUTES const array of 15 routes: /, /werkwijze, /over-mij, /behandelingen, /contact, /diensten, /diensten/mahatma-healing, /diensten/goldhealing, /diensten/raster-energie, /diensten/spinal-touch, /blog, /artikelen, /faq, /privacyverklaring, /algemene-voorwaarden"
      contains: "/werkwijze"
    - path: "src/routes/sitemap.xml/+server.ts"
      provides: "Prerendered XML endpoint; iterates ALL_ROUTES; emits valid XML 1.0 with xmlns sitemaps schema; lastmod = build date YYYY-MM-DD; changefreq=monthly; priority=1.0 for /, 0.5 for others; Content-Type: application/xml; Cache-Control: public, max-age=3600, s-maxage=86400"
      contains: "urlset"
    - path: "scripts/check-robots.sh"
      provides: "Bash script that asserts named-bot Allow blocks appear before User-agent: * in static/robots.txt; exits 1 with diagnostic on violation"
      contains: "User-agent: \\*"
  key_links:
    - from: "static/robots.txt"
      to: "/sitemap.xml"
      via: "Sitemap: line"
      pattern: "Sitemap: https"
    - from: "src/routes/sitemap.xml/+server.ts"
      to: "src/lib/constants/routes.ts"
      via: "import ALL_ROUTES"
      pattern: "ALL_ROUTES"
    - from: "src/routes/sitemap.xml/+server.ts"
      to: "SITE_URL"
      via: "import from $lib/seo/defaults"
      pattern: "SITE_URL"
---

<objective>
Ship the crawler-and-AI surface area: a static `robots.txt` that explicitly allows the 8 named AI/training bots BEFORE the wildcard `User-agent: *` (the #1 AEO trap is the wildcard `Disallow` shadowing the allows — Pitfall #2 / #10), plus a prerendered `sitemap.xml` endpoint that iterates the 15-route manifest. Also lands `src/lib/constants/routes.ts` as the single source-of-truth that Plans 05 (stubs), Phase 1 (nav), Phase 5 (Search Console submission) all read.

Purpose: Per `seo-aeo-samenvatting-checklist.md` Deel 2 + RESEARCH Pitfalls #2 + #10, a wildcard `Disallow` placed before specific `Allow` rules silently kills AI citation surface. This plan ships the order-protected robots.txt + the route manifest that drives sitemap + nav for the rest of the project.
Output: `static/robots.txt` + `src/routes/sitemap.xml/+server.ts` + `src/lib/constants/routes.ts` + order-enforcement CI script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/00-foundation-seo-scaffolding/00-CONTEXT.md
@.planning/phases/00-foundation-seo-scaffolding/00-RESEARCH.md
@.planning/phases/00-foundation-seo-scaffolding/00-VALIDATION.md
@seo-aeo-samenvatting-checklist.md
@.planning/phases/00-foundation-seo-scaffolding/00-01-SUMMARY.md
@src/lib/seo/defaults.ts

<interfaces>
From Plan 01: SITE_URL is exported from `$lib/seo/defaults` (added in Plan 02 but the import is forward-safe via wave ordering — sitemap endpoint runs at build time after the lib lands).

The 15 routes (locked in CONTEXT.md D-04):
  /                          (kind: landing)
  /werkwijze                 (stub)
  /over-mij                  (stub)
  /behandelingen             (stub)
  /contact                   (stub — Phase 2 deepens)
  /diensten                  (stub)
  /diensten/mahatma-healing  (service-stub)
  /diensten/goldhealing      (service-stub)
  /diensten/raster-energie   (service-stub)
  /diensten/spinal-touch     (service-stub)
  /blog                      (stub)
  /artikelen                 (stub)
  /faq                       (stub)
  /privacyverklaring         (stub — Phase 2 deepens)
  /algemene-voorwaarden      (stub — Phase 2 deepens)

The 8 AI/training bots (locked in CONTEXT.md D-03):
  OAI-SearchBot, ChatGPT-User, PerplexityBot, Perplexity-User, ClaudeBot, Claude-User  (search/RAG)
  Google-Extended, Applebot-Extended  (training)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/lib/constants/routes.ts (15-route manifest) + unit test</name>
  <files>src/lib/constants/routes.ts, tests/unit/routes-manifest.test.ts</files>
  <read_first>
    - .planning/phases/00-foundation-seo-scaffolding/00-RESEARCH.md (§11 `src/lib/constants/routes.ts` + Pattern 9 stub strategy)
    - .planning/phases/00-foundation-seo-scaffolding/00-CONTEXT.md (Site structure section — exact 15-route list)
    - .planning/REQUIREMENTS.md (FND-03 reserved 200-response stub pages)
    - seo-aeo-samenvatting-checklist.md §A (XML sitemap requirement)
  </read_first>
  <behavior>
    - Test: ALL_ROUTES.length === 15
    - Test: ALL_ROUTES.map(r => r.path) deep-equals the 15-route array above in exact order
    - Test: ALL_ROUTES[0].kind === 'landing' AND ALL_ROUTES[0].path === '/'
    - Test: ALL_ROUTES.filter(r => r.kind === 'service-stub').length === 4 AND covers the 4 modality slugs
    - Test: no path contains a trailing slash (matches `trailingSlash: 'never'` policy from Plan 01)
    - Test: every entry has fields { path, title, kind } at minimum
  </behavior>
  <action>
    Write `src/lib/constants/routes.ts` per RESEARCH §11 example, exactly: `export const ALL_ROUTES = [ { path: '/', title: 'TRINITY Breath & Healing', kind: 'landing' }, { path: '/werkwijze', title: 'Werkwijze', kind: 'stub' }, { path: '/over-mij', title: 'Over mij', kind: 'stub' }, { path: '/behandelingen', title: 'Behandelingen', kind: 'stub' }, { path: '/contact', title: 'Contact', kind: 'stub' }, { path: '/diensten', title: 'Diensten', kind: 'stub' }, { path: '/diensten/mahatma-healing', title: 'Mahatma Healing', kind: 'service-stub' }, { path: '/diensten/goldhealing', title: 'Goldhealing', kind: 'service-stub' }, { path: '/diensten/raster-energie', title: 'Raster Energie', kind: 'service-stub' }, { path: '/diensten/spinal-touch', title: 'Spinal Touch', kind: 'service-stub' }, { path: '/blog', title: 'Blog', kind: 'stub' }, { path: '/artikelen', title: 'Artikelen', kind: 'stub' }, { path: '/faq', title: 'Veelgestelde vragen', kind: 'stub' }, { path: '/privacyverklaring', title: 'Privacyverklaring', kind: 'stub' }, { path: '/algemene-voorwaarden', title: 'Algemene voorwaarden', kind: 'stub' } ] as const;` Include type-narrowing export `export type RouteEntry = typeof ALL_ROUTES[number];`.

    Write `tests/unit/routes-manifest.test.ts` verifying all 6 behaviors above.
  </action>
  <verify>
    <automated>pnpm test:unit -- --run routes-manifest &amp;&amp; pnpm check</automated>
  </verify>
  <done>
    - tests/unit/routes-manifest.test.ts passes
    - per D-04 (14 stubs + landing locked)
    - per D-08 (4 service slugs locked in `/diensten/<slug>`)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: static/robots.txt + scripts/check-robots.sh + order-enforcement test</name>
  <files>static/robots.txt, scripts/check-robots.sh, tests/unit/robots.test.ts</files>
  <read_first>
    - .planning/phases/00-foundation-seo-scaffolding/00-RESEARCH.md (§11 `static/robots.txt` verbatim text + Pattern 5 robots.txt + Pitfall #2 wildcard shadow + Pitfall #10 training crawler decision documentation)
    - .planning/phases/00-foundation-seo-scaffolding/00-CONTEXT.md (SEO/crawler config section — 8 named bots + training crawlers ALLOWED)
    - .planning/REQUIREMENTS.md (SEO-08 exact bot list)
    - seo-aeo-samenvatting-checklist.md Deel 2 §3 (AI-crawler allow + sitemap reference)
  </read_first>
  <behavior>
    - Test: static/robots.txt file exists
    - Test: file contains line `User-agent: OAI-SearchBot` followed by `Allow: /` (similarly for the other 7 bots)
    - Test: every named bot's `User-agent:` line index is LESS THAN the line index of `User-agent: *`
    - Test: the wildcard block contains both `Allow: /` and `Disallow: /api/`
    - Test: file contains a line starting with `Sitemap: https://` ending in `/sitemap.xml`
    - Test: file contains the literal DECISION comment from Pitfall #10 mentioning CONTEXT.md
    - Test: scripts/check-robots.sh exits 0 on the committed file AND exits 1 on a synthetic violation (wildcard moved before named bots)
  </behavior>
  <action>
    Write `static/robots.txt` verbatim from RESEARCH §11 (under "static/robots.txt (SEO-08)"). Header comment: `# robots.txt — Trinity Breath & Healing`. Then 6 search/RAG bot blocks (OAI-SearchBot, ChatGPT-User, PerplexityBot, Perplexity-User, ClaudeBot, Claude-User) each with `Allow: /`. Then DECISION comment block per Pitfall #10: `# DECISION: training crawlers (Google-Extended, Applebot-Extended) ALLOWED per CONTEXT.md (2026-06-15).` `# Rationale: wellness content has low IP risk + high long-term citation surface in Gemini + Apple Intelligence baseline knowledge. Change requires architectural discussion.` Then 2 training bot blocks. Then `# Wildcard fallback.` then `User-agent: *` then `Allow: /` then `Disallow: /api/`. Then blank line and `Sitemap: https://trinity-breath-healing.vercel.app/sitemap.xml`.

    Write `scripts/check-robots.sh` (bash, POSIX-portable; first line `#!/usr/bin/env bash` + `set -euo pipefail`). Logic: parse static/robots.txt; find line index of each named bot's `User-agent: <bot>` line and line index of `User-agent: *`; if any named bot's index >= wildcard's index, print diagnostic + exit 1; if Sitemap line missing, print + exit 1; otherwise exit 0. Use `grep -n` to capture line numbers; iterate with `for bot in OAI-SearchBot ChatGPT-User PerplexityBot Perplexity-User ClaudeBot Claude-User Google-Extended Applebot-Extended; do ...; done`. Mark executable (note: on Windows/git-bash this is via `git update-index --chmod=+x` on commit — call it out in the SUMMARY).

    Write `tests/unit/robots.test.ts` using node fs to read `static/robots.txt`. Cover all 7 behaviors above. For the synthetic-violation test (behavior 7), write a temp file with reordered content into a tmpdir, run `scripts/check-robots.sh <tmpfile>` via `child_process.execSync`, assert non-zero exit. (Modify check-robots.sh to accept an optional filename arg defaulting to `static/robots.txt`.)
  </action>
  <verify>
    <automated>pnpm test:unit -- --run robots &amp;&amp; bash scripts/check-robots.sh</automated>
  </verify>
  <done>
    - tests/unit/robots.test.ts passes
    - scripts/check-robots.sh exits 0 on the committed file
    - 8 named bots' Allow blocks appear before `User-agent: *` in source order
    - DECISION comment present per Pitfall #10
    - per D-03 (training crawlers allowed) + per Phase 0 success criterion #2
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: src/routes/sitemap.xml/+server.ts + sitemap unit + integration test</name>
  <files>src/routes/sitemap.xml/+server.ts, tests/unit/sitemap.test.ts</files>
  <read_first>
    - .planning/phases/00-foundation-seo-scaffolding/00-RESEARCH.md (§11 `src/routes/sitemap.xml/+server.ts` verbatim + Pattern 5)
    - src/lib/constants/routes.ts (Task 1 output)
    - src/lib/seo/defaults.ts (SITE_URL — created in Plan 02)
    - seo-aeo-samenvatting-checklist.md §A (XML sitemap)
  </read_first>
  <behavior>
    - Test: GET handler returns Response with Content-Type 'application/xml'
    - Test: response body parses as valid XML (uses node `DOMParser` shim or simple regex assertions)
    - Test: response body root element is `<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">`
    - Test: response body contains exactly 15 `<url>` children (matches ALL_ROUTES.length)
    - Test: every `<loc>` is absolute (starts with `https://`) and matches `${SITE_URL}${route.path}`
    - Test: landing entry has `<priority>1.0</priority>`; all others have `<priority>0.5</priority>`
    - Test: file declares `export const prerender = true` (build-time generation)
    - Test (integration via build): after `pnpm build`, `.svelte-kit/output/prerendered/pages/sitemap.xml` exists and contains 15 `<loc>` entries
  </behavior>
  <action>
    Write `src/routes/sitemap.xml/+server.ts` per RESEARCH §11. Imports: `import type { RequestHandler } from './$types';` `import { SITE_URL } from '$lib/seo/defaults';` `import { ALL_ROUTES } from '$lib/constants/routes';`. Export `prerender = true`. GET handler builds the XML body iterating `ALL_ROUTES`. Each `<url>` block has `<loc>${SITE_URL}${r.path}</loc>`, `<lastmod>${new Date().toISOString().split('T')[0]}</lastmod>`, `<changefreq>monthly</changefreq>`, `<priority>${r.path === '/' ? '1.0' : '0.5'}</priority>`. Wraps in `<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">...</urlset>`. Returns `new Response(body, { headers: { 'Content-Type': 'application/xml', 'Cache-Control': 'public, max-age=3600, s-maxage=86400' } })`.

    Write `tests/unit/sitemap.test.ts`. Direct-import the GET handler from the +server module (vitest can do this — use the `?import` resolution or stub `$lib/seo/defaults` via vi.mock to provide a known SITE_URL). Cover behaviors 1-7. For behavior 8 (integration check on prerender output), add a second describe block that runs only if `process.env.RUN_BUILD_TESTS` is set OR move that assertion to Plan 08's html-audit script and verify here via `pnpm build` exit + presence of the file in the verify command.
  </action>
  <verify>
    <automated>pnpm test:unit -- --run sitemap &amp;&amp; PUBLIC_SITE_URL=https://trinity-breath-healing.vercel.app pnpm build &amp;&amp; test -f .svelte-kit/output/prerendered/pages/sitemap.xml &amp;&amp; node -e "const x=require('fs').readFileSync('.svelte-kit/output/prerendered/pages/sitemap.xml','utf8');const locs=(x.match(/<loc>/g)||[]).length;if(locs!==15)throw new Error('expected 15 <loc>, got '+locs);if(!x.includes('https://trinity-breath-healing.vercel.app/'))throw new Error('SITE_URL not applied');if(!x.includes('<priority>1.0</priority>'))throw new Error('landing priority missing');console.log('OK',{locs});"</automated>
  </verify>
  <done>
    - tests/unit/sitemap.test.ts passes
    - `.svelte-kit/output/prerendered/pages/sitemap.xml` exists after build and contains 15 absolute `<loc>` entries
    - Landing has priority 1.0; others 0.5
    - per SEO-07 + Phase 0 success criterion #2
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static/robots.txt → public crawlers | Plain text policy file; bad ordering silently denies traffic |
| sitemap.xml endpoint → public crawlers | XML output drives indexation; URLs must be derived from SITE_URL env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-crawl | Spoofing of intent | static/robots.txt user-agent ordering | mitigate | (1) Pitfall #10 DECISION comment inline; (2) scripts/check-robots.sh enforces named-bot-before-wildcard ordering; (3) tests/unit/robots.test.ts verifies file structure on every commit; (4) Plan 08 wires check-robots.sh into the build-and-audit job. Future contributors copying a "block training crawlers" snippet without reading CONTEXT.md will fail CI. |
| T-00-stub-discovery | Information Disclosure | sitemap.xml exposing every stub URL | accept | All 15 routes are intentionally indexable per D-04 (zero-301 v2 migration guarantee). Stub UX is "Komt binnenkort" + link home (Plan 05 ships); the stubs return 200 + per-page JSON-LD so AI engines see them as legitimate placeholder pages, not duplicates or thin content. |

</threat_model>

<verification>

Phase-level checks:
- `pnpm test:unit -- --run` includes routes-manifest + robots + sitemap, all pass
- `bash scripts/check-robots.sh` exits 0 (committed file is correctly ordered)
- After `pnpm build`, `.svelte-kit/output/prerendered/pages/sitemap.xml` exists with 15 absolute `<loc>` entries
- `static/robots.txt` is served by Vercel at `/robots.txt` (post-deploy verification — handled in Plan 09)

</verification>

<success_criteria>

- All 8 named AI/training bots are explicitly allowed in `robots.txt` BEFORE `User-agent: *` (Phase 0 success criterion #2)
- Sitemap is auto-generated from `ALL_ROUTES` (Phase 0 success criterion #2)
- DECISION rationale is inline in robots.txt (Pitfall #10)
- `src/lib/constants/routes.ts` is the single source-of-truth that Plans 05 (stubs), Plan 02's Head canonical, and Phase 5 verification all consume

</success_criteria>

<output>
Create `.planning/phases/00-foundation-seo-scaffolding/00-04-SUMMARY.md` when done. Record: exact contents of static/robots.txt (or diff if it deviates from RESEARCH §11); whether check-robots.sh needed bash-on-Windows compatibility fixes; output of the prerendered sitemap.xml `<loc>` count.
</output>
