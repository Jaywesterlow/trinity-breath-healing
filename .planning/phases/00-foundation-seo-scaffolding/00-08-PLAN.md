---
phase: 00-foundation-seo-scaffolding
plan: 08
type: execute
wave: 3
depends_on: ["00-02", "00-03", "00-04", "00-05", "00-06", "00-07"]
files_modified:
  - scripts/check-html.ts
  - scripts/validate-json-ld.ts
  - scripts/grep-placeholders.sh
  - scripts/check-initial-html-ai.ts
  - tests/integration/html-audit.spec.ts
  - tests/integration/synthetic-violations.spec.ts
  - .github/workflows/ci.yml
  - lighthouserc.json
  - .pa11yci.json
  - package.json
autonomous: true
requirements:
  - FND-06
  - FND-07
  - FND-08
  - FND-09
  - FND-10
  - SEO-09
  - SEO-11
  - SCH-08
  - PRF-08
  - A11Y-05

must_haves:
  truths:
    - "scripts/check-html.ts walks .svelte-kit/output/prerendered/pages/**/*.html and asserts per file: exactly one <h1>, <title> length 50-60, <meta name='description'> length 150-160, absolute canonical, semantic landmarks (<main>, <nav>, <footer> at least once across the doc), hreflang nl + x-default, og:* + twitter:* present"
    - "scripts/validate-json-ld.ts walks the same files and asserts: exactly one <script type='application/ld+json'> per page, parses as valid JSON, has @context and @graph array, every node in @graph has @type; for the landing it specifically asserts Organization + ProfessionalService + Person + WebSite + 4 Service nodes present"
    - "scripts/check-initial-html-ai.ts runs curl with each AI crawler User-Agent (OAI-SearchBot, ChatGPT-User, PerplexityBot, ClaudeBot, Google-Extended) against a target URL and verifies the SAME assertions as check-html.ts (no cloaking — AI crawlers see what humans see)"
    - "scripts/grep-placeholders.sh greps src/ + static/ for TODO and PLACEHOLDER strings, prints them, but is non-blocking in Phase 0 (continue-on-error: true in CI) — flips to blocking at Phase 4 LGL-11"
    - "tests/integration/synthetic-violations.spec.ts proves: a deliberately broken H1 (two H1s in a route), missing JSON-LD, missing canonical, missing meta description, oversized image without dimensions, and missing alt EACH cause the respective gate to exit non-zero"
    - ".github/workflows/ci.yml has job build-and-audit running on push and pull_request that executes check + build + check-html + validate-json-ld + check-tokens + no-shared-css + check-robots + grep-placeholders (continue-on-error)"
    - ".github/workflows/ci.yml has job lighthouse-and-a11y triggered by pull_request that polls GitHub deployments API for the Vercel preview URL and runs @lhci/cli + pa11y-ci against it"
    - "lighthouserc.json budgets: LCP error at 2500ms, CLS error at 0.1, SEO score error at 1.0, Accessibility error at 0.95"
    - ".pa11yci.json WCAG 2.2 AA standard"
    - "All 4 gates (HTML audit, JSON-LD audit, Lighthouse, pa11y) are required status checks (note: marking them required happens in Plan 09 GitHub branch protection step; Plan 08 only ensures they exist and pass)"
  artifacts:
    - path: "scripts/check-html.ts"
      provides: "node-html-parser-based audit of prerendered HTML; walks .svelte-kit/output/prerendered/pages/**/*.html; checks H1 count, title length 50-60, meta description length 150-160, absolute canonical, hreflang nl+x-default presence, og:title+og:description+og:url presence, twitter:card+twitter:title presence, <main> presence; prints diagnostics with file:reason; exits 1 on any violation"
      contains: "querySelectorAll"
    - path: "scripts/validate-json-ld.ts"
      provides: "Walks same files; parses every <script type='application/ld+json'>; asserts exactly one per page; asserts @context + @graph; asserts every @graph entry has @type; for landing index.html additionally asserts Organization + ProfessionalService + Person + WebSite + 4 Service @ids present"
      contains: "@graph"
    - path: "scripts/check-initial-html-ai.ts"
      provides: "Take --url and --ua args; curl the URL with the UA; pipe through node-html-parser; run the same checks as check-html.ts; print verdict per UA"
      contains: "User-Agent"
    - path: "scripts/grep-placeholders.sh"
      provides: "Bash script: grep -rE 'TODO|PLACEHOLDER' src/ static/ — prints all matches; exit 0 always in Phase 0 (informational only); Phase 4 LGL-11 will modify to exit 1 on matches"
      contains: "TODO"
    - path: ".github/workflows/ci.yml"
      provides: "Wired workflow per RESEARCH §11 — two jobs: build-and-audit (always) and lighthouse-and-a11y (pull_request only); both succeed for Plan 08 acceptance"
      contains: "lhci"
  key_links:
    - from: ".github/workflows/ci.yml job build-and-audit"
      to: "all 6 gate scripts"
      via: "pnpm run audit:html, audit:json-ld, etc."
      pattern: "audit:html"
    - from: ".github/workflows/ci.yml job lighthouse-and-a11y"
      to: "Vercel preview URL"
      via: "actions/github-script polling deployments API"
      pattern: "listDeployments"
---

<objective>
Wire all CI quality gates that catch the failures Phase 0 is designed to prevent: HTML/SEO audits, JSON-LD validation, initial-HTML AI-crawler smoke test, Lighthouse CI budgets (LCP/INP/CLS + SEO/A11y scores), pa11y WCAG 2.2 AA, placeholder grep (non-blocking until Phase 4), token/CSS-shape enforcement, robots.txt order. Replace the placeholder ci.yml steps from Plan 01 with real implementations.

Purpose: The Phase 0 RESEARCH validation architecture says "this phase ships the gates themselves." Without these scripts, the prerendered output can silently degrade (one extra H1, a stray meta in client JS, a duplicate JSON-LD script) and no one notices until production launch fails Lighthouse. This plan makes "the build succeeded" mean "the SEO/AEO contract is intact."
Output: 4 audit scripts + synthetic-violation test suite + complete ci.yml + Lighthouse + pa11y configs. After this plan, every PR runs the full gate set and merges are blocked on failure (branch protection wired in Plan 09).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/00-foundation-seo-scaffolding/00-CONTEXT.md
@.planning/phases/00-foundation-seo-scaffolding/00-RESEARCH.md
@.planning/phases/00-foundation-seo-scaffolding/00-VALIDATION.md
@seo-aeo-samenvatting-checklist.md
@.planning/phases/00-foundation-seo-scaffolding/00-01-SUMMARY.md
@.planning/phases/00-foundation-seo-scaffolding/00-02-SUMMARY.md
@.planning/phases/00-foundation-seo-scaffolding/00-03-SUMMARY.md
@.planning/phases/00-foundation-seo-scaffolding/00-04-SUMMARY.md
@.planning/phases/00-foundation-seo-scaffolding/00-05-SUMMARY.md
@.planning/phases/00-foundation-seo-scaffolding/00-06-SUMMARY.md
@.planning/phases/00-foundation-seo-scaffolding/00-07-SUMMARY.md
@src/lib/seo/stub-meta.ts
@src/lib/schema/buildGraph.ts
@scripts/check-robots.sh
@scripts/no-shared-css.sh
@scripts/check-tokens.sh
@.github/workflows/ci.yml

<interfaces>
From upstream plans:
- scripts/check-robots.sh (Plan 04) — already in repo, must be invoked from ci.yml
- scripts/no-shared-css.sh + scripts/check-tokens.sh (Plan 06) — already in repo, must be invoked
- 15 prerendered HTML files in .svelte-kit/output/prerendered/pages/ (Plan 05)
- node-html-parser package installed (Plan 01)
- @lhci/cli + pa11y-ci + structured-data-testing-tool packages installed (Plan 01)

Audit-output convention:
  - On violation: print `✘ <file>: <reason>` to stderr; exit 1 at end
  - On success: print `<gate-name> passed: N file(s) checked.`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scripts/check-html.ts + scripts/validate-json-ld.ts + scripts/check-initial-html-ai.ts + scripts/grep-placeholders.sh</name>
  <files>scripts/check-html.ts, scripts/validate-json-ld.ts, scripts/check-initial-html-ai.ts, scripts/grep-placeholders.sh, tests/integration/synthetic-violations.spec.ts</files>
  <read_first>
    - .planning/phases/00-foundation-seo-scaffolding/00-RESEARCH.md (§11 `scripts/check-html.ts` verbatim, §11 `scripts/validate-json-ld.ts` verbatim, §3 Pattern 12 Post-build HTML+JSON-LD audit, §3 Pattern 15 Placeholder grep gate)
    - .planning/phases/00-foundation-seo-scaffolding/00-VALIDATION.md (Per-Task Verification Map + Wave 0 Gaps)
    - .planning/REQUIREMENTS.md (SEO-11 + SCH-08 + LGL-11 acceptance criteria)
    - src/lib/seo/stub-meta.ts (STUB_META titles/descriptions — gate must accept the lengths shipped in Plan 05)
    - seo-aeo-samenvatting-checklist.md §A
  </read_first>
  <behavior>
    - Test: scripts/check-html.ts exits 0 on the committed prerendered output (after `pnpm build`)
    - Test: synthetic-violations.spec.ts mutates a temp copy of an HTML file to add a second <h1>, runs check-html.ts on it, asserts exit code 1 AND stderr mentions the file + 'expected 1 <h1>'
    - Test: similar synthetic violations for missing title, oversized meta description, missing canonical — each gate fires
    - Test: scripts/validate-json-ld.ts exits 0 on the committed output; exits 1 when a synthetic mutation (a) removes the <script type='application/ld+json'>, (b) inserts a second one, (c) corrupts the JSON, (d) removes the Organization node from landing
    - Test: scripts/check-initial-html-ai.ts called with `--url file://...index.html --ua OAI-SearchBot` exits 0 with valid output (use file:// for offline testing; live URLs handled by Plan 09)
    - Test: scripts/grep-placeholders.sh ALWAYS exits 0 in Phase 0 (prints matches without failing)
  </behavior>
  <action>
    Write `scripts/check-html.ts` per RESEARCH §11. Use `node-html-parser`. Walk `.svelte-kit/output/prerendered/pages/**/*.html`. For each HTML file:
      - assert `querySelectorAll('h1').length === 1` else `✘ <path>: expected 1 <h1>, found N`
      - extract title text; assert `length >= 50 && length <= 60` (allow stubs +/-2 chars only if the SUMMARY from Plan 05 documents the variance; if Plan 05 shipped lengths outside 50-60, raise the upper bound to match the actual lengths and document in SUMMARY)
      - extract `meta[name='description']` content; assert `length >= 150 && length <= 160` (same allowance for stubs)
      - extract `link[rel='canonical']` href; assert non-empty AND starts with `https://`
      - extract `link[rel='alternate'][hreflang='nl']`; assert present
      - extract `link[rel='alternate'][hreflang='x-default']`; assert present
      - extract `meta[property='og:title']`, `og:description`, `og:url`, `og:image`, `og:type`, `og:locale`; assert all present
      - extract `meta[name='twitter:card']`, `twitter:title`, `twitter:description`; assert present
      - extract `main` element; assert present (SEO-03 semantic landmark)
    Track failure count; exit 1 if any failure. Print `HTML audit passed: N file(s) checked.` on success.

    Write `scripts/validate-json-ld.ts` per RESEARCH §11. For each HTML file:
      - extract `script[type='application/ld+json']`; assert exactly 1
      - JSON.parse the text content; assert `parsed['@context'] === 'https://schema.org'`
      - assert `Array.isArray(parsed['@graph'])`
      - assert every node in @graph has `@type` string
      - if file path is `index.html` (landing), additionally assert types include: 'Organization', 'ProfessionalService', 'Person', 'WebSite', 'Service' (at least 4); collect Service `@id`s and assert all 4 modality slugs present
      - if file path is anything else (stubs), assert types include 'BreadcrumbList' AND 'WebPage'
      - if file path matches `diensten/<modality>.html`, additionally assert a Service node with @id matching `#service-<modality>` is present
    Exit 1 on failure.

    Write `scripts/check-initial-html-ai.ts`. Args: `--url <URL>` `--ua <UserAgent>`. Use `fetch` with `headers: { 'User-Agent': <ua> }`. Pipe response body through node-html-parser. Run the SAME assertions as check-html.ts. Print `AI crawler ${ua} sees: title=<...>, h1count=<...>, jsonLdNodes=<...>` summary line. Exit 1 on assertion fail. (Per CONTEXT.md success criterion #1: `curl -A 'OAI-SearchBot' <prod-url>` must return initial HTML with content + meta + JSON-LD; this script automates that.)

    Write `scripts/grep-placeholders.sh` per RESEARCH §3 Pattern 15. Logic: `grep -rEn 'TODO|PLACEHOLDER' src/ static/ || true`; always exit 0 (Phase 0 informational). Inline comment: `# LGL-11 plumbing: Phase 4 will flip this to exit 1 on any match. Phase 0 ships intentional TODO markers in static/global.css (placeholder design tokens) and src/lib/constants/brand.ts (practitioner name, phone, KvK, BIG).`

    Write `tests/integration/synthetic-violations.spec.ts` (Playwright or vitest — synthetic file mutations done in tempdir). For each of 6+ synthetic violations:
      1. Read a real prerendered HTML file into a string
      2. Mutate per scenario (add second <h1>, remove title, oversize meta, etc.)
      3. Write to a tmpdir
      4. Run the relevant audit script (`tsx scripts/check-html.ts` or `tsx scripts/validate-json-ld.ts`) pointed at the tmpdir via an env var or CLI arg (you may need to modify the scripts to accept `ROOT_DIR` env or `--root` arg to point at the tmpdir instead of the hardcoded `.svelte-kit/output/prerendered/pages`)
      5. Assert exit code 1 and stderr contains a meaningful diagnostic

    Add `package.json` scripts:
      - `audit:html`: `tsx scripts/check-html.ts`
      - `audit:json-ld`: `tsx scripts/validate-json-ld.ts`
      - `audit:initial-html-ai`: `tsx scripts/check-initial-html-ai.ts`
      - `audit:placeholders`: `bash scripts/grep-placeholders.sh`
      - `test:html-audit`: alias `audit:html`
      - `test:schema`: `npx structured-data-testing-tool` (will be configured against preview URL in CI)
      - `test:routes`: alias for the Playwright integration tests (already in Plan 01 scripts allowlist)
  </action>
  <verify>
    <automated>PUBLIC_SITE_URL=https://trinity-breath-healing.vercel.app pnpm build &amp;&amp; pnpm audit:html &amp;&amp; pnpm audit:json-ld &amp;&amp; bash scripts/grep-placeholders.sh &amp;&amp; pnpm test:integration -- synthetic-violations</automated>
  </verify>
  <done>
    - scripts/check-html.ts + validate-json-ld.ts + check-initial-html-ai.ts + grep-placeholders.sh exist
    - All 4 scripts exit 0 on the committed build output (after `pnpm build`)
    - synthetic-violations.spec.ts proves every gate fires on its synthetic violation
    - package.json has audit:* + test:html-audit + test:schema + test:routes scripts
    - per SEO-11 + SCH-08 + LGL-11 plumbing
  </done>
</task>

<task type="auto">
  <name>Task 2: Complete .github/workflows/ci.yml + lighthouserc.json + .pa11yci.json + tests/integration/html-audit.spec.ts</name>
  <files>.github/workflows/ci.yml, lighthouserc.json, .pa11yci.json, tests/integration/html-audit.spec.ts, package.json</files>
  <read_first>
    - .planning/phases/00-foundation-seo-scaffolding/00-RESEARCH.md (§11 `.github/workflows/ci.yml` verbatim, §11 `lighthouserc.json`, §11 `.pa11yci.json`, Pitfall #9 Lighthouse CI running against wrong URL, Pattern 11 Vercel preview detection)
    - .planning/REQUIREMENTS.md (FND-10 + PRF-08 + A11Y-05)
    - .planning/phases/00-foundation-seo-scaffolding/00-VALIDATION.md (Sampling Rate + Manual-Only Verifications)
    - scripts/check-html.ts (Task 1 output)
    - scripts/validate-json-ld.ts (Task 1 output)
    - scripts/check-robots.sh (Plan 04)
    - scripts/no-shared-css.sh + scripts/check-tokens.sh (Plan 06)
    - .github/workflows/ci.yml (Plan 01 skeleton — replace placeholders)
  </read_first>
  <action>
    Replace `.github/workflows/ci.yml` skeleton from Plan 01 with the full RESEARCH §11 example, ADAPTED:
      - Trigger: `pull_request` AND `push: branches: [main]`
      - Job `build-and-audit`:
        * checkout v4
        * setup-node v4 (node 20.x) + pnpm cache (use `pnpm/action-setup@v4` BEFORE setup-node so pnpm is on PATH)
        * `pnpm install --frozen-lockfile`
        * `pnpm check` (svelte-check + tsc)
        * `pnpm lint` (ESLint + Prettier check)
        * `pnpm build` with `env: PUBLIC_SITE_URL: https://trinity-breath-healing.vercel.app`
        * `pnpm test:unit -- --run` (all vitest unit tests)
        * `bash scripts/no-shared-css.sh` (FND-02)
        * `bash scripts/check-tokens.sh` (FND-04)
        * `bash scripts/check-robots.sh` (SEO-08)
        * `pnpm audit:html` (SEO-11 + FND-08)
        * `pnpm audit:json-ld` (SCH-08)
        * `bash scripts/grep-placeholders.sh` with `continue-on-error: true` (LGL-11 plumbing, non-blocking until Phase 4)
        * Upload `.svelte-kit/output/prerendered/` as build artifact for inspection on failure
      - Job `lighthouse-and-a11y`:
        * `needs: build-and-audit`
        * `if: github.event_name == 'pull_request'`
        * checkout v4
        * Detect Vercel preview URL via `actions/github-script@v7` polling deployments API (verbatim from RESEARCH §11 — 10 tries × 30s)
        * `npx -y @lhci/cli@latest autorun --config=./lighthouserc.json --collect.url=${url}` (PRF-08)
        * `npx -y pa11y-ci --sitemap ${url}/sitemap.xml --config .pa11yci.json` (A11Y-05)
        * `npx -y structured-data-testing-tool --url ${url}` (SCH-08 — Rich Results runtime validation against deployed preview)
      - Job `playwright-integration`:
        * `needs: build-and-audit`
        * `if: github.event_name == 'pull_request'`
        * Run `pnpm test:integration` (routes.spec.ts + html-audit.spec.ts + synthetic-violations.spec.ts) against `pnpm preview` localhost OR against the Vercel preview URL — pick localhost for speed, since the routes are static
    Validate YAML loads (Plan 01 verified syntax; re-verify after expansion).

    Replace `lighthouserc.json` skeleton with full config per RESEARCH §11:
      ```jsonc
      {
        "ci": {
          "collect": { "numberOfRuns": 3, "settings": { "preset": "desktop" } },
          "assert": {
            "preset": "lighthouse:no-pwa",
            "assertions": {
              "categories:performance": ["error", { "minScore": 0.85 }],
              "categories:seo": ["error", { "minScore": 1.0 }],
              "categories:accessibility": ["error", { "minScore": 0.95 }],
              "largest-contentful-paint": ["error", { "maxNumericValue": 2500 }],
              "cumulative-layout-shift": ["error", { "maxNumericValue": 0.1 }],
              "interactive": ["warn", { "maxNumericValue": 3000 }]
            }
          }
        }
      }
      ```
    Add a `mobile` config variant to cover both desktop and mobile budgets, OR set the assertion preset to `mobile` since CONTEXT.md target audience is mobile-first. Use one config that runs both presets, e.g., add another `collect` with `preset: 'mobile'`. (Per Phase 0 success criterion CWV targets which apply to mobile.) Pick mobile preset for the Plan 08 lighthouserc.json — desktop is bonus coverage for Plan 09 / Phase 5.

    Replace `.pa11yci.json` skeleton with full config: `{ "defaults": { "standard": "WCAG2AA", "timeout": 30000, "wait": 1000, "ignore": [] } }`.

    Write `tests/integration/html-audit.spec.ts` (Playwright). After `pnpm build`, for each prerendered HTML file in `.svelte-kit/output/prerendered/pages/`, parse and assert all the same SEO requirements check-html.ts asserts — this is a redundant gate (defense in depth) and proves the gate logic is testable beyond just running the shell script.
  </action>
  <verify>
    <automated>node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/ci.yml','utf8'));" &amp;&amp; cat lighthouserc.json | node -e "JSON.parse(require('fs').readFileSync(0,'utf8'));" &amp;&amp; cat .pa11yci.json | node -e "JSON.parse(require('fs').readFileSync(0,'utf8'));" &amp;&amp; PUBLIC_SITE_URL=https://trinity-breath-healing.vercel.app pnpm build &amp;&amp; pnpm audit:html &amp;&amp; pnpm audit:json-ld &amp;&amp; bash scripts/no-shared-css.sh &amp;&amp; bash scripts/check-tokens.sh &amp;&amp; bash scripts/check-robots.sh &amp;&amp; pnpm test:integration -- html-audit</automated>
  </verify>
  <done>
    - .github/workflows/ci.yml replaced with the full RESEARCH §11 example + adaptations (pnpm/action-setup, mobile lighthouse preset, all 6 gate scripts wired, lighthouse + pa11y + structured-data-testing-tool against Vercel preview URL)
    - lighthouserc.json + .pa11yci.json contain full configs (LCP/INP/CLS budgets + WCAG2AA)
    - tests/integration/html-audit.spec.ts exists and passes
    - All gate scripts exit 0 on the committed repo state
    - per FND-10 + PRF-08 + A11Y-05 + SEO-11 + SCH-08
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow -> Vercel deployment status API | Read-only via GitHub deployments API; no Vercel API token required for preview URL detection |
| audit scripts -> prerendered HTML output | Static file reads; no network |
| lighthouse-and-a11y -> Vercel preview URL | Public preview URL; no auth needed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-htmlaudit | Tampering | CI gate bypass | mitigate | (1) All gates run on every PR + push to main; (2) Plan 09 marks build-and-audit + lighthouse-and-a11y + playwright-integration as required status checks via branch protection - merge is impossible without green gates; (3) synthetic-violations.spec.ts proves the gates actually catch violations (defense against "gate is wired but silently no-ops") |
| T-00-meta | Tampering | future contributors injecting unvalidated meta | mitigate | check-html.ts asserts every prerendered page has the full SEO tag set; deviations fail CI |
| T-00-jsonld | Tampering | future contributors adding a second JsonLd or removing nodes | mitigate | validate-json-ld.ts asserts exactly one JSON-LD per page + specific @type presence on landing + stub routes |
| T-00-crawl | Spoofing | future contributors changing robots.txt order | mitigate | check-robots.sh wired into CI; failure blocks merge |
| T-00-stub | Information Disclosure / duplication | future contributors deepening one stub but breaking others | mitigate | playwright-integration runs routes.spec.ts which checks all 14 stubs return distinct title/meta/canonical/@graph |

</threat_model>

<verification>

Phase-level checks:
- `node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/ci.yml','utf8'))"` exits 0
- `lighthouserc.json` and `.pa11yci.json` are valid JSON
- After `pnpm build`, all 6 gates pass locally: audit:html, audit:json-ld, no-shared-css.sh, check-tokens.sh, check-robots.sh, grep-placeholders.sh
- `pnpm test:integration -- synthetic-violations` proves every gate fires on its synthetic violation
- The workflow runs on first PR after merge (verified in Plan 09)

</verification>

<success_criteria>

- All Phase 0 CI gates are wired and pass on the committed repo
- Every gate has a corresponding synthetic-violation test proving it fires when it should
- Lighthouse CI runs against Vercel preview URL with mobile preset and budgets per Phase 0 success criterion CWV targets
- pa11y WCAG 2.2 AA runs against preview URL
- structured-data-testing-tool runs against preview URL (SCH-08 runtime validation)
- per FND-06 (font-display swap is part of HTML audit), FND-07 (PUBLIC_SITE_URL fail-loud already enforced in Plan 01 build step + CI workflow env), FND-08 (stub gates), FND-09 (preview URL detection), FND-10 (full CI workflow), SEO-09 (dateModified visible - landing-side wiring lands in Phase 1; gate plumbed here), SEO-11 (HTML audit), SCH-08 (JSON-LD validation), PRF-08 (Lighthouse budgets), A11Y-05 (pa11y)

</success_criteria>

<output>
Create `.planning/phases/00-foundation-seo-scaffolding/00-08-SUMMARY.md` when done. Record: actual workflow YAML deviation from RESEARCH §11 (e.g., pnpm/action-setup added, mobile preset chosen); whether the Vercel deployments-API polling needed adjustments for the project's specific deployment environment name (`Preview` vs `preview-trinity-breath-healing` etc.); how many gates fire green on first run; any structured-data-testing-tool runtime mismatches surfaced (these may indicate Schema.org property naming issues in Plan 03 that need a follow-up fix).
</output>
