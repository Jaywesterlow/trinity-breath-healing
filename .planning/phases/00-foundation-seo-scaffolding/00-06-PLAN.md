---
phase: 00-foundation-seo-scaffolding
plan: 06
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - static/global.css
  - src/routes/+layout.ts
  - src/routes/+layout.svelte
  - static/fonts/inter/inter-regular.woff2
  - static/fonts/inter/inter-bold.woff2
  - static/fonts/cormorant-garamond/cormorant-garamond-regular.woff2
  - static/fonts/cormorant-garamond/cormorant-garamond-bold.woff2
  - scripts/check-tokens.sh
  - scripts/no-shared-css.sh
  - tests/unit/tokens.test.ts
autonomous: true
requirements:
  - FND-02
  - FND-04
  - FND-05
  - FND-06

must_haves:
  truths:
    - "static/global.css is imported exactly once — from src/routes/+layout.ts top-level (Pitfall #3 FOUC prevention)"
    - "static/global.css declares CSS variables on :root for every required token group: --color-*, --space-*, --radius-*, --font-*, --font-size-*, --line-height-*, --font-weight-*, --motion-*, --container-max"
    - "static/global.css contains 4 placeholder color tokens marked with TODO comments (--color-bg-sand, --color-fg-forest, --color-accent-gold, --color-card-warm)"
    - "static/global.css contains @font-face declarations using font-display: swap; size-adjust/ascent-override/descent-override are marked TODO until Fontaine runs"
    - "static/global.css contains a minimal CSS reset (box-sizing, margin: 0) AND mobile-first body defaults"
    - "static/global.css contains .visually-hidden utility class for a11y"
    - "static/global.css contains @media (prefers-reduced-motion: reduce) block"
    - "scripts/no-shared-css.sh confirms NO .css file exists anywhere in the repo except static/global.css (no src/styles/, no shared per-route .css)"
    - "scripts/check-tokens.sh confirms all required token groups exist in static/global.css"
    - "src/routes/+layout.svelte preloads the primary font woff2 via <link rel='preload' as='font' type='font/woff2' crossorigin> in <svelte:head>"
  artifacts:
    - path: "static/global.css"
      provides: "Single global stylesheet — CSS variables on :root for tokens (color, spacing, radius, type, motion, layout), reset, typography baseline, @font-face for Inter + Cormorant Garamond (placeholder pair), .visually-hidden utility, prefers-reduced-motion block"
      contains: "--color-bg-sand"
    - path: "scripts/no-shared-css.sh"
      provides: "Bash script: find all .css files in the repo; assert the only one is static/global.css; exit 1 with diagnostic if any other .css file exists anywhere (FND-02 enforcement per CONTEXT.md D-09)"
      contains: "global.css"
    - path: "scripts/check-tokens.sh"
      provides: "Bash script: grep static/global.css for required token names (--color-bg-sand, --color-fg-forest, --color-accent-gold, --space-4, --radius-md, --font-display, --font-body, --font-size-base, --motion-base, --container-max); exit 1 on any missing"
      contains: "--color-bg-sand"
    - path: "static/fonts/inter/inter-regular.woff2"
      provides: "Self-hosted Inter regular (400) woff2 — placeholder file licensed under SIL-OFL; replaced when design tokens land"
      contains: "(binary)"
    - path: "static/fonts/cormorant-garamond/cormorant-garamond-regular.woff2"
      provides: "Self-hosted Cormorant Garamond regular (400) woff2 — placeholder display font; replaced when design tokens land"
      contains: "(binary)"
  key_links:
    - from: "src/routes/+layout.ts"
      to: "static/global.css"
      via: "import '../../static/global.css'"
      pattern: "global.css"
    - from: "src/routes/+layout.svelte"
      to: "static/fonts/<family>/<weight>.woff2"
      via: "<link rel='preload' as='font' type='font/woff2' crossorigin>"
      pattern: "preload.*font"
    - from: "static/global.css"
      to: "Svelte component <style> blocks"
      via: "var(--token) reference"
      pattern: "var\\(--"
---

<objective>
Land the plain-CSS design-token system per the LOCKED architecture rules in CONTEXT.md D-09: single `static/global.css` for tokens + reset + typography baseline + `.visually-hidden`; NO Tailwind, NO CSS-in-JS, NO PostCSS preset, NO SCSS, NO src/styles/, NO shared CSS files beyond `static/global.css`. Ship self-hosted woff2 placeholder fonts with `font-display: swap`. Wire the font preload `<link>` in root `+layout.svelte`. Add two CI enforcement scripts: `no-shared-css.sh` (FND-02) and `check-tokens.sh` (FND-04 + FND-05).

Purpose: Per PROJECT.md and CONTEXT.md, plain CSS is a user-locked decision. No discretion to add Tailwind/SCSS/CSS-in-JS later. The discipline is enforced by `no-shared-css.sh`. Design tokens are placeholders — real values land when the user provides Figma export, but the token VARIABLE NAMES are locked here so component <style> blocks can be written against them from Phase 1 forward.
Output: `static/global.css` + 2 placeholder woff2 font pairs + 2 enforcement shell scripts + a token-presence vitest test + the layout-level font preload + the global.css import in +layout.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/00-foundation-seo-scaffolding/00-CONTEXT.md
@.planning/phases/00-foundation-seo-scaffolding/00-RESEARCH.md
@.planning/phases/00-foundation-seo-scaffolding/00-VALIDATION.md
@.planning/phases/00-foundation-seo-scaffolding/00-01-SUMMARY.md
@CLAUDE.md
@src/routes/+layout.ts
@src/routes/+layout.svelte

<interfaces>
Locked architecture rules (CONTEXT.md D-09 + RESEARCH Pattern 2):
- `static/global.css` ONLY for: CSS variables on :root (tokens), CSS reset, typography baseline, truly-global utility classes (.visually-hidden)
- EVERY .svelte component owns its styles in a scoped <style> block
- NO shared CSS files beyond global.css
- NO src/styles/ directory
- NO @import chains
- NO Tailwind / CSS-in-JS / SCSS / SASS / CSS modules
- NO PostCSS preprocessing beyond Vite defaults

Token groups required (from RESEARCH §11 global.css):
  --color-*: bg-sand, fg-forest, accent-gold, card-warm, muted, border (4 are TODO placeholder; 2 are derived)
  --space-1..--space-16
  --radius-sm, --radius-md, --radius-lg, --radius-full
  --font-display, --font-body
  --font-size-xs..--font-size-3xl (with clamp() for 2xl/3xl per RESEARCH §11)
  --line-height-tight, --line-height-normal, --line-height-loose
  --font-weight-regular, --font-weight-medium, --font-weight-bold
  --motion-fast, --motion-base, --motion-slow, --ease-out, --ease-in-out
  --container-max

Placeholder font pair (locked in Plan 06; final pair waits for design tokens):
  Body: Inter (sans, SIL-OFL — confirm at fetch)
  Display: Cormorant Garamond (serif, SIL-OFL — confirm at fetch)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: static/global.css + placeholder woff2 fonts + +layout.ts import + +layout.svelte font preload</name>
  <files>static/global.css, static/fonts/inter/inter-regular.woff2, static/fonts/inter/inter-bold.woff2, static/fonts/cormorant-garamond/cormorant-garamond-regular.woff2, static/fonts/cormorant-garamond/cormorant-garamond-bold.woff2, src/routes/+layout.ts, src/routes/+layout.svelte</files>
  <read_first>
    - .planning/phases/00-foundation-seo-scaffolding/00-RESEARCH.md (§11 `static/global.css` verbatim, §3 Pattern 2 Token system, §3 Pattern 7 Self-hosted fonts, Pitfall #3 import order, Pitfall #5 font fallback metric match)
    - .planning/phases/00-foundation-seo-scaffolding/00-CONTEXT.md (Styling architecture rules section + Design tokens section)
    - .planning/REQUIREMENTS.md (FND-02, FND-04, FND-05, FND-06)
    - seo-aeo-samenvatting-checklist.md §B (CLS=0; INP<200ms; no runtime CSS-in-JS)
    - src/routes/+layout.svelte (Plans 02-03 added Head + JsonLd mounts; preserve those)
  </read_first>
  <action>
    Write `static/global.css` verbatim from RESEARCH §11 `static/global.css` example. Preserve all sections: token block on :root (color, spacing, radius, typography, motion, layout); @font-face declarations for Inter (regular + bold) and Cormorant Garamond (regular + bold) with `font-display: swap` and TODO marker for Fontaine-computed `size-adjust`/`ascent-override`/`descent-override` (Pitfall #5 — placeholders OK in Phase 0; Fontaine integration may slip to Phase 1 when real fonts land); minimal reset (box-sizing, margin: 0, image defaults, font inheritance, focus-visible); typography baseline (h1-h6 font-family/line-height/weight); .visually-hidden utility; `@media (prefers-reduced-motion: reduce)` block.

    Inline-comment the 4 placeholder colors with `/* TODO: replace with Figma values (see UNKNOWNS.md) */`. Keep palette values from CONTEXT.md: `--color-bg-sand: #F2EBDD`, `--color-fg-forest: #3A4530`, `--color-accent-gold: #D4A968`, `--color-card-warm: #E8DEC8`.

    Fetch placeholder woff2 files. Source: Google Fonts or fontsource.org for Inter (SIL-OFL) and Cormorant Garamond (SIL-OFL). Use the `fontsource` CDN URLs or the Google Fonts API to download regular (weight 400) and bold (weight 700) for both families. Place them at `static/fonts/inter/inter-regular.woff2`, `static/fonts/inter/inter-bold.woff2`, `static/fonts/cormorant-garamond/cormorant-garamond-regular.woff2`, `static/fonts/cormorant-garamond/cormorant-garamond-bold.woff2`. Confirm license is SIL-OFL or equivalent permissive license and note the license source in the SUMMARY. (If fetch fails in CI environment, document the manual fetch instruction in SUMMARY and ship 0-byte placeholder woff2 stubs marked TODO_FONT — the build will succeed; runtime fonts will fall back to system fonts via the font-family stack.)

    Modify `src/routes/+layout.ts` to add `import '../../static/global.css';` at the TOP of the file (Pitfall #3 — must be imported at +layout.ts top level, not from a deep component, to avoid FOUC). Note: in some SvelteKit setups, CSS imports go in +layout.svelte's `<script>` block — pick whichever Vite's static analysis hoists into the prerendered HTML `<link rel='stylesheet'>` correctly. Confirm by inspecting `.svelte-kit/output/prerendered/pages/index.html` after build — it MUST contain a `<link rel="stylesheet">` referencing a hashed global.css output. If Vite does not hoist it from +layout.ts (because +layout.ts is a TypeScript module), move the import to `<script>` in +layout.svelte instead. Document the decision in SUMMARY.

    Modify `src/routes/+layout.svelte` to ADD inside `<svelte:head>` (preserving the existing `<html lang='nl'>` and any Head/JsonLd mounts from earlier plans):
    ```svelte
    <link rel="preload" as="font" type="font/woff2" crossorigin="anonymous" href="/fonts/inter/inter-regular.woff2" />
    ```
    (Per RESEARCH Pattern 7 + Pitfall #18: preload PRIMARY weight only — `inter-regular` body font. Display font Cormorant Garamond is used only by headings; preload not needed because it's not on critical path for the LCP element on stubs. Phase 1 hero may add a second preload for Cormorant Garamond when the design demands it.)

    The +layout.svelte may also add a basic mobile-first `<main>` style block (forward-port from Plan 01) referencing `var(--space-4)`, `var(--container-max)`, and a `@media (min-width: 768px)` desktop override per RESEARCH §11 layout example.
  </action>
  <verify>
    <automated>test -f static/global.css &amp;&amp; grep -q "\-\-color-bg-sand" static/global.css &amp;&amp; grep -q "\-\-space-4" static/global.css &amp;&amp; grep -q "\-\-font-display" static/global.css &amp;&amp; grep -q "font-display: swap" static/global.css &amp;&amp; grep -q "visually-hidden" static/global.css &amp;&amp; grep -q "prefers-reduced-motion" static/global.css &amp;&amp; test -f static/fonts/inter/inter-regular.woff2 &amp;&amp; test -f static/fonts/cormorant-garamond/cormorant-garamond-regular.woff2 &amp;&amp; PUBLIC_SITE_URL=https://trinity-breath-healing.vercel.app pnpm build &amp;&amp; node -e "const h=require('node-html-parser').parse(require('fs').readFileSync('.svelte-kit/output/prerendered/pages/index.html','utf8'));const ss=h.querySelectorAll('link[rel=stylesheet]');if(ss.length<1)throw new Error('no stylesheet link in prerendered HTML');const pre=h.querySelectorAll('link[rel=preload][as=font]');if(pre.length<1)throw new Error('no font preload');console.log('OK',{stylesheets:ss.length,fontPreloads:pre.length});"</automated>
  </verify>
  <done>
    - static/global.css exists with all token groups (color, spacing, radius, type, motion, layout)
    - 4 woff2 placeholder files exist under static/fonts/
    - After build, prerendered index.html contains at least one `<link rel='stylesheet'>` referencing hashed global.css output
    - After build, prerendered index.html contains at least one `<link rel='preload' as='font'>` for the body font
    - per FND-02 (single global.css), FND-04 (placeholder tokens with TODO markers), FND-05 (mobile-first reset), FND-06 (self-hosted woff2 + font-display: swap)
    - per D-09 (plain CSS locked)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scripts/no-shared-css.sh + scripts/check-tokens.sh + tests/unit/tokens.test.ts</name>
  <files>scripts/no-shared-css.sh, scripts/check-tokens.sh, tests/unit/tokens.test.ts</files>
  <read_first>
    - .planning/phases/00-foundation-seo-scaffolding/00-RESEARCH.md (Validation Architecture FND-02 + FND-04 + FND-05 enforcement scripts)
    - .planning/phases/00-foundation-seo-scaffolding/00-CONTEXT.md (Styling architecture rules — NO shared CSS files beyond global.css)
    - static/global.css (Task 1 output — list of token names that must exist)
  </read_first>
  <behavior>
    - Test: scripts/no-shared-css.sh exits 0 when only static/global.css exists
    - Test: scripts/no-shared-css.sh exits 1 when a synthetic src/styles/foo.css or src/lib/components/Button.css exists (verified by creating temp file in test)
    - Test: scripts/check-tokens.sh exits 0 when all required tokens present in static/global.css
    - Test: scripts/check-tokens.sh exits 1 when a required token name is missing (verified by mocking a modified css file)
    - Test (unit): vitest reads static/global.css and asserts all required token names exist as `--<name>:` declarations (defense in depth alongside the shell script)
  </behavior>
  <action>
    Write `scripts/no-shared-css.sh` (bash, #!/usr/bin/env bash, set -euo pipefail). Logic: `find static src -type f -name "*.css" -not -path "*/node_modules/*"`. The only allowed result is `static/global.css`. If `find` returns any other path, print diagnostic ("FND-02 violation: per CLAUDE.md and CONTEXT.md, only static/global.css is allowed as a shared CSS file. Found: <list>. Component styles must live in <style> blocks inside .svelte files (Svelte auto-scopes them).") and exit 1. Also reject any directory `src/styles/`.

    Write `scripts/check-tokens.sh` (bash, same shebang/options). Required tokens list: `--color-bg-sand`, `--color-fg-forest`, `--color-accent-gold`, `--color-card-warm`, `--color-muted`, `--color-border`, `--space-1`, `--space-4`, `--space-16`, `--radius-sm`, `--radius-md`, `--radius-lg`, `--radius-full`, `--font-display`, `--font-body`, `--font-size-base`, `--font-size-3xl`, `--line-height-tight`, `--line-height-normal`, `--font-weight-regular`, `--font-weight-bold`, `--motion-fast`, `--motion-base`, `--ease-out`, `--container-max`. For each token, `grep -q "<token>:" static/global.css` — exit 1 with diagnostic if any missing.

    Write `tests/unit/tokens.test.ts` reading `static/global.css` and asserting same required token list via `expect(css).toContain('--<token>:')` for each. Additionally assert: file contains `font-display: swap`, file contains `prefers-reduced-motion`, file contains `.visually-hidden`, file does NOT contain `@import` (no @import chains per CONTEXT.md), file does NOT contain `@apply` (no Tailwind directive).
  </action>
  <verify>
    <automated>bash scripts/no-shared-css.sh &amp;&amp; bash scripts/check-tokens.sh &amp;&amp; pnpm test:unit -- --run tokens</automated>
  </verify>
  <done>
    - Both shell scripts exit 0 on the committed file tree
    - tests/unit/tokens.test.ts passes
    - Synthetic-violation tests (in vitest, create temp src/styles/foo.css; run no-shared-css.sh; expect exit 1; cleanup) prove the gate fires on violation
    - per FND-02 + FND-04 (the gates that Plan 08 will wire into CI)
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static/global.css → all .svelte component styles | One-way: tokens flow OUT of global.css into scoped style blocks. Component styles never feed back into global.css. |
| static/fonts/*.woff2 → public CDN delivery | Self-hosted; no third-party font service trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-style-discipline | Tampering | future contributors adding shared CSS files | mitigate | scripts/no-shared-css.sh enforces FND-02 in CI (wired in Plan 08); any PR adding src/styles/, a .css file outside static/global.css, or @import in global.css fails the build |
| T-00-font-tracking | Information Disclosure | external font CDN tracking | accept-by-design | Self-hosted woff2 — no Google Fonts CDN runtime fetch (would leak IP to Google per GDPR) |
| T-00-css-injection | Tampering | css-in-js or runtime style mutation | mitigate | Locked OFF by D-09; tokens.test.ts asserts global.css has no @apply/@import, no CSS-in-JS runtime allowed in repo |

</threat_model>

<verification>

Phase-level checks:
- `bash scripts/no-shared-css.sh` exits 0
- `bash scripts/check-tokens.sh` exits 0
- `pnpm test:unit -- --run tokens` passes
- After `pnpm build`, prerendered HTML contains `<link rel='stylesheet'>` AND `<link rel='preload' as='font'>`
- No .css file exists in the repo outside `static/global.css`

</verification>

<success_criteria>

- `static/global.css` is the SOLE shared stylesheet (FND-02 enforced)
- All required token names exist as CSS variables on :root (FND-04 enforced via check-tokens.sh)
- Mobile-first reset + typography baseline in place (FND-05)
- Self-hosted woff2 with `font-display: swap` declared (FND-06)
- Primary body font preloaded from root layout (Pattern 7)

</success_criteria>

<output>
Create `.planning/phases/00-foundation-seo-scaffolding/00-06-SUMMARY.md` when done. Record: actual font sources + licenses (SIL-OFL confirmed?); whether CSS import was placed in +layout.ts or +layout.svelte and the rationale; any Fontaine deferral notes; the prerendered HTML link counts (stylesheet + preload).
</output>
