---
context: phase
phase: 00-foundation-seo-scaffolding
workflow_stage: plan-phase revision loop (iteration 1 of 3)
status: paused — user manually paused to start fresh chat (hook reload)
last_updated: 2026-06-15T17:35:00.000Z
---

# 🔴 RESUME INSTRUCTIONS — PASTE THESE LISTS BACK TO USER ON RESUME

> User explicitly requested: when resuming, paste the 3 lists below verbatim as the first response. Do NOT skip. Do NOT summarize. Do NOT proceed to action until user picks an option.

## LIST 1 — Where each stage stands (plain language)

Think of building this site as 4 big stages: **Decide what** → **Decide how** → **Build it** → **Ship it**.

### Stage 1: Decide WHAT to build — **DONE (100%)**

| Step | What it does | Status |
|---|---|---|
| Project intake | What this site is, who it's for, what success looks like, what's off-limits | ✅ Done |
| Requirements list | 70 specific things v1 must do (FAQ, <2.5s load, Dutch language, etc.) | ✅ Done |
| Roadmap | Split into 6 build-stages (Foundation → Landing → Content → Trust → Launch → Polish) | ✅ Done |
| Stack pivot | Astro→SvelteKit, Cloudflare→Vercel, Tailwind→plain CSS | ✅ Done |
| Figma + SEO checklist saved | Designs + Dutch SEO playbook locked in git | ✅ Done |

**Not cuttable. Done.**

### Stage 2: Decide HOW to build Foundation (Stage 1 of 6) — **~80–85% done**

Foundation = invisible plumbing: code skeleton, SEO machinery, robots.txt, sitemap, schema markup, CI safety nets. No visible design yet. Must be right because everything else depends on it.

| Sub-step | What it does | Status |
|---|---|---|
| Phase context | 14 reserved URLs, AI bot allowlist, no `/boeken` v1, etc. | ✅ Done |
| Phase research | 128KB guide on HOW to build Foundation (15 patterns, traps) | ✅ Done |
| Test strategy | Plan for which tests prove each piece works | ✅ Done |
| First-pass plans | 9 buildable chunks (scaffold, head tags, JSON-LD, robots, stubs, CSS tokens, image, CI, deploy) | ✅ Done |
| Quality check 1 | Reviewer found 7 mistakes (wrong paths, missing requirements, dup claims) | ✅ Done |
| Fix the mistakes | Started fixing 6 of 7. Session limit cut it. Partial work saved on disk. | 🟡 80% — written, unverified |
| Re-check fixes (quality check 2) | Confirm all 7 mistakes really gone | ⬜ Not yet (~5–8 min) |
| Maybe one more fix round | Only if re-check finds new issues | ⬜ Maybe (~20 min) |
| Lock in & exit | Final commit, hand off to "now build it" | ⬜ Not yet (~2 min) |

### Stage 3: Decide HOW to build Stages 2–6 — **0% done**

Each future stage (Landing, Content, Trust, Launch, Polish) goes through the same Stage-2 cycle: context → research → plans → check → lock. **Not needed now. Just-in-time per stage.**

Rough time:
- Landing page: ~2 hrs planning (biggest — hero, FAQ, 12-section build)
- Each remaining (Content/Trust/Launch/Polish): ~1 hr planning each

### Stage 4: Actually BUILD it — **0% done**

Code time. Foundation alone: ~9 chunks × 30–60 min each = **~5–9 hrs build time** (mostly automated, with checkpoints).

## LIST 2 — What's cuttable

1. **Safest cut:** Stop now. Resume Phase 0 finish later. Nothing lost. (Current path — already paused.)
2. **Medium cut:** Force-accept current partially-fixed plans, skip re-check. Saves ~30 min now. **Cost:** the 7 unverified fixes become ~1–3 hrs debugging during build.
3. **Hardest cut:** Abandon Foundation plans, start over with simpler scope. Wastes ~3 hrs of research/planning already on disk.

## LIST 3 — Recommendation + decision

**Recommended:** Stop now (option 1). Resume Phase 0 re-check next session — 10–40 min job. Then decide: plan Stage 2 (Landing) or jump into building Foundation.

**Ask user which option:**
- (a) Finish Phase 0 plan-phase now (re-check + maybe fix + commit)
- (b) Force-accept current plans, exit plan-phase, move on
- (c) Stop entirely (already in this state)

**DO NOT take action until user picks.**

---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each constraint below was discovered through actual session failure. Do not skip.

- [ ] **CONSTRAINT: Revision-planner did not emit `## REVISION COMPLETE` marker.** 6 of 9 plans + VALIDATION.md modified on disk, but no per-plan changelog was returned. Treat the on-disk state as the source of truth; re-spawn `gsd-plan-checker` as authoritative verifier rather than trusting an unverified revision.
- [ ] **CONSTRAINT: Plan 09 (00-09-PLAN.md) is NOT in the modified set.** BLOCKER-8 (remove `SEO-11` from Plan 09's `requirements:` frontmatter — already owned by Plan 08) may be unapplied. Grep before re-spawning checker.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trusting subagent return marker presence as proof of completion | Revision-planner ran ~14 min and modified 6 files but session limit cut before `## REVISION COMPLETE` arrived. Files on disk are the only authoritative state. | blocking | On resume: read `git diff` first, then re-spawn `gsd-plan-checker` with iteration-2 prompt. Do NOT assume blockers are fixed just because plans changed. |
| Stale `CLAUDE.md` TL;DR | Project `CLAUDE.md` "Technology Stack" TL;DR table still describes Astro 5 + Cloudflare + Tailwind v4. Authoritative stack (per commits 8cbadb3, 4532a2a, a7f96e1 + PROJECT.md/ROADMAP/CONTEXT) is SvelteKit + Svelte 5 + Vercel + plain CSS. | advisory | Any subagent must be explicitly told the TL;DR is stale; trust PROJECT.md/CONTEXT.md/recent commits instead. |
| VALIDATION.md SEO ID drift | First pass of VALIDATION.md used non-authoritative SEO ID mapping (e.g. SEO-04=robots, SEO-05=sitemap). Authoritative map is in REQUIREMENTS.md lines 28–36 (SEO-04=canonical, SEO-05=hreflang, SEO-06=OG, SEO-07=sitemap, SEO-08=robots, SEO-09=dateModified). Corrected mid-workflow. | advisory | When building VALIDATION.md or referencing SEO IDs, read REQUIREMENTS.md lines 28–36 directly — do not pattern-match against generic SEO knowledge. |

<current_state>
`/gsd:plan-phase 0` is mid-revision-loop. Workflow position: **step 12 (Revision Loop), iteration 1 of 3, awaiting iteration-2 checker spawn.**

Timeline this session:
1. STATE.md reconciled with stack pivot (commit `d5acc41`).
2. Committed `seo-aeo-samenvatting-checklist.md` (`e291063`), `Figma/` (`486502d`), `00-VALIDATION.md` (`132a872`), 9 plans written by planner agent (`752a3fd`).
3. `gsd-plan-checker` returned `## ISSUES FOUND`: 7 blockers, 5 warnings.
4. Fixed VALIDATION.md SEO ID warning inline.
5. Spawned revision `gsd-planner` with structured blocker list.
6. Revision-planner ran ~14 min, modified 6 of 9 plans + VALIDATION.md, then session limit hit.
7. No `## REVISION COMPLETE` marker received. Modified files uncommitted.
</current_state>

<completed_work>

- STATE.md reconciliation — done
- 3 setup commits (checklist, Figma, VALIDATION) — done
- 9 plans created by planner — done (committed `752a3fd`)
- Plan-checker iteration 1 — done (7 blockers, 5 warnings surfaced)
- VALIDATION.md SEO ID correction — done (uncommitted)
- Revision-planner spawn — in progress; 6 of 9 plans modified on disk, no completion marker
</completed_work>

<remaining_work>

- Verify revision diffs structurally address each iteration-1 blocker (read each modified plan's relevant section)
- Manually inspect 00-09-PLAN.md frontmatter for residual `SEO-11` (BLOCKER-8 — Plan 09 not in modified set)
- Re-spawn `gsd-plan-checker` for iteration 2 of 3
- If `## VERIFICATION PASSED` → commit revisions, proceed to plan-phase step 13 (final output)
- If `## ISSUES FOUND` again → assess stall (issue_count vs prev=7); decide between revision-3 or stall-handling
- Final commit message proposal: `docs(00): revise phase 0 plans (iteration 2) — address all checker blockers`
- Exit plan-phase workflow with `/gsd:execute-phase 0` recommendation
</remaining_work>

<decisions_made>

- Skipped Pattern Mapper (greenfield repo, no analogs to map).
- Skipped UI-SPEC (ROADMAP `UI hint: no` for Phase 0).
- Skipped Schema Push gate (no ORM in scope; content via mdsvex).
- Injected 6 STRIDE threat IDs (T-00-meta, jsonld, crawl, stub, htmlaudit, deploy) into planner prompt for mandatory per-plan `<threat_model>` blocks.
- Wrote 00-VALIDATION.md before planner spawn so plans could align task IDs against per-task verification map.
- Suggested 8–10 plan decomposition; planner produced exactly 9 (one per architectural responsibility in RESEARCH.md).
- Mid-workflow correction of VALIDATION.md SEO IDs against REQUIREMENTS.md authoritative map.
</decisions_made>

<blockers>

- **Session-limit interrupt** — Anthropic 5h limit hit at ~17:23 UTC 2026-06-15 during revision-planner Agent() call. Reset: 7:20pm Europe/Amsterdam. No technical workaround; wait for reset.
- **Plan 09 not in modified set** — BLOCKER-8 (remove SEO-11 from Plan 09 requirements) may be unapplied. Grep before re-checking.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured handoff, machine-readable mirror of this file
2. `.planning/STATE.md` — project state (stack: SvelteKit/Svelte 5 + Vercel + plain CSS — NOT Astro/Tailwind despite stale CLAUDE.md TL;DR)
3. `.planning/REQUIREMENTS.md` lines 28–36 — authoritative SEO ID definitions (SEO-04..SEO-11)
4. `.planning/ROADMAP.md` Phase 0 section — Success Criteria #1–#5 are the goal-backward truth-set
5. `.planning/phases/00-foundation-seo-scaffolding/00-CONTEXT.md` — locked decisions D-01..D-09
6. `.planning/phases/00-foundation-seo-scaffolding/00-RESEARCH.md` — Patterns 1–15 are the implementation truth-set
7. `.planning/phases/00-foundation-seo-scaffolding/00-VALIDATION.md` — per-task verification map (SEO IDs corrected this session)
8. `.planning/phases/00-foundation-seo-scaffolding/00-{01..09}-PLAN.md` — current on-disk plans (6 modified, 3 untouched: 06, 07, 09)

## Infrastructure State

- **Git:** 7 files modified, uncommitted, on branch `master`. Last commit `752a3fd`. No background processes.
- **Vercel:** Not yet bound (Plan 09 deliverable, not yet executed).
- **GitHub repo:** Not yet created (Plan 09 deliverable).
- **Local dev:** No `package.json`, no `node_modules` yet — Phase 0 not executed, only planned.
- **CLAUDE.md:** TL;DR Technology Stack section is STALE (says Astro/Cloudflare/Tailwind). Trust `PROJECT.md` + recent commits for stack truth.

<context>
We were in `/gsd:plan-phase 0` step 12 revision loop, iteration 1 of 3. The checker passed back 7 blockers with clear fix prescriptions. Revision-planner was given a structured YAML fix list with surgical-edit instructions (no replanning). It modified 6 of 9 plans before the session limit cut it. The diff sizes (10–116 lines per file) suggest meaningful structural edits, not just whitespace — but verification of correctness must be re-done by the plan-checker, not assumed.

The cleanest resume path is: re-spawn `gsd-plan-checker` with the same prompt as iteration 1 plus an iteration-2 directive to flag any unresolved blocker. Whatever it returns becomes the basis for either committing and exiting, running iteration 3, or invoking stall-handling.
</context>

<next_action>
Start with:

1. `git diff .planning/phases/00-foundation-seo-scaffolding/00-{01,02,03,04,05,08}-PLAN.md` — confirm structural edits are sane (no truncated mid-edit content).
2. `grep -E "requirements:.*SEO-11" .planning/phases/00-foundation-seo-scaffolding/00-09-PLAN.md` — if hit, manually edit out SEO-11 from Plan 09 frontmatter.
3. Re-spawn `gsd-plan-checker` (model: sonnet) with the iteration-1 prompt PLUS this addition: "This is iteration 2/3 of the revision loop. Iteration 1 surfaced 7 blockers (BLOCKER-1..8 listed below). For each one: confirm it is structurally fixed OR re-flag it as still-blocking. Apply the same 12-point verification checklist as before."
4. If `## VERIFICATION PASSED`: commit with message `docs(00): revise phase 0 plans (iteration 2) — address all checker blockers from iteration 1` and proceed to plan-phase step 13.
5. If `## ISSUES FOUND` with `issue_count >= 7`: stall-handling — ask user "Proceed anyway" vs "Adjust approach".
6. If `## ISSUES FOUND` with `issue_count < 7`: spawn revision-planner for iteration 3 (final).
</next_action>
