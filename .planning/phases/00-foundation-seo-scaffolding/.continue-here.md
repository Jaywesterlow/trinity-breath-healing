---
context: phase
phase: 00-foundation-seo-scaffolding
task: 2
total_tasks: 4
status: in_progress
last_updated: 2026-06-21T00:00:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through actual failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: gh-cli-path — `gh` is not on PATH in Claude's shell (Bash or PowerShell). Must always call `& "C:\Program Files\GitHub CLI\gh.exe"` with `$env:GH_CONFIG_DIR = "$env:APPDATA\GitHub CLI"` set in every PowerShell invocation.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| gh not on PATH | Claude's Bash and PowerShell both fail `gh` commands without full path + GH_CONFIG_DIR | blocking | Always: `$env:GH_CONFIG_DIR = "$env:APPDATA\GitHub CLI"; & "C:\Program Files\GitHub CLI\gh.exe" <cmd>` |
| PR with identical branches | GitHub rejects PR when base and head share all commits | advisory | main = seed commit only; all work on feature branch |

<current_state>
Wave 6 / Plan 09 (human checkpoints). Task 1 complete — GitHub repo + PR exist. Waiting for user to connect Vercel (Task 2) before CI can run and branch protection can be configured (Task 3).

Repo: https://github.com/Jaywesterlow/trinity-breath-healing (PRIVATE)
PR #1: https://github.com/Jaywesterlow/trinity-breath-healing/pull/1 (phase-0-foundation → main)
Local branches: master (original), main (at seed commit 79d0c9a), phase-0-foundation (all Phase 0 work)
</current_state>

<completed_work>

- Task 1: GitHub repo created (private), `main` set as default branch, `phase-0-foundation` branch pushed with all Plans 01-08 work, PR #1 opened. PHASE0-LAUNCH-CHECKLIST.md template created at `.planning/phases/00-foundation-seo-scaffolding/PHASE0-LAUNCH-CHECKLIST.md`.
</completed_work>

<remaining_work>

- Task 2: User connects Vercel to GitHub repo via vercel.com → sets PUBLIC_SITE_URL env var → confirms preview deploy + CI run
- Task 3: Agent wires branch protection on `main` via gh CLI (build-and-audit, lighthouse-and-a11y, playwright-integration as required checks)
- Task 4: User initiates Search Console verification → agent commits google<hash>.html → production smoke test per AI crawler UA → PHASE0-LAUNCH-CHECKLIST.md finalized + committed
</remaining_work>

<decisions_made>

- `main` reset to seed commit `79d0c9a` so Phase 0 PR has a real diff
- `gh` CLI must use full path + GH_CONFIG_DIR in PowerShell
- PHASE0-LAUNCH-CHECKLIST.md template pre-created
- Preview env var: use production URL for both Production and Preview scopes (Phase 0 stubs only — acceptable)
</decisions_made>

<blockers>

- Task 2 requires Vercel dashboard (cannot automate repo import)
- Task 4 Search Console always manual (Google account required)
</blockers>

## Required Reading (in order)

1. `.planning/phases/00-foundation-seo-scaffolding/00-09-PLAN.md` — full task specs + acceptance criteria
2. `.planning/phases/00-foundation-seo-scaffolding/PHASE0-LAUNCH-CHECKLIST.md` — fill in as each task completes
3. `.planning/STATE.md` — phase context and wave map

## Infrastructure State

- **GitHub repo:** https://github.com/Jaywesterlow/trinity-breath-healing (PRIVATE, main = default)
- **PR #1:** https://github.com/Jaywesterlow/trinity-breath-healing/pull/1 (open, no CI yet — Vercel not connected)
- **Vercel:** not yet connected
- **gh CLI:** installed at `C:\Program Files\GitHub CLI\gh.exe`, logged in as `Jaywesterlow`, auth config at `%APPDATA%\GitHub CLI\hosts.yml`
- **Local git:** check `git branch` on resume; likely on `phase-0-foundation` or `master`

<context>
Plan 09 is entirely human checkpoints. The agent's role is: guide manual steps, run automatable parts (branch protection via gh CLI, smoke tests via scripts), fill in PHASE0-LAUNCH-CHECKLIST.md as evidence accumulates. After Task 4, Phase 0 is COMPLETE and Phase 1 (Landing Sections) can begin.
</context>

<next_action>
Ask user if they've connected Vercel yet. If yes: get preview URL + CI run URL, then run Task 3 (branch protection via gh CLI). If no: direct them to vercel.com → Add New Project → import trinity-breath-healing.
</next_action>
