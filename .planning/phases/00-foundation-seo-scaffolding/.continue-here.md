---
context: phase
phase: 00-foundation-seo-scaffolding
plan: 1
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-06-18
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints discovered via failure this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| sv create v0.16.1 no svelte.config.js | New sv create embeds all SvelteKit config in vite.config.ts via sveltekit(). Do NOT put adapter/compilerOptions/kit options in vite.config.ts sveltekit() call — we created a separate svelte.config.js for this. | advisory | svelte.config.js exists and owns all Kit config. vite.config.ts calls sveltekit() with no args. |

<current_state>
Phase 0, Plan 01, Task 2 — in progress.

Task 1 (package legitimacy gate) complete. All 22 packages verified. zod pinned to 3.25.76 (not 4.x). 00-RESEARCH.md audit table updated.

Task 2 scaffold in progress:
- sv create v0.16.1 ran with --template minimal --types ts --add prettier eslint vitest (no Tailwind, no playwright add-on)
- All deps installed via pnpm: adapter-vercel, mdsvex, enhanced-img, schema-dts, fontaine, node-html-parser, @lhci/cli, pa11y-ci, structured-data-testing-tool, @playwright/test, zod@3.25.76
- adapter-auto removed
- Build scripts approved (sharp, puppeteer, core-js) via pnpm approve-builds

Config files written:
- package.json — name fixed, scripts added, engines, pnpm.onlyBuiltDependencies
- svelte.config.js — NEW (adapter-vercel fra1/nodejs20.x, mdsvex, runes, aliases, prerender)
- vite.config.ts — rewritten (enhancedImages + sveltekit() no-args + vitest projects: unit/jsdom + scripts/node)
- tsconfig.json — updated (noUncheckedIndexedAccess + verbatimModuleSyntax + isolatedModules + $schema/$content paths)
- src/app.html — lang="en" → lang="nl"
- .env.example — PUBLIC_SITE_URL= with no default
- src/lib/seo/defaults.ts — fail-loud SITE_URL + defaults export (temporary; Plan 02 owns this properly)
- src/routes/+layout.ts — prerender=true, trailingSlash=never, load() with meta+graph

NOT YET WRITTEN (remaining Task 2 items):
- src/routes/+layout.svelte — rewrite to minimal placeholder
- src/routes/+page.svelte — rewrite to single H1
- src/routes/+page.ts — placeholder page load
- src/lib/index.ts — verify/confirm empty re-export hub
- src/content/index.ts — FND-03: frontmatterSchema + loadCollection + collections glob
- src/content/_smoke/example.svx — smoke fixture
- tests/unit/content.test.ts — vitest FND-03 assertions
- tests/setup.ts — MOCK_SITE_URL export
- static/global.css — empty placeholder (Plan 06 fills it)
- playwright.config.ts — webServer pnpm preview on 4173
- .github/workflows/ci.yml — CI skeleton
- .pa11yci.json — WCAG2AA config
- lighthouserc.json — LCP/CLS gates
- Directory stubs: src/lib/{components,schema,constants,content/faq,server}, static/fonts, tests/{integration,scripts}, scripts
- Remove src/lib/vitest-examples/ (generated by vitest add-on, not needed)
- Build + test verification + commit
</current_state>

<completed_work>

**Task 1: Package Legitimacy Gate — COMPLETE**
- Ran npm view for all 22 packages: version, repo URL, postinstall hook
- All repos match official GitHub orgs; zero postinstall hooks on any package
- mdsvex Svelte 5 peer dep confirmed: `^5.0.0-next.120` covers 5.56.3
- adapter-vercel 6.3.4: regions: string[] + nodejs20.x confirmed in extracted type defs
- Vitest 4 migration audit ran (fork agent) — no config-breaking changes for Plan 01 setup
- zod: npm latest is 4.4.3 but Zod 4 breaks z.string().date(); pinned to zod@3.25.76
- TypeScript 6.0.3: SvelteKit 2 peer explicitly lists `^5.3.3 || ^6.0.0` — OK
- 00-RESEARCH.md Package Legitimacy Audit table updated with all verified versions
- User typed `approved`

**Task 2: Scaffold — Partially Complete**
- sv create v0.16.1 scaffolded minimal+ts+prettier+eslint+vitest (no Tailwind, no playwright add-on)
- pnpm install ran; all deps installed; build scripts approved for sharp/puppeteer/core-js
- adapter-auto removed, adapter-vercel installed
- Config files written (see current_state above)
</completed_work>

<remaining_work>

**Task 2: Complete remaining files (in order):**

1. `src/routes/+layout.svelte` — minimal placeholder: `import '../../static/global.css'`, `let { children } = $props()`, `<main>{@render children()}</main>` + scoped style. NO `<svelte:head><html lang>` (lang is in app.html). No Head/JsonLd components yet (Plan 02).
2. `src/routes/+page.svelte` — `<h1>TRINITY Breath & Healing</h1>` only
3. `src/routes/+page.ts` — re-export defaults as page meta placeholder
4. `src/lib/index.ts` — verify it's empty re-export hub (should already be from sv create)
5. `src/content/index.ts` — FND-03 surface: `frontmatterSchema` (z.object with title/datePublished/dateModified/author), `loadCollection<T>()` typed helper that parses+throws with path, `collections` glob factory
6. `src/content/_smoke/example.svx` — valid frontmatter: title, datePublished 2026-06-18, author; one paragraph body
7. `tests/unit/content.test.ts` — vitest: (a) smoke fixture loads with correct title, (b) invalid frontmatter (title: 123) throws with path in message
8. `tests/setup.ts` — export `MOCK_SITE_URL = 'https://trinity-breath-healing.vercel.app'`
9. `static/global.css` — empty placeholder (Plan 06 fills it)
10. `playwright.config.ts` — webServer: pnpm preview on 4173; testDir: tests/integration
11. `.github/workflows/ci.yml` — jobs: build-and-audit (checkout, node20, pnpm install --frozen-lockfile, pnpm check, pnpm build with PUBLIC_SITE_URL env, audit placeholder steps) + lighthouse-and-a11y skeleton (depends on build-and-audit, PR only, polling Vercel preview URL)
12. `.pa11yci.json` — `{ "defaults": { "standard": "WCAG2AA", "timeout": 30000, "wait": 1000, "ignore": [] } }`
13. `lighthouserc.json` — desktop preset; error thresholds: LCP 2500ms, CLS 0.1, SEO score 1.0, A11y 0.95
14. Directory stubs with `.gitkeep`: `src/lib/components/`, `src/lib/schema/`, `src/lib/constants/`, `src/lib/content/faq/`, `src/lib/server/`, `static/fonts/`, `tests/integration/`, `tests/scripts/`, `scripts/`
15. Remove `src/lib/vitest-examples/` (sv create vitest add-on generated this; not in plan)
16. **VERIFY:** `PUBLIC_SITE_URL=https://trinity-breath-healing.vercel.app pnpm build` → check .svelte-kit/output/prerendered/pages/index.html exists + grep `lang="nl"`
17. **VERIFY:** `pnpm test:unit -- --run` → smoke test passes, invalid frontmatter test passes
18. **COMMIT** all as `feat(00): Plan 01 complete — SvelteKit scaffold + test infra + FND-03 content pipeline`
</remaining_work>

<decisions_made>

- **sv create config pattern**: v0.16.1 no longer generates svelte.config.js. We created it separately for svelte-check/IDE compatibility. vite.config.ts calls `sveltekit()` with no args.
- **zod@3 pinned**: npm latest is 4.4.3 but Zod 4 breaks `z.string().date()`. Using `zod@^3.25.76`.
- **vitest 4.1.9 used**: Fork agent verified no config-breaking changes for our simple unit test setup.
- **vitest projects in vite.config.ts**: No separate vitest.config.ts; `projects` array covers unit (jsdom) + scripts (node).
- **pnpm approve-builds**: sharp/puppeteer/core-js approved; written to `package.json pnpm.onlyBuiltDependencies`.
- **enhancedImages() before sveltekit()**: Enforced in vite.config.ts as per RESEARCH.md.
</decisions_made>

<blockers>
None currently blocking.
</blockers>

## Required Reading (in order)
1. `.planning/phases/00-foundation-seo-scaffolding/00-01-PLAN.md` — Task 2 full spec: exact content for every remaining file
2. `.planning/phases/00-foundation-seo-scaffolding/00-RESEARCH.md` — §Code Examples for svelte.config.js, +layout.ts, content pipeline patterns
3. `src/routes/+layout.ts` — already written; +layout.svelte must import global.css from same relative path style

## Critical Anti-Patterns (do NOT repeat these)
- **SVELTE:HEAD CAN'T SET HTML LANG**: `<svelte:head><html lang="nl">` is syntactically invalid Svelte. The `lang` attribute is set ONLY in `src/app.html` on the root `<html>` element. Already done correctly.
- **NO src/app.css**: D-09 locks CSS source-of-truth at `static/global.css`. Do not create `src/app.css`.
- **NO Tailwind**: Locked OFF. Do not add any Tailwind plugin or PostCSS preset.

## Infrastructure State
- **pnpm**: 11.8.0 installed globally via npm
- **node**: v24.4.1
- **All deps installed**: pnpm-lock.yaml current; `pnpm install --frozen-lockfile` will succeed
- **No running dev server**

<context>
Mid-Task 2 of Plan 01. Most config files written. Remaining work is creating the application source files (routes, content pipeline, tests) and CI/QA configs, then verifying the build succeeds and smoke tests pass before committing. This is pure file-writing work with no blocking decisions — follow Plan 01 Task 2 spec exactly.
</context>

<next_action>
Start with: write `src/routes/+layout.svelte` (minimal placeholder, see remaining_work #1), then continue down the list in order through all 18 remaining items. Run build + test verification before committing.
</next_action>
