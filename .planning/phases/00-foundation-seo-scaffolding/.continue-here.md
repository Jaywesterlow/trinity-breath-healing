---
context: phase
phase: 00-foundation-seo-scaffolding
task: 0
total_tasks: 0
status: in_progress
last_updated: 2026-06-20T15:30:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> No blocking constraints from execution failures. Advisory constraint below.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| npm install via tool runner | Running `npm install` through Claude's Bash/PowerShell tools fails with npm 11.x bug (`Cannot read properties of null (reading 'matches')`). Reproducible every attempt. | advisory | User must run `npm install --legacy-peer-deps` directly in their own terminal (not via `!` prefix workaround — that also failed). |

<current_state>
Mid-session on pnpm → npm migration (ad-hoc task, not a GSD plan). Wave 4 (Plan 05) completed and merged earlier this session — 140/140 tests, 14 stub routes prerendered, SEO-09/BLOCKER-3 closed.

Migration is ~80% done. All source files edited. Blocker: `npm install --legacy-peer-deps` must be run by the user in their terminal — tool-based execution fails with npm 11.x bug.

Once `package-lock.json` exists: write `.npmrc`, run tests, commit.
</current_state>

<completed_work>

**Wave 4 (Plan 05) — DONE this session:**
- 14 stub routes (28 files) prerendered
- SEO-09/BLOCKER-3 closed: `__BUILD_DATE__=2026-06-20` → visible `<time datetime>` + JSON-LD `WebPage.dateModified`
- 140/140 unit tests passing
- STATE.md updated, committed at `5cda227`

**pnpm → npm migration — PARTIALLY DONE (uncommitted):**
- `package.json` — removed `"packageManager": "pnpm@11.8.0"`, removed `"pnpm": { "onlyBuiltDependencies": [...] }`, changed `"test": "npm run test:unit -- --run"`
- `.github/workflows/ci.yml` — removed `pnpm/action-setup@v4`, changed all `pnpm` → `npm run` / `npm ci`
- `playwright.config.ts` — `pnpm preview` → `npm run preview`
- `CLAUDE.md` — `Package Manager: pnpm` → `Package Manager: npm`
- `tests/unit/landing-date.test.ts` — updated pnpm references in comments/messages
- `pnpm-lock.yaml` — DELETED
- `new_project/` folder — DELETED
</completed_work>

<remaining_work>

1. **USER ACTION REQUIRED**: Run in terminal: `npm install --legacy-peer-deps`
2. Write `.npmrc` with `legacy-peer-deps=true` (prevents future `npm ci` failures in CI)
3. Run `npm run test:unit -- --run` — verify 140/140 still pass
4. Commit all changes: package.json, package-lock.json, ci.yml, playwright.config.ts, CLAUDE.md, landing-date.test.ts, .npmrc, (deleted pnpm-lock.yaml)
5. Start new session for Wave 5 (Plan 08 — HTML audit gate CI script)
</remaining_work>

<decisions_made>

- Switch pnpm → npm: user preference, feasible, `--legacy-peer-deps` required due to `knip` peer dep conflict
- `.npmrc` with `legacy-peer-deps=true` makes switch permanent without per-command flags
</decisions_made>

<blockers>

- **npm install via tool**: npm 11.7.0 bug `Cannot read properties of null (reading 'matches')` when run through Claude tools. Workaround: user runs directly in terminal.
- **No package-lock.json yet**: Until user runs install, `npm ci` in CI will fail. Do not commit without lockfile.
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — current wave position (Wave 5 / Plan 08 next)
2. `.planning/phases/00-foundation-seo-scaffolding/00-05-SUMMARY.md` — Wave 4 completion record

## Infrastructure State
- `node_modules/` — present (from pnpm, still works for dev/test)
- `pnpm-lock.yaml` — DELETED (staged)
- `package-lock.json` — MISSING (npm install not yet completed)
- `.svelte-kit/output/prerendered/` — has fresh build from this session (2026-06-20)
- Stale worktree dir `.claude/worktrees/agent-af6422c664bad6c56` — on disk, git refs pruned, harmless

<context>
Session covered Wave 4 execution (Plan 05 complete, merged, tested) then pivoted to pnpm→npm migration. Migration hit npm 11.x tool-execution bug. All file edits are done; only missing piece is the lockfile which requires user to run npm install themselves.

Wave 5 (Plan 08 — HTML audit gate: check-html.ts CI script) is the next GSD work after migration is committed.
</context>

<next_action>
Ask user to run `npm install --legacy-peer-deps` in their terminal. Once confirmed, write `.npmrc`, run tests, commit migration. Then start new session for Wave 5.
</next_action>
