---
context: phase
phase: 01-landing-sections
task: build-2-tdd-gates
total_tasks: 2
status: paused
last_updated: 2026-06-25T10:30:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through actual failure this session. Acknowledge before proceeding.

- [ ] CONSTRAINT: All 10 phase-1 plans have SUMMARY.md files → `/gsd-execute-phase` finds 0 incomplete plans and exits without executing. Do NOT run execute-phase expecting it to pick up the 2 gates. Build them directly via an executor dispatch using this file as the plan spec, then run the verifier.

**Do not proceed until checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Misattributing svelte-check errors | "implicitly any" on `Footer.svelte` blamed on barrel exports; real cause was a scriptless `.svelte` file. Wasted a revert. | advisory | Reproduce the error on a direct import before blaming the indirection layer. |

<current_state>
Component restructure DONE and committed (633bd2e). svelte-check = 0 errors. Branch: phase-1-landing-sections.
GSD model profile set to "inherit" (Sonnet 4.6) in .planning/config.json — uncommitted.
Paused before executing 2 remaining TDD gates. Both test files read and analyzed this session.
</current_state>

<completed_work>

- Compared old repos (DJ-Webapp = empty scaffold; apex-automotive = real, Paraglide/inlang i18n noted as v2 EN/hreflang reference) — done
- Restructured `src/lib/components/` → `global/ seo/ ui/ (interactions, cards) sections/ (home, over-mij, werkwijze, diensten, contact)` + per-folder & root barrel `index.ts` — done, committed 633bd2e
- Fixed pre-existing errors: JsonLd `graph` typed `AnyNode[]`; Footer empty `<script lang=ts>`; landmarks.test.ts rewritten for current Nav/Footer (with `$app/stores` mock); robots.test.ts split `/\r?\n/` (CRLF) — done, committed 633bd2e
- Set GSD model profile → "inherit" (.planning/config.json edited, uncommitted)
- Read and analyzed `tests/unit/image.test.ts` and `tests/unit/landing-date.test.ts` — done
</completed_work>

<remaining_work>

- GATE 1 — EnhancedImage primitive.
  - Create `src/lib/components/ui/media/EnhancedImage.svelte` wrapping `<enhanced:img>`
  - Required props: `src, alt, width, height`; `loading` default `lazy`; `fetchpriority` optional (omit attr when not set)
  - **CRITICAL**: `tests/unit/image.test.ts` line 38 reads `src/lib/components/EnhancedImage.svelte` (old flat path). Update `componentPath` in the test to the new location `src/lib/components/ui/media/EnhancedImage.svelte`
  - Spec: `.planning/phases/00-foundation-seo-scaffolding/` Plan 00-07 (PRF-01)
  - Makes `tests/unit/image.test.ts` (4 red) green

- GATE 2 — Landing dateModified.
  - Wire `__BUILD_DATE__` via Vite `define` in `vite.config.ts` (value: `new Date().toISOString().slice(0,10)` at build time)
  - Add a single visible `<time datetime={date}>{date}</time>` to landing page (+page.svelte or a suitable section component); `date` comes from `__BUILD_DATE__`
  - WebPage JSON-LD `dateModified` must equal the same date (single source — read from same `__BUILD_DATE__` constant)
  - Run `PUBLIC_SITE_URL=https://trinity-breath-healing.vercel.app npm run build` to prerender
  - `tests/unit/landing-date.test.ts` reads `.svelte-kit/output/prerendered/pages/index.html` — 5 assertions:
    1. File exists
    2. Exactly one `<time datetime="YYYY-MM-DD">` element
    3. `<time>` text content === datetime attribute value
    4. JSON-LD WebPage.dateModified === datetime attribute value
    5. `__BUILD_DATE__` NOT present in HTML (Vite replaced it)
  - SEO-09 / BLOCKER-3
</remaining_work>

<decisions_made>

- Kept barrel exports (apex pattern) — one-line multi-import `import { Nav, Footer, Head } from '$lib/components'`
- Section folders named by Dutch route slug (1:1 with routes)
- GSD model profile set to "inherit" so all subagents use Sonnet 4.6 session model (not Opus)
- Update image.test.ts componentPath to new `ui/media/` path (NOT putting component at old flat path — the restructure was intentional)
</decisions_made>

<blockers>
- image.test.ts line 38: componentPath points at old flat path — fix in GATE 1 by updating the path in the test.
- GATE 2 requires `npm run build` before the test can pass — executor must run the build.
</blockers>

## Required Reading (in order)
1. `tests/unit/image.test.ts` — full test contract for GATE 1 (line 38 = old componentPath)
2. `tests/unit/landing-date.test.ts` — full test contract for GATE 2
3. `.planning/phases/00-foundation-seo-scaffolding/00-07-PLAN.md` — PRF-01 EnhancedImage spec
4. `src/routes/+page.svelte` — current landing page (where to add `<time>`)
5. `vite.config.ts` — where to add `define: { __BUILD_DATE__: ... }`
6. `src/lib/components/seo/` — WebPage JSON-LD emitter (find the file emitting WebPage node)

## Infrastructure State
- Branch: `phase-1-landing-sections`. Last commit: `f4acc0a`.
- Uncommitted: `.planning/config.json` (model_profile: inherit), `.planning/STATE.md`, `static/global.css`, `static/logo.svg`
- svelte-check: 0 errors. vitest: 5 failing (the 2 gates above), rest green.
- All 10 phase-1 plans have SUMMARY.md → execute-phase finds nothing to execute; use verifier after gates are green.

<context>
User wants the 2 gates built to green on Sonnet 4.6 — no pauses, no permission prompts. Model profile already set to inherit. Do NOT run /gsd-execute-phase (it exits immediately — no plans to run). Instead: dispatch executor directly with this file as spec, build both gates, run `npm run check && PUBLIC_SITE_URL=https://trinity-breath-healing.vercel.app npm run build && npx vitest run`, verify all green.
</context>

<next_action>
1. Commit .planning/config.json (model profile change) as separate small commit
2. Dispatch gsd-executor for GATE 1: read image.test.ts + 00-07-PLAN.md, create src/lib/components/ui/media/EnhancedImage.svelte, update image.test.ts line 38 componentPath, run vitest image.test to verify 4 green
3. Dispatch gsd-executor for GATE 2: read landing-date.test.ts + vite.config.ts + +page.svelte + WebPage JSON-LD emitter, wire __BUILD_DATE__ in vite define, add <time datetime> to landing, wire JSON-LD dateModified, run build + vitest landing-date
4. After both green: run full test suite + svelte-check
5. Run /gsd-verify-work or spawn verifier to close out phase 1
</next_action>
