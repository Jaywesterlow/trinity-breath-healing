---
context: phase
phase: 01-landing-sections
task: human-checkpoint
total_tasks: 10
status: in_progress
last_updated: 2026-06-23T09:25:02Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: CMD-ampersand — npm/npx/prettier.cmd scripts fail when invoked from the project directory because `Trinity Breath & Healing` contains `&` which CMD interprets as a command separator. Use `node node_modules/prettier/bin/prettier.cjs` or PowerShell `& node $prettier ...` directly.
- [ ] CONSTRAINT: Executor agents load permissions at startup — permissions added to `settings.local.json` mid-session are NOT seen by already-running task agents. Always start fresh session after editing permissions.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| CMD ampersand path split | `&` in project folder name breaks all `.cmd` batch wrappers (npm, npx, prettier.cmd, etc.) — path gets truncated at `&` | blocking | Always invoke via PowerShell with `node path\to\bin\script.cjs` directly; never use `.cmd` wrappers for this project |
| Stale executor permissions | Task agents spawned before permissions edit ignore the updated `settings.local.json` | advisory | Edit permissions file FIRST in a session, then spawn executors; never edit mid-flight |

<current_state>
Phase 1 automated work 100% complete. All 10 plans (01-00 through 01-09) done. Branch `phase-1-landing-sections` is ahead of `main` by 34 commits. PR #5 is open. CI passed after prettier fix (ce751fb). Vercel preview deployed. Blocked on human visual sign-off for Plan 01-09 (autonomous: false).
</current_state>

<completed_work>

All plans done:
- Plan 01-00: check-copy.sh, html-audit red tests, image stubs, logo.svg, ci.yml step
- Plan 01-01: HeroSection.svelte, Head.svelte preload, +page.svelte
- Plan 01-02: SiteNav — fixed header, scroll-hide/show, hamburger mobile overlay
- Plan 01-03: WerkwijzeSection — 3-card accordion
- Plan 01-04: AboutSection — stats (8+, 65+, ∞), Lees meer
- Plan 01-05: BehandelingenSection — 5-card horizontal scroll carousel
- Plan 01-06: ContactSection — email form, JS toggle, AVG checkbox
- Plan 01-07: FaqSection — 10-item accordion (multiple open simultaneously)
- Plan 01-08: SiteFooter — email, Almere address, 3 columns, copyright
- Plan 01-09: Automated verification — 140/141 tests pass (1 pre-existing Windows CRLF), 159/159 Playwright html-audit, check-copy.sh passes, svelte-check 0 errors

Automated gates: ALL PASS
</completed_work>

<remaining_work>

- Plan 01-09 human checkpoint: Visual verification of Vercel preview
  - Open PR #5 preview URL on mobile (390px)
  - Verify all 8 sections render correctly (see checklist below)
  - Disable JS in Chrome → verify email form still visible, toggle hidden
  - Reply "approved" or describe issues
- After approval: merge PR #5 → start Phase 2
</remaining_work>

<decisions_made>

- Switched from pnpm to npm (Wave 4 of Phase 0) — `.npmrc` has `legacy-peer-deps=true`
- `Trinity Breath & Healing` folder name contains `&` — all CMD batch wrappers broken; use `node ... prettier.cjs` directly
- CI branch protection on main requires 2 status checks — cannot push directly; must use PR
- `gh` CLI invoked via `$env:GH_CONFIG_DIR = "$env:APPDATA\GitHub CLI"; & 'C:\Program Files\GitHub CLI\gh.exe' ...`
- Executor agent permissions added to `.claude/settings.local.json` — all `Write`, `Edit`, `Bash(*)` variants now present
</decisions_made>

<blockers>

- Human visual checkpoint: Plan 01-09 is `autonomous: false`. Cannot complete without user approval.
- Pre-existing: `robots.test.ts` CRLF failure on Windows (`$` anchor vs `\r`) — dates to Phase 0, out of scope for Phase 1.
</blockers>

## Required Reading (in order)

1. `.planning/phases/01-landing-sections/01-09-PLAN.md` — human checkpoint checklist and must-haves
2. `.planning/HANDOFF.json` — structured state for resume-work
3. `.planning/phases/01-landing-sections/01-VALIDATION.md` — Phase 1 validation criteria

## Visual Checklist for PR #5 Preview

Open PR #5: https://github.com/Jaywesterlow/trinity-breath-healing/pull/5

- [ ] Hero: H1 visible, CTA button gold, 3 service tiles
- [ ] Header: fixed, hides on scroll-down, shows on scroll-up; hamburger at mobile width opens full-screen overlay; X and Escape close it
- [ ] Werkwijze: 3 accordion cards — tap to open, others close
- [ ] About: stats (8+, 65+, ∞) visible, "Lees meer" link
- [ ] Behandelingen: 5 cards scrollable, partial edge cards peeking
- [ ] Contact: email form visible by default; toggle shows when JS active; AVG consent checkbox
- [ ] FAQ: 10 accordion items, multiple can be open simultaneously
- [ ] Footer: email, address (Almere), 3 columns (DIENSTEN/MENU/LEZEN), copyright
- [ ] JS disabled (Chrome DevTools → Network conditions → Block JavaScript): email form still visible, toggle hidden, all content accessible

## Infrastructure State

- Branch: `phase-1-landing-sections` (34 commits ahead of main)
- PR #5: open, CI green, Vercel preview deployed
- Local working tree: clean
- npm (not pnpm) — `node_modules` present, `.npmrc` has `legacy-peer-deps=true`

<context>
All automated Phase 1 work is done. The only remaining step is the user doing a visual check of the Vercel preview and saying "approved". After that: merge PR #5 into main, then plan/execute Phase 2.
</context>

<next_action>
Start with: Open PR #5 on GitHub, find Vercel preview URL in PR comments, open on mobile (390px), run through the visual checklist above, then come back and say "approved" (or describe issues).
</next_action>
