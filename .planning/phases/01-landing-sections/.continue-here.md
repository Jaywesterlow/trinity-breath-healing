---
context: phase
phase: 01-landing-sections
task: build-2-tdd-gates
total_tasks: 2
status: paused
last_updated: 2026-06-25T09:14:17.959Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through actual failure this session. Acknowledge before proceeding.

- [ ] CONSTRAINT: GSD subagent model — `/gsd-autonomous` spawns planner/executor with the GSD-configured model (currently **opus**), NOT the session model. Switching the session to Sonnet alone does NOT save Opus. Mitigation: run `/gsd-settings` and set model profile to Sonnet BEFORE the autonomous run.

**Do not proceed until checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Misattributing svelte-check errors | "implicitly any" on `Footer.svelte` blamed on barrel exports; real cause was a scriptless `.svelte` file. Wasted a revert. | advisory | Reproduce the error on a direct import before blaming the indirection layer. |

<current_state>
Component restructure DONE and committed (633bd2e). svelte-check = 0 errors. Branch: phase-1-landing-sections.
Paused before building the 2 remaining phase-1 TDD red gates. Session about to switch Opus → Sonnet 4.6.
</current_state>

<completed_work>

- Compared old repos (DJ-Webapp = empty scaffold; apex-automotive = real, Paraglide/inlang i18n noted as v2 EN/hreflang reference) — done
- Restructured `src/lib/components/` → `global/ seo/ ui/ (interactions, cards) sections/ (home, over-mij, werkwijze, diensten, contact)` + per-folder & root barrel `index.ts` — done, committed 633bd2e
- Fixed pre-existing errors: JsonLd `graph` typed `AnyNode[]`; Footer empty `<script lang=ts>`; landmarks.test.ts rewritten for current Nav/Footer (with `$app/stores` mock); robots.test.ts split `/\r?\n/` (CRLF) — done, committed 633bd2e
</completed_work>

<remaining_work>

- GATE 1 — EnhancedImage primitive. Create `src/lib/components/ui/media/EnhancedImage.svelte` wrapping `<enhanced:img>`. Required props: `src, alt, width, height`; `loading` default `lazy`; `fetchpriority` optional (omit attr when not set). Spec: `.planning/phases/00-foundation-seo-scaffolding/` Plan 07 (PRF-01). Makes `tests/unit/image.test.ts` (4 red) green. NOTE: that test reads `src/lib/components/EnhancedImage.svelte` (old flat path) — update its `componentPath` to the new location, or site the component where the test expects.
- GATE 2 — Landing dateModified. Add a single visible `<time datetime="YYYY-MM-DD">` to the landing page; wire `__BUILD_DATE__` via Vite `define`; WebPage JSON-LD `dateModified` must equal the same date (single source). Run `npm run build` (prerender) so `tests/unit/landing-date.test.ts` (1 red) reads `.svelte-kit/output/prerendered/pages/index.html`. SEO-09 / BLOCKER-3.
</remaining_work>

<decisions_made>

- Kept barrel exports (apex pattern) — one-line multi-import `import { Nav, Footer, Head } from '$lib/components'`
- Section folders named by Dutch route slug (1:1 with routes)
- Use `/gsd-autonomous` for the build run — `/goal` is NOT a real command
</decisions_made>

<blockers>
- image.test.ts componentPath points at old flat path — resolve during GATE 1.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable state mirror of this file
2. `.planning/phases/00-foundation-seo-scaffolding/01-07-PLAN.md` (or whichever is the PRF-01 EnhancedImage plan) — GATE 1 spec
3. `tests/unit/image.test.ts` + `tests/unit/landing-date.test.ts` — the contracts to satisfy

## Infrastructure State
- Branch: `phase-1-landing-sections`. Last commit: `633bd2e`.
- Unrelated uncommitted edits in working tree (pre-existing, NOT this session): `static/global.css`, `static/logo.svg`, `.planning/STATE.md`. Leave or handle separately.
- svelte-check: 0 errors. vitest: 5 failing (the 2 gates above), rest green.

<context>
User wants the 2 gates built to green autonomously on Sonnet — no wave/phase pauses, no permission prompts. ~1-1.5h. Set GSD model profile to Sonnet FIRST (see blocking constraint) or it runs on Opus.
</context>

<next_action>
Start with: switch model to Sonnet 4.6 (/model), set GSD model profile to Sonnet (/gsd-settings), then run /gsd-autonomous. Build GATE 1 then GATE 2. Verify green: `npm run check && npm run build && npx vitest run`.
</next_action>
