---
context: phase
phase: 01-landing-sections
task: nav-ui-polish
status: in_progress
last_updated: 2026-06-29
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session._

<current_state>
Nav UI polish session (phase-1 branch). All major changes done and svelte-check clean.
ONE item incomplete: mobile menu CTA button overlaps last nav link on short viewports.

Browser JS confirmed:
- `.mobile-menu__cta`: position=absolute, bottom=40px ✓
- ctaRect.top=368 inside menuHeight=448
- Flex nav links (5 items, centered in 448-80=368px usable space) extend to ~439px
- CTA starts at 368px → overlap

Fix: add `padding-bottom: 120px` to `.mobile-menu` in Nav.svelte. This shrinks the flex area so the 5 nav links center in (448-80-120)=248px, ending at ~338px — clear of the absolute CTA at 368px.
</current_state>

<completed_work>

- Removed social icon `<a>` + `.nav__social-btn` CSS from Nav.svelte
- Extracted `NavCta.svelte` (ButtonLink pill, mirrors NavLogo pattern)
- Split `Button.svelte` (form `<button>`) / `ButtonLink.svelte` (pill `<a>`) / `TextLink.svelte` (underline `<a>`, Figma node 424-60)
- Updated `src/lib/components/ui/interactions/index.ts` + `src/lib/components/global/index.ts`
- Fixed nav overlap: `--nav-height` (80px mobile / 100px desktop) in global.css, `padding-top: var(--nav-height)` on `<main>` in layout.svelte
- Hamburger always-clockwise: `let rotation = $state(0)`, `rotation += 45` on `toggleMenu()` and `closeMenu()`, inline style on `.dot-grid` SVG
- Slower transitions: 350ms on `.dot-grid` and `.mobile-menu` (was `var(--motion-base)` = 250ms)
- Page fade: `onNavigate` + View Transitions API in layout.svelte; `fade-out 180ms` / `fade-in 280ms` in global.css
- Added `ButtonLink "Maak een afspraak"` to mobile menu (`position:absolute; bottom:40px` in `.mobile-menu__cta`)
</completed_work>

<remaining_work>

- **One-line fix**: Add `padding-bottom: 120px` to `.mobile-menu` in `Nav.svelte` (around line 192) — prevents flex content overlapping absolute CTA
- **Commit**: Propose commit for ALL session changes (no commits made this session, user must approve)
</remaining_work>

<decisions_made>

- Button split: 3 components for 3 Figma types (Button=form, ButtonLink=pill link, TextLink=underline link)
- NavCta.svelte follows NavLogo.svelte pattern (self-contained, handles own responsive visibility)
- Hamburger rotation: cumulative JS state (not CSS class) — CSS class reverses direction on close
- Mobile CTA: `position:absolute` removes from flex flow, always visible. Needs `padding-bottom` on parent to prevent flex content overlap.
- Page transitions: View Transitions API (zero deps, degrades gracefully, SvelteKit-native via `onNavigate`)
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading
1. `src/lib/components/global/Nav.svelte` — full component; all changes this session live here
2. `.planning/HANDOFF.json` — structured state

## Infrastructure State
- Dev server: `npm run dev` on port 5173 (may or may not still be running)
- svelte-check: 0 errors, 0 warnings confirmed before pausing

## Speed-to-resume lines (exact locations)

| What | File | Where |
|---|---|---|
| Add `padding-bottom: 120px` | `Nav.svelte` | `.mobile-menu { ... }` block |
| Rotation speed tweak | `Nav.svelte` | `.dot-grid { transition: transform 350ms ... }` |
| Overlay fade speed tweak | `Nav.svelte` | `.mobile-menu { transition: opacity 350ms ... }` |
| Page fade-out speed | `global.css` | `::view-transition-old(root) { animation: 180ms ... }` |
| Page fade-in speed | `global.css` | `::view-transition-new(root) { animation: 280ms ... }` |

<context>
User walked through Figma designs for 3 button types (291-47, 405-34, 424-60), confirmed split into Button/ButtonLink/TextLink. All nav UI polish tasks requested by user are implemented. Session context at 91% — paused before committing.
</context>

<next_action>
Start with: Add `padding-bottom: 120px` to `.mobile-menu` CSS block in `Nav.svelte`. Then run `npx svelte-check --threshold error`. Then screenshot the mobile menu to confirm no overlap. Then propose commit for all session changes.
</next_action>
