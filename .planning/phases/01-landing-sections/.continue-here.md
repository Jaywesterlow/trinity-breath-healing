---
context: phase
phase: 01-landing-sections
task: fluid-step1
total_tasks: null
status: in_progress
last_updated: 2026-06-24T16:21:38.482Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: CMD-ampersand — npm/npx/prettier.cmd scripts fail when invoked from the project directory because `Trinity Breath & Healing` contains `&` which CMD interprets as a command separator. Use `node node_modules/prettier/bin/prettier.cjs` or PowerShell `& node $prettier ...` directly.
- [ ] CONSTRAINT: Executor agents load permissions at startup — permissions added to `settings.local.json` mid-session are NOT seen by already-running task agents. Always start fresh session after editing permissions.
- [ ] CONSTRAINT: Wrong fluid token scheme in global.css — a `--font-size-fluid-*` block (375→1440px, rem-based, wrong names) was added this session and must be deleted FIRST before adding the correct tokens.
- [ ] CONSTRAINT: Stepped padding breakpoints in Nav.svelte — 1024/1200/1440 `padding` overrides added this session are WRONG (clamp replaces them; zero per-breakpoint overrides allowed) and must be deleted.
- [ ] CONSTRAINT: Approval gate — do NOT touch any `.svelte` file until the user has approved the printed `:root` block from Step 1.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| CMD ampersand path split | `&` in project folder name breaks all `.cmd` batch wrappers (npm, npx, prettier.cmd, etc.) — path gets truncated at `&` | blocking | Always invoke via PowerShell with `node path\to\bin\script.cjs` directly; never use `.cmd` wrappers for this project |
| Stale executor permissions | Task agents spawned before permissions edit ignore the updated `settings.local.json` | advisory | Edit permissions file FIRST in a session, then spawn executors; never edit mid-flight |
| Importing deleted components | Building without checking git status causes silent build failures when pages import deleted component files | advisory | Always run `git status` before starting work on components |
| Wrong viewport range for clamp | Used 375px as min instead of 768px (the project's mobile base breakpoint) | blocking | Formula is `clamp(MINpx, MINpx + (MAX−MIN)*(100vw−768px)/672, MAXpx)` — 768px always, not device widths |
| Wrong units in clamp | Used rem-based intercept inside clamp instead of px | blocking | All values inside the clamp formula are px. Do not convert to rem inside the calc. |
| Stepped font-size/padding overrides | Added per-breakpoint padding to Nav instead of a single clamp token | blocking | Zero per-breakpoint font-size or padding overrides after the fluid system is applied. Clamp handles it. |

<current_state>
Phase 01 (landing-sections), branch `phase-1-landing-sections`.

Nav.svelte has real content (logo, links, hamburger, CTA) — committed at 05191d5.

Uncommitted partial work (both files have WRONG changes that need correction before new tokens are applied):
- `static/global.css` — has a `--font-size-fluid-*` block (wrong scheme — delete it)
- `src/lib/components/Nav.svelte` — has stepped 1024/1200/1440 padding overrides (wrong — delete them)

The correct fluid type+spacing system is fully specced (see decisions) but NOT yet implemented. Pausing at Step 0.
</current_state>

<completed_work>

- Phase 1 plans 01–09: all executed then reset by user decision
- `static/global.css`: design tokens, fonts, reset, utilities — in place
- `src/lib/components/Nav.svelte`: real content (logo, links, hamburger, CTA) — committed 05191d5
- Fluid typography first attempt (SUPERSEDED — must be reverted): `--font-size-fluid-*` tokens + stepped Nav padding
</completed_work>

<remaining_work>

**Immediate (fluid type system):**
1. Delete `--font-size-fluid-*` block from `static/global.css`
2. Delete stepped padding breakpoints (1200/1440 overrides) from `Nav.svelte`
3. Compute MIN/MAX for all 6 `--fs-*` tokens and 5 `--space-*` tokens
4. Write `:root` blocks in `static/global.css`, print full output, STOP for user approval
5. After approval: refactor every `.svelte` under `src/` — swap raw px font-size → `var(--fs-*)`, raw px padding/margin → `var(--space-*)`; delete font-size/padding-only media query overrides

**After fluid system:**
- `src/lib/components/Footer.svelte`: real content
- Home page sections: rebuild from scratch
</remaining_work>

<decisions_made>

**Fluid type+spacing spec (prompt-master, 2026-06-24):**
- Token names: `--fs-caption`, `--fs-description` (12→16, fixed), `--fs-body`, `--fs-h3`, `--fs-h2`, `--fs-h1` and `--space-xs`, `--space-sm`, `--space-md`, `--space-lg`, `--space-xl`
- Formula: `clamp(MINpx, MINpx + (MAX−MIN) * (100vw − 768px) / 672, MAXpx)` — px units throughout
- Viewport range: 768px (base/min) → 1440px (max); 672 = 1440−768
- Scope: `static/global.css` + all `*.svelte` under `src/` — nothing else
- Zero per-breakpoint font-size or padding overrides anywhere after refactor
- Static `--space-1` through `--space-16` tokens stay untouched (additive, not replaced)
- Workflow gate: print `:root` → user approves → then touch components

**Pre-existing decisions:**
- Full frontend reset: user chose to start fresh
- global.css linked in app.html
- npm (not pnpm) — `.npmrc` has `legacy-peer-deps=true`
- CI branch protection on main — cannot push directly; must PR
</decisions_made>

<blockers>
- Human approval gate for `:root` block before any component work begins.
</blockers>

## Required Reading (in order)
1. `static/global.css` — current token state (read before editing to see what's there and what needs deletion)
2. `src/lib/components/Nav.svelte` — current state with wrong stepped breakpoints that need removal
3. `CLAUDE.md` — design constraints (plain CSS, no Tailwind, SEO priority)
4. `.planning/phases/01-landing-sections/01-UI-SPEC.md` — design specs for rebuild

## Infrastructure State
- Branch: `phase-1-landing-sections`
- Last clean commit: 05191d5 (Nav real content)
- Uncommitted: `Nav.svelte` + `global.css` (both need correction first)
- `npm run dev` → localhost:5173
- `npm run build` → passes at last commit

<context>
User noticed nav text breaking out of container at mid-screen widths. Agreed to implement fluid type+spacing via clamp. First attempt used wrong viewport range (375px) and wrong token names/units. User then provided a complete prompt-master spec that supersedes the first attempt. I summarized it, flagged the conflicts with my previous changes, user confirmed understanding and ran /gsd-pause-work. The correct approach is fully clear — just follow the spec, clean up wrong partial work first, obey the approval gate.
</context>

<next_action>
Start with: Read `static/global.css` then delete the `--font-size-fluid-*` block. Read `Nav.svelte` then delete the 1200/1440 padding override blocks. Then compute MIN/MAX for all 11 tokens, write the two `:root` blocks, print them, and STOP waiting for user approval.
</next_action>
