---
phase: 01-landing-sections
plan: "04"
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/lib/constants/brand.ts
  - src/lib/components/AboutSection.svelte
  - src/routes/+page.svelte
autonomous: true
requirements:
  - LND-04
  - PRF-03
  - A11Y-04

must_haves:
  truths:
    - "Section has id=over-mij"
    - "H2 'Vanuit eigen ervaring weet ik wat jij doormaakt.' in initial HTML"
    - "Stats block shows 8+ / 65+ / ∞ values sourced from BRAND.stats"
    - "BRAND.stats object added to src/lib/constants/brand.ts (D-16)"
    - "'Lees meer over mij →' link points to /over-mij"
    - "About illustrations use EnhancedImage with loading=lazy"
    - "npm run build succeeds; npm run check 0 errors"
  artifacts:
    - path: src/lib/constants/brand.ts
      provides: "BRAND.stats property added: yearsExperience, clientsHelped, satisfaction"
    - path: src/lib/components/AboutSection.svelte
      provides: "About section — heading, bullets, stats from BRAND.stats, 2 lazy illustrations"
      min_lines: 80
    - path: src/routes/+page.svelte
      provides: "AboutSection mounted after WerkwijzeSection"
  key_links:
    - from: "src/lib/components/AboutSection.svelte"
      to: "src/lib/constants/brand.ts"
      via: "import BRAND; BRAND.stats.yearsExperience etc."
      pattern: "BRAND\\.stats"
---

<objective>
Plan 04 — About + Stats section (LND-04). Figma Frame 3. Parallel with Plan 03 (different files).

Purpose: Add BRAND.stats to constants, build AboutSection with heading, 2 bullet blocks, stats from BRAND, 2 lazy illustrations, and the "Lees meer" link.

Output: brand.ts updated, AboutSection.svelte created, +page.svelte updated.
</objective>

<execution_context>
@C:\Users\jaywe\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jaywe\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/01-landing-sections/01-CONTEXT.md
@.planning/phases/01-landing-sections/01-UI-SPEC.md
@static/global.css
@src/lib/constants/brand.ts
@src/lib/components/EnhancedImage.svelte
@src/routes/+page.svelte
</context>

<interfaces>
<!-- Current BRAND object from src/lib/constants/brand.ts -->
<!-- Missing: stats property. Must add (D-16): -->
<!-- stats: { yearsExperience: '8+', clientsHelped: '65+', satisfaction: '∞' } as const -->

<!-- D-15: Two illustration PNGs via EnhancedImage loading="lazy" -->
<!-- D-16: Stats sourced from BRAND.stats -->
<!-- D-17: "Lees meer over mij →" links to /over-mij -->
<!-- D-18: Section ID = over-mij -->

<!-- UI-SPEC LND-04 stats styling: -->
<!-- Stat number: --font-size-3xl, --font-display, --color-accent-gold -->
<!-- Stat label: --font-size-sm, --color-muted -->

<!-- UI-SPEC LND-04 layout: 2-column desktop (text left, illustrations right), stacked mobile -->

<!-- About copy (from UI-SPEC copywriting contract + LND-04 requirements): -->
<!-- Bullet 1: "Vanuit eigen ervaring" block -->
<!-- Bullet 2: "Vakkundig opgeleid" (placeholder — bullet 2 text is TBD per LND-04) -->
<!-- Body prose: Figma-verbatim (inferred from context — planner uses placeholder pending aunt copy) -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add BRAND.stats to brand.ts and build AboutSection.svelte</name>
  <files>src/lib/constants/brand.ts, src/lib/components/AboutSection.svelte</files>
  <behavior>
    - BRAND.stats.yearsExperience === '8+'
    - BRAND.stats.clientsHelped === '65+'
    - BRAND.stats.satisfaction === '∞'
    - Section has id="over-mij"
    - H2 text is exactly "Vanuit eigen ervaring weet ik wat jij doormaakt."
    - Stats block renders all 3 stat values from BRAND.stats (not hardcoded)
    - "Lees meer over mij →" anchor href="/over-mij"
    - 2 EnhancedImage components with loading="lazy" (PRF-03)
    - No onMount needed — pure static SSR component
    - All CSS token-only
  </behavior>
  <action>
First, update src/lib/constants/brand.ts: add a `stats` property to the BRAND object (D-16). Place it after the `services` array. Add:
```typescript
stats: {
  yearsExperience: '8+',
  clientsHelped: '65+',
  satisfaction: '∞'
} as const
```
The outer `as const` on the BRAND object already covers this, but include it for clarity. The file uses JSDoc comments — add a comment: `/** Statistics displayed in the About section (LND-04, D-16). Update here; AboutSection reads from BRAND.stats. */`

Then create src/lib/components/AboutSection.svelte.

Script: import EnhancedImage from '$lib/components/EnhancedImage.svelte'; import { BRAND } from '$lib/constants/brand'; import AboutImg1 from '$lib/assets/images/about-illustration-1.png?enhanced'; import AboutImg2 from '$lib/assets/images/about-illustration-2.png?enhanced';

No $state, no onMount — pure static component.

Template:
```svelte
<section id="over-mij">
  <div class="container">
    <div class="about-grid">
      <div class="about-content">
        <p class="eyebrow">Over mij</p>
        <h2>Vanuit eigen ervaring weet ik wat jij doormaakt.</h2>
        <p class="about-body">
          Als kind van mijn moeder weet ik hoe diep spanning en trauma in het lichaam kunnen vastzitten.
          Mijn eigen herstelreis bracht mij bij ademwerk en energetische genezing — technieken die voor mij
          werkten en die ik nu met andere mensen deel. Niet als therapeut, maar als begeleider op jouw pad.
        </p>
        <ul class="bullet-blocks">
          <li>
            <strong>Vanuit eigen ervaring</strong>
            <p>Mijn aanpak is geworteld in wat ik zelf heb meegemaakt en doorgewerkt. Ik weet wat het betekent om vast te zitten — en om los te komen.</p>
          </li>
          <li>
            <strong>Vakkundig opgeleid</strong>
            <p>TODO_OPLEIDING_TEKST</p>
          </li>
        </ul>
        <a href="/over-mij" class="read-more">Lees meer over mij →</a>
        <div class="stats">
          <div class="stat">
            <span class="stat-number">{BRAND.stats.yearsExperience}</span>
            <span class="stat-label">Jaren ervaring</span>
          </div>
          <div class="stat">
            <span class="stat-number">{BRAND.stats.clientsHelped}</span>
            <span class="stat-label">Klachten verholpen</span>
          </div>
          <div class="stat">
            <span class="stat-number">{BRAND.stats.satisfaction}</span>
            <span class="stat-label">Mogelijkheden voor herstel</span>
          </div>
        </div>
      </div>
      <div class="about-images">
        <EnhancedImage
          src={AboutImg1}
          alt="Illustratie bij Over mij — Trinity Breath & Healing"
          width={400}
          height={500}
          loading="lazy"
        />
        <EnhancedImage
          src={AboutImg2}
          alt="Tweede illustratie — Trinity Breath & Healing"
          width={400}
          height={300}
          loading="lazy"
        />
      </div>
    </div>
  </div>
</section>
```

Note: "TODO_OPLEIDING_TEKST" is intentional — CI grep warns but does not block (Phase 5 launch gate blocks). This matches the LND-04 requirement note "placeholder text on bullet 2."

Style block (token-only):
- section: padding-block var(--space-12), background var(--color-bg-sand)
- .container: max-width var(--container-max), margin-inline auto, padding-inline var(--space-6)
- .about-grid: display flex, flex-direction column, gap var(--space-8)
- .eyebrow: font-size var(--font-size-sm), font-weight var(--font-weight-bold), color var(--color-muted), text-transform uppercase, letter-spacing 0.08em, margin-bottom var(--space-4)
- h2: font-family var(--font-display), font-size var(--font-size-2xl), font-weight var(--font-weight-bold), color var(--color-fg-forest), margin-bottom var(--space-6)
- .about-body: font-size var(--font-size-base), line-height var(--line-height-normal), color var(--color-fg-forest), margin-bottom var(--space-6)
- .bullet-blocks: list-style none, padding 0, display flex, flex-direction column, gap var(--space-4), margin-bottom var(--space-8)
- .bullet-blocks li: display flex, flex-direction column, gap var(--space-2)
- .bullet-blocks strong: font-weight var(--font-weight-bold), color var(--color-fg-forest)
- .bullet-blocks p: font-size var(--font-size-base), color var(--color-fg-forest), line-height var(--line-height-normal)
- .read-more: color var(--color-fg-forest), font-weight var(--font-weight-bold), text-decoration underline, display inline-block, margin-bottom var(--space-8)
- .stats: display grid, grid-template-columns repeat(3, 1fr), gap var(--space-6)
- .stat: display flex, flex-direction column, align-items center, text-align center
- .stat-number: font-family var(--font-display), font-size var(--font-size-3xl), font-weight var(--font-weight-bold), color var(--color-accent-gold), line-height var(--line-height-tight)
- .stat-label: font-size var(--font-size-sm), color var(--color-muted), margin-top var(--space-2)
- .about-images: display flex, flex-direction column, gap var(--space-4)
- @media (min-width: 1024px): .about-grid: display grid, grid-template-columns 1fr 1fr, align-items start, gap var(--space-16). section: padding-block var(--space-16)
  </action>
  <verify>
    <automated>npm run check 2>&1 | tail -10</automated>
  </verify>
  <done>brand.ts has BRAND.stats with correct values. AboutSection.svelte compiles with 0 errors. Stats rendered from BRAND.stats. 2 EnhancedImage with loading=lazy. Section id=over-mij. H2 verbatim. read-more link to /over-mij.</done>
</task>

<task type="auto">
  <name>Task 2: Mount AboutSection in +page.svelte</name>
  <files>src/routes/+page.svelte</files>
  <action>
Import AboutSection from '$lib/components/AboutSection.svelte'. Add `<AboutSection />` after `<WerkwijzeSection />` in the page template.

If WerkwijzeSection is not yet in +page.svelte (this plan is Wave 2, parallel with Plan 03), add it at the correct position. The final page order is: HeroSection → WerkwijzeSection → AboutSection → (later plans add more). The SEO-09 recency block stays at the bottom.

Run npm run build. Verify index.html contains id="over-mij", the H2 text, and the 3 stat values (8+, 65+, ∞).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>npm run build succeeds. index.html contains id="over-mij", "Vanuit eigen ervaring weet ik wat jij doormaakt.", "8+", "65+", "∞". npm run check: 0 errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static section | All about/stats content is build-time static from TypeScript constants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | TODO_OPLEIDING_TEKST placeholder | accept | CI warns on TODO_ values per Phase 0 grep-placeholders.sh; launch gate blocks until replaced |
| T-04-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- npm run check: 0 errors
- npm run build succeeds
- BRAND.stats.yearsExperience === '8+' verifiable via `node -e "const {BRAND}=require('./src/lib/constants/brand.ts')" ` — confirm via TypeScript check instead
- index.html contains 3 stat values from BRAND.stats
- npm test: green
</verification>

<success_criteria>
BRAND.stats added. AboutSection mounted. Stats from constants. 2 lazy illustrations. H2 verbatim. Link to /over-mij. 0 TypeScript errors.
</success_criteria>

<output>
Create .planning/phases/01-landing-sections/01-04-SUMMARY.md when done.
</output>
