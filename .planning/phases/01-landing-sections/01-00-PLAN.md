---
phase: 01-landing-sections
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/check-copy.sh
  - tests/integration/html-audit.spec.ts
  - tests/unit/schema-faq.test.ts
  - src/lib/assets/images/hero.png
  - src/lib/assets/images/card-spinal-touch.png
  - src/lib/assets/images/card-goldhealing.png
  - src/lib/assets/images/about-illustration-1.png
  - src/lib/assets/images/about-illustration-2.png
  - static/logo.svg
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - LND-09
  - LND-10
  - PRF-02
  - PRF-03
  - PRF-05
  - PRF-06
  - PRF-07
  - A11Y-02

must_haves:
  truths:
    - "check-copy.sh exists and exits 1 when a locked string is absent from built HTML"
    - "html-audit.spec.ts asserts hero preload link, eager loading, overlay aria, carousel links, form labels, no third-party scripts"
    - "schema-faq.test.ts asserts faqItems.length >= 8 once the content file exists"
    - "All 6 image stub files exist at their required paths so npm run build does not fail"
    - "ci.yml runs check-copy.sh after the existing HTML audit step"
  artifacts:
    - path: scripts/check-copy.sh
      provides: "Copy preservation + hedge-language CI gate (LND-09, LND-10)"
    - path: tests/integration/html-audit.spec.ts
      provides: "Phase 1 landing-page assertions added to existing audit suite"
    - path: tests/unit/schema-faq.test.ts
      provides: "faqItems.length >= 8 guard (gated until faq/index.ts exists)"
    - path: static/logo.svg
      provides: "Logo placeholder SVG at public path"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-copy.sh"
      via: "bash scripts/check-copy.sh step in build-and-audit job"
      pattern: "check-copy\\.sh"
    - from: "tests/integration/html-audit.spec.ts"
      to: ".svelte-kit/output/prerendered/pages/index.html"
      via: "readFileSync + parse assertions"
      pattern: "html-audit"
---

<objective>
Wave 0 — CI and test infrastructure for Phase 1. Must land before ANY section sub-unit work begins.

Purpose: Establish the Phase 1 test contracts (html-audit assertions for hero preload, overlay ARIA, carousel links, form labels, no third-party scripts), the copy-preservation CI gate, 1×1 PNG image stubs so the build never fails on missing assets, and a logo placeholder.

Output: check-copy.sh, extended html-audit.spec.ts, updated schema-faq.test.ts, 6 image stubs, logo.svg stub, ci.yml updated.
</objective>

<execution_context>
@C:\Users\jaywe\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jaywe\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-landing-sections/01-CONTEXT.md
@.planning/phases/01-landing-sections/01-RESEARCH.md
@.planning/phases/01-landing-sections/01-VALIDATION.md
@static/global.css
@src/lib/constants/brand.ts
@src/lib/schema/faq.ts
@tests/integration/html-audit.spec.ts
@.github/workflows/ci.yml
</context>

<interfaces>
<!-- Existing contracts executors must know -->

From tests/integration/html-audit.spec.ts (existing pattern):
```typescript
// Reads files from PAGES_DIR = '.svelte-kit/output/prerendered/pages'
// Uses: import { parse } from 'node-html-parser'
// Pattern: for (const file of htmlFiles) { test(`${rel}: assertion`, () => { ... }) }
// Landing page rel = 'index.html'
```

From src/lib/schema/faq.ts:
```typescript
export function buildFaqPage(entries: { question: string; answer: string }[]): FAQPage
```

From scripts/grep-placeholders.sh (existing — hedge pattern to extend):
```bash
# Existing script scans for TODO_ and TBD_ patterns
# check-copy.sh is a new COMPANION script for locked Figma copy strings
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create check-copy.sh and extend html-audit.spec.ts with Phase 1 assertions</name>
  <files>scripts/check-copy.sh, tests/integration/html-audit.spec.ts, tests/unit/schema-faq.test.ts</files>
  <action>
Create scripts/check-copy.sh with execute permission. The script reads .svelte-kit/output/prerendered/pages/index.html (failing loudly if absent) and greps for each locked Figma copy string. Exit code 1 if any string missing. Locked strings per UI-SPEC copywriting contract (LND-09):
  - "Rust in je hoofd. Ontspanning in je lichaam."
  - "Rustig, persoonlijk en op jouw tempo."
  - "Vanuit eigen ervaring weet ik wat jij doormaakt."
  - "Een eerste stap hoeft niet groot te zijn."
  - "Maak een afspraak"
  - "Verstuur email"

Also add a hedge-language scan (LND-10) using grep -iE for: misschien|zou kunnen|wellicht|mogelijk|eventueel. If any match found in index.html, exit 1 with message indicating hedge language detected. Both checks in the same script.

Extend tests/integration/html-audit.spec.ts with a new describe block "Phase 1 landing-page assertions" scoped to index.html only. Add tests for (all operating on `root = parse(indexHtml)`):
  - `<link rel="preload" as="image">` exists (PRF-02)
  - No `<img>` with `loading="eager"` except the one inside `.hero` or with class hero-related — actually test that `loading="eager"` appears exactly once in the HTML (the hero image)
  - `<div role="dialog"` or `<div role="dialog"` is present (A11Y-01 overlay)
  - `.carousel-track a` or at least 5 `<a>` elements linking to `/diensten/` paths (LND-05 / PRF-07)
  - All `<input>` elements have an associated `<label>` (A11Y-02) — check that input count matches label count for form inputs
  - No `<script src` pointing to third-party domains (PRF-07) — scan all script[src] for external URLs (not starting with / or relative)

Note: These assertions test FUTURE state from sub-unit plans 01–06. They will FAIL until those plans execute. That is correct — they are the red tests that sub-unit PRs turn green. Do NOT gate these on file existence; let them fail fast.

Update tests/unit/schema-faq.test.ts: add a test block that conditionally imports faqItems if the file exists at src/lib/content/faq/index.ts, and if it does, asserts faqItems.length >= 8. Use dynamic import with try/catch — if module not found, skip the test (use test.skip or conditional). This prevents the unit test from failing until sub-unit 7 creates the file.

Per D-39: all new script/test files use UTF-8, Unix line endings. No Tailwind, no SCSS references. No hardcoded hex values.
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>check-copy.sh exists with shebang + set -euo pipefail + both locked-copy and hedge-language checks. html-audit.spec.ts has Phase 1 assertions block (may be failing — expected). schema-faq.test.ts has conditional faqItems test. npm test passes on existing tests (new assertions may fail — that is correct).</done>
</task>

<task type="auto">
  <name>Task 2: Create image stubs and logo placeholder + update ci.yml</name>
  <files>src/lib/assets/images/hero.png, src/lib/assets/images/card-spinal-touch.png, src/lib/assets/images/card-goldhealing.png, src/lib/assets/images/about-illustration-1.png, src/lib/assets/images/about-illustration-2.png, static/logo.svg, .github/workflows/ci.yml</files>
  <action>
Create the src/lib/assets/images/ directory if it does not exist. Place minimal valid PNG stubs at each path. A valid 1×1 transparent PNG is a specific byte sequence — write it as a base64-decoded binary. The exact minimal PNG bytes for a 1×1 transparent image:
  89 50 4E 47 0D 0A 1A 0A 00 00 00 0D 49 48 44 52 00 00 00 01 00 00 00 01 08 06 00 00 00 1F 15 C4 89 00 00 00 0A 49 44 41 54 78 9C 62 00 01 00 00 05 00 01 0D 0A 2D B4 00 00 00 00 49 45 4E 44 AE 42 60 82

Write this binary content to each of the 5 PNG paths. Use Node.js via Bash:
node -e "const fs=require('fs');const buf=Buffer.from('iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==','base64');['src/lib/assets/images/hero.png','src/lib/assets/images/card-spinal-touch.png','src/lib/assets/images/card-goldhealing.png','src/lib/assets/images/about-illustration-1.png','src/lib/assets/images/about-illustration-2.png'].forEach(p=>{fs.mkdirSync(require('path').dirname(p),{recursive:true});fs.writeFileSync(p,buf);})"

Create static/logo.svg as a minimal placeholder SVG (not in src/lib/assets — logo is served from static/ per D-09 / RESEARCH pitfall 10). Content: a simple text SVG showing "TRINITY":
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 40" width="200" height="40" aria-label="TRINITY Breath &amp; Healing logo">
  <text x="0" y="30" font-family="serif" font-size="20" fill="#3D4A35">TRINITY Breath &amp; Healing</text>
</svg>
```

Update .github/workflows/ci.yml build-and-audit job: add a step after the existing "HTML audit" step:
```yaml
- name: Copy preservation gate — locked Figma copy in rendered HTML (LND-09, LND-10)
  run: bash scripts/check-copy.sh
```

This step will pass once sub-unit plans have shipped the locked copy strings into the built HTML. Until then it will fail in CI on Phase 1 feature branches — that is correct and expected behavior.

Note: The check-copy.sh step must come AFTER the npm run build step in the workflow, not before.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>npm run build succeeds (no missing-asset errors from enhanced-img). static/logo.svg exists at /logo.svg. All 5 PNG stubs exist and are valid PNG binary. ci.yml has the check-copy.sh step. npm run check produces 0 TypeScript errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate scripts | Shell scripts read build output — no user input, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Wave 0 |
| T-01-01 | Tampering | check-copy.sh | accept | Script reads local build output only; no user-controlled input; set -euo pipefail prevents silent failures |
</threat_model>

<verification>
- npm test passes (existing 312+ tests green; new Phase 1 html-audit assertions may fail — expected)
- npm run build succeeds with stub images
- static/logo.svg exists and is valid SVG
- scripts/check-copy.sh is executable (chmod +x applied)
- ci.yml has check-copy.sh step
</verification>

<success_criteria>
Build green. Stub assets in place. CI gate script created. Phase 1 html-audit assertions exist (failing = correct before sub-unit implementation). schema-faq conditional test exists.
</success_criteria>

<output>
Create .planning/phases/01-landing-sections/01-00-SUMMARY.md when done.
</output>
