---
phase: 01-landing-sections
plan: "09"
type: execute
wave: 5
depends_on:
  - "01-07"
  - "01-08"
files_modified:
  - scripts/check-copy.sh
  - .github/workflows/ci.yml
  - tests/integration/html-audit.spec.ts
autonomous: false
requirements:
  - LND-09
  - LND-10
  - PRF-04
  - PRF-05
  - PRF-06
  - A11Y-01
  - A11Y-02
  - A11Y-03
  - A11Y-04

must_haves:
  truths:
    - "check-copy.sh passes on built index.html (all locked copy strings present)"
    - "Hedge-language scan finds no unqualified misschien/zou kunnen/wellicht in index.html"
    - "html-audit.spec.ts Phase 1 assertions all green"
    - "All existing 312+ tests still green (no regressions)"
    - "curl -A OAI-SearchBot output contains: H1, 5 nav links, 3 section headings, FAQ answers, footer NAP"
    - "Lighthouse mobile SEO=100, Accessibility=100 target verified"
    - "npm run build succeeds; npm run check 0 errors"
  artifacts:
    - path: scripts/check-copy.sh
      provides: "Updated with all locked copy strings from completed sections"
    - path: .github/workflows/ci.yml
      provides: "check-copy.sh step confirmed wired; pa11y and Lighthouse gates confirmed active"
    - path: tests/integration/html-audit.spec.ts
      provides: "All Phase 1 assertions verified green"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-copy.sh"
      via: "bash scripts/check-copy.sh in build-and-audit job"
      pattern: "check-copy"
---

<objective>
Plan 09 — Cross-cutting gates (LND-09, LND-10). Wave 5 — final integration gate after all 8 sections land.

Purpose: Verify the full landing page against all copy preservation, hedge-language, html-audit, and AI-crawler smoke tests. Finalize scripts and CI config. This plan produces the "Phase 1 DONE" signal.

Output: Verified green CI, updated check-copy.sh (if any locked strings need adding), confirmed html-audit all-green, checkpoint: human visual verification on preview URL.
</objective>

<execution_context>
@C:\Users\jaywe\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jaywe\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/01-landing-sections/01-CONTEXT.md
@.planning/phases/01-landing-sections/01-VALIDATION.md
@.planning/phases/01-landing-sections/01-UI-SPEC.md
@static/global.css
@scripts/check-copy.sh
@tests/integration/html-audit.spec.ts
@.github/workflows/ci.yml
</context>

<interfaces>
<!-- check-copy.sh locked strings (from Wave 0) — verify all 8 sections contribute locked strings -->
<!-- LND-09 locked strings (from UI-SPEC copywriting contract): -->
<!-- Hero: "Rust in je hoofd. Ontspanning in je lichaam.", "Maak een afspraak" -->
<!-- Werkwijze: "Rustig, persoonlijk en op jouw tempo.", "Kennismaking", "De sessie", "Verdieping" -->
<!-- About: "Vanuit eigen ervaring weet ik wat jij doormaakt." -->
<!-- Contact: "Een eerste stap hoeft niet groot te zijn.", "Verstuur email" -->
<!-- Footer: "©Copyright 2026 Trinity Breath & Healing, alle rechten voorbehouden", "info@trinitybnh.nl" -->
<!-- FAQ heading: "Veelgestelde vragen" -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite, verify all Phase 1 assertions green, update check-copy.sh</name>
  <files>scripts/check-copy.sh, tests/integration/html-audit.spec.ts</files>
  <action>
Run the complete test suite in sequence:
1. npm run check — must produce 0 TypeScript errors
2. npm run build — must succeed cleanly
3. npm test -- --run — all unit tests must pass
4. npm run test:html (or npm run test:integration) — all html-audit assertions must pass

For any failing html-audit.spec.ts assertion that was added in Wave 0 (Plan 00 Task 1), diagnose and fix. Common failure reasons:
- Hero preload link assertion: verify <link rel="preload" as="image"> is present in index.html head. If not, check HeroSection.svelte svelte:head block or Head.svelte heroPreload prop.
- Overlay aria assertion: verify role="dialog" aria-modal="true" is in SiteNav.svelte template and present in built HTML.
- Carousel links: verify all 5 <a href="/diensten/..."> are in index.html.
- Form labels: verify all input ids match label for attributes in ContactSection.svelte.
- No third-party scripts: verify no external script src in index.html.

Update scripts/check-copy.sh to include any additional locked strings that were confirmed during implementation:
- "Veelgestelde vragen" (FAQ section H2)
- "©Copyright 2026 Trinity Breath & Healing, alle rechten voorbehouden" (footer)
- "info@trinitybnh.nl" (footer email)
- "Werkwijze", "Over mij", "Behandelingen", "Contact" (section eyebrows)

Run bash scripts/check-copy.sh and verify it exits 0.
Run the hedge-language check portion of the script and verify it exits 0 on the FAQ copy.

If any assertion is still red after fixing, add a NOTE in the SUMMARY explaining what is deferred and why (e.g., "hero preload requires real image asset — stub does not produce correct hashed URL").
  </action>
  <verify>
    <automated>npm run check && npm run build && npm test -- --run 2>&1 | tail -30</automated>
  </verify>
  <done>npm run check: 0 errors. npm run build: succeeds. npm test: all green. check-copy.sh: exits 0. html-audit Phase 1 assertions: green. Any remaining failures documented in SUMMARY with root cause.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All 8 landing sections (Hero, Header, Werkwijze, About, Behandelingen, Contact, FAQ, Footer) built and verified by automated tests. CI gates: copy preservation, hedge language, HTML audit all green.</what-built>
  <how-to-verify>
1. Push the Phase 1 branch to GitHub. Vercel creates a preview URL automatically (e.g. https://trinity-breath-healing-phase1.vercel.app).
2. Open the preview URL on a mobile device (or Chrome DevTools mobile simulation, iPhone 390px).
3. Verify each section visually:
   - Hero: H1 visible, hero illustration present (or 1×1 stub if real asset not yet provided), CTA button gold, 3 service tiles below
   - Header: fixed, hides on scroll-down, re-appears on scroll-up. Hamburger visible at mobile width.
   - Mobile hamburger: opens green full-screen overlay with nav links. X button closes. Escape key closes.
   - Werkwijze: 3 accordion items — tap to open, sibling closes.
   - About: stats (8+, 65+, ∞) visible. "Lees meer" link.
   - Behandelingen: 5 service cards scrollable. Partial edge cards visible.
   - Contact: email form visible by default. Toggle visible when JS active. AVG consent checkbox.
   - FAQ: 10 accordion items openable.
   - Footer: NAP (email, address), 3 columns, copyright.
4. Disable JavaScript in Chrome (DevTools → Network → Block JS). Reload. Verify:
   - Email form still visible (toggle hidden)
   - All section content still accessible
   - No broken UI
5. Run: `curl -A "OAI-SearchBot" <preview-url>/` | grep -c "Rust in je hoofd"
   Expected: output is 1.
6. Run PageSpeed Insights (https://pagespeed.web.dev) on the preview URL, mobile tab.
   Expected: SEO=100, Accessibility=100 (phase goal), LCP noted.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found. If real image assets are now available, provide the paths so they can be copied to src/lib/assets/images/ and static/ before final PR merge.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI scripts | Read-only on build output; no user input |
| Preview URL | Public Vercel preview; no auth; no sensitive data in Phase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | Preview URL publicly accessible | accept | No sensitive data in Phase 1; TODO_ placeholders are non-sensitive |
| T-09-02 | Tampering | check-copy.sh CI gate | accept | Script reads build output only; no network calls; set -euo pipefail |
| T-09-SC | Tampering | npm installs | accept | No new packages in Plan 09 |
</threat_model>

<verification>
- npm run check: 0 errors
- npm run build: clean
- npm test -- --run: all tests green (including Phase 1 html-audit assertions)
- bash scripts/check-copy.sh: exits 0
- curl -A "OAI-SearchBot" on preview URL returns H1 + meta + JSON-LD + FAQ answers
- Human visual checkpoint: approved
</verification>

<success_criteria>
Phase 1 complete. All 8 landing sections in initial SSR HTML. Copy preservation gate passes. Hedge-language scan passes. All html-audit assertions green. Human visual verification approved. Ready for Phase 2.
</success_criteria>

<output>
Create .planning/phases/01-landing-sections/01-09-SUMMARY.md when done.
</output>
