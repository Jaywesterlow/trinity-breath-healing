---
phase: 01-landing-sections
plan: "06"
type: execute
wave: 3
depends_on:
  - "01-03"
  - "01-04"
files_modified:
  - src/lib/components/ContactSection.svelte
  - src/routes/+page.svelte
autonomous: true
requirements:
  - LND-06
  - PRF-07
  - A11Y-02
  - A11Y-03

must_haves:
  truths:
    - "Section has id=contact"
    - "H2 'Een eerste stap hoeft niet groot te zijn.' in initial HTML"
    - "Email form (Voornaam, Achternaam, Email, Telefoon, Bericht) in initial HTML"
    - "All form inputs have associated <label> elements (A11Y-02)"
    - "AVG consent checkbox present and unchecked by default"
    - "Toggle group hidden when no JS (js-enabled body class approach per D-25)"
    - "Cal.com placeholder <div id=cal-mount> present in initial HTML"
    - "No third-party JS loaded on initial page (PRF-07)"
    - "Form action is blank (Phase 3 wires endpoint)"
    - "npm run build succeeds; npm run check 0 errors"
  artifacts:
    - path: src/lib/components/ContactSection.svelte
      provides: "Contact section — radio toggle, email form with labels, AVG consent, cal-mount placeholder"
      min_lines: 100
    - path: src/routes/+page.svelte
      provides: "ContactSection mounted after BehandelingenSection"
  key_links:
    - from: "ContactSection.svelte onMount"
      to: "document.body.classList"
      via: "body.classList.add('js-enabled') for progressive enhancement"
      pattern: "js-enabled"
    - from: "ContactSection.svelte"
      to: "#cal-mount"
      via: "empty placeholder div — Phase 3 mounts Cal.com here"
      pattern: "cal-mount"
---

<objective>
Plan 06 — Contact + booking toggle section (LND-06). Figma Frame 5. Parallel with Plan 05 (different files).

Purpose: Build the contact section with progressive-enhancement radio toggle, email form with proper labels (A11Y-02), AVG consent checkbox, and empty Cal.com mount placeholder. No third-party JS loaded (PRF-07).

Output: ContactSection.svelte, +page.svelte updated.
</objective>

<execution_context>
@C:\Users\jaywe\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jaywe\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/01-landing-sections/01-CONTEXT.md
@.planning/phases/01-landing-sections/01-UI-SPEC.md
@static/global.css
@src/routes/+page.svelte
</context>

<interfaces>
<!-- D-24: Radio toggle "Email formulier" / "Online meeting". Progressive enhancement. -->
<!-- D-25: No-JS = email form only. Toggle hidden via js-enabled body class. -->
<!-- D-26: Form fields: Voornaam, Achternaam, Email, Telefoon (+31), Bericht, submit "Verstuur email" -->
<!-- D-27: Cal.com panel = <div id="cal-mount"> empty placeholder only in Phase 1 -->
<!-- D-28: Section ID = contact -->

<!-- UI-SPEC LND-06 -->
<!-- Radio toggle: active pill = --color-accent-gold bg; inactive = --color-card-warm bg -->
<!-- role="radiogroup" aria-label="Contactmethode kiezen" wrapper -->
<!-- Form input borders: 1px solid var(--color-border). Focus: 2px solid var(--color-accent-gold) -->
<!-- AVG consent: unchecked by default, required -->
<!-- Legal disclaimer: visible above Bericht field (LGL-04) -->
<!-- Submit button: --color-accent-gold fill, same style as CTA -->
<!-- All inputs: <label for> association (A11Y-02) -->

<!-- RESEARCH Pattern 4: js-enabled class progressive enhancement -->
<!-- :global(body:not(.js-enabled)) .toggle-group { display: none } -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ContactSection.svelte — radio toggle, form, cal-mount placeholder</name>
  <files>src/lib/components/ContactSection.svelte</files>
  <behavior>
    - Section id="contact"
    - Eyebrow "Contact", H2 "Een eerste stap hoeft niet groot te zijn." in initial HTML
    - Email form rendered in initial HTML (not gated on JS)
    - All 5 form inputs have <label for=...> associations (ids: firstName, lastName, email, phone, message)
    - AVG consent checkbox id="avg-consent" with <label for="avg-consent">
    - Toggle group has class "toggle-group"; hidden via :global(body:not(.js-enabled)) .toggle-group { display:none }
    - onMount adds 'js-enabled' to document.body.classList
    - <div id="cal-mount" aria-label="Agenda voor online afspraak"> present in initial HTML
    - No Cal.com or third-party scripts loaded
    - Radio inputs: name="contact-method" with values "email" and "online"
    - Active radio panel toggling via $state and class binding
    - All CSS token-only
  </behavior>
  <action>
Create src/lib/components/ContactSection.svelte. Svelte 5 runes.

Script:
```typescript
import { onMount } from 'svelte';
let activeTab = $state<'email' | 'online'>('email');

onMount(() => {
  document.body.classList.add('js-enabled');
});
```

Template structure:
```svelte
<section id="contact">
  <div class="container">
    <p class="eyebrow">Contact</p>
    <h2>Een eerste stap hoeft niet groot te zijn.</h2>
    <p class="contact-body">
      Of je nu wilt mailen, bellen of direct een online afspraak wilt inplannen — kies wat voor jou werkt.
      Ik reageer zo snel mogelijk, meestal binnen één werkdag.
    </p>

    <!-- Toggle group — hidden when no JS (js-enabled class approach, D-25) -->
    <div class="toggle-group" role="radiogroup" aria-label="Contactmethode kiezen">
      <label class="toggle-pill" class:active={activeTab === 'email'}>
        <input type="radio" name="contact-method" value="email" checked={activeTab === 'email'} onchange={() => activeTab = 'email'} />
        Email formulier
      </label>
      <label class="toggle-pill" class:active={activeTab === 'online'}>
        <input type="radio" name="contact-method" value="online" checked={activeTab === 'online'} onchange={() => activeTab = 'online'} />
        Online meeting
      </label>
    </div>

    <!-- Email form panel — always in initial HTML -->
    <div class="panel panel-email" class:panel--active={activeTab === 'email'}>
      <form method="POST" action="">
        <div class="form-row">
          <div class="form-field">
            <label for="firstName">Voornaam *</label>
            <input type="text" id="firstName" name="firstName" required autocomplete="given-name" />
          </div>
          <div class="form-field">
            <label for="lastName">Achternaam *</label>
            <input type="text" id="lastName" name="lastName" required autocomplete="family-name" />
          </div>
        </div>
        <div class="form-field">
          <label for="email">E-mailadres *</label>
          <input type="email" id="email" name="email" required autocomplete="email" />
        </div>
        <div class="form-field">
          <label for="phone">Telefoonnummer</label>
          <input type="tel" id="phone" name="phone" autocomplete="tel" placeholder="+31" />
        </div>
        <p class="disclaimer">
          Trinity Breath & Healing begeleidt bij lichaamsgerichte therapie. Wij zijn geen medische zorgverlener en behandelen geen diagnoses. Bij medische klachten raadpleeg altijd een arts.
        </p>
        <div class="form-field">
          <label for="message">Bericht *</label>
          <textarea id="message" name="message" required rows={5}></textarea>
        </div>
        <div class="form-field form-field--checkbox">
          <input type="checkbox" id="avg-consent" name="avg-consent" required />
          <label for="avg-consent">
            Ik ga akkoord met de verwerking van mijn gegevens conform de <a href="/privacyverklaring">privacyverklaring</a>. *
          </label>
        </div>
        <button type="submit" class="btn-submit">Verstuur email</button>
      </form>
    </div>

    <!-- Online meeting panel — always in initial HTML (D-25, PRF-07) -->
    <div class="panel panel-online" class:panel--active={activeTab === 'online'}>
      <div id="cal-mount" aria-label="Agenda voor online afspraak">
        <!-- Phase 3 mounts Cal.com inline embed here (INT-04, INT-05) -->
        <p class="cal-placeholder">Online afspraakenboek wordt hier geladen zodra beschikbaar.</p>
      </div>
    </div>
  </div>
</section>
```

Style block (scoped, token-only):
- section: padding-block var(--space-12), background var(--color-bg-sand)
- .container: max-width var(--container-max), margin-inline auto, padding-inline var(--space-6)
- .eyebrow: same pattern as other sections
- h2: font-family var(--font-display), font-size var(--font-size-2xl), font-weight var(--font-weight-bold), color var(--color-fg-forest), margin-bottom var(--space-4)
- .contact-body: font-size var(--font-size-base), line-height var(--line-height-normal), color var(--color-fg-forest), margin-bottom var(--space-8)
- .toggle-group: display flex, gap var(--space-2), margin-bottom var(--space-8), background var(--color-card-warm), border-radius var(--radius-full), padding var(--space-2), width fit-content
- .toggle-pill: display flex, align-items center, padding var(--space-2) var(--space-5), border-radius var(--radius-full), cursor pointer, font-size var(--font-size-sm), font-weight var(--font-weight-bold), color var(--color-muted), min-height 44px, transition background var(--motion-base) var(--ease-out)
- .toggle-pill.active: background var(--color-accent-gold), color var(--color-fg-forest)
- .toggle-pill input[type="radio"]: position absolute, opacity 0, width 0, height 0 (visually hidden but functionally present for screen readers)
- .panel: display none
- .panel--active: display block
- :global(body:not(.js-enabled)) .toggle-group: display none (progressive enhancement)
- :global(body:not(.js-enabled)) .panel-email: display block (always show email form without JS)
- .form-row: display grid, grid-template-columns 1fr 1fr, gap var(--space-4). @media < 640px: grid-template-columns 1fr
- .form-field: display flex, flex-direction column, gap var(--space-2), margin-bottom var(--space-4)
- label: font-size var(--font-size-sm), font-weight var(--font-weight-bold), color var(--color-fg-forest)
- input, textarea: border 1px solid var(--color-border), border-radius var(--radius-md), padding var(--space-3) var(--space-4), font-size var(--font-size-base), color var(--color-fg-forest), background var(--color-bg-sand), width 100%
- input:focus, textarea:focus: outline 2px solid var(--color-accent-gold), outline-offset 2px
- .form-field--checkbox: flex-direction row, align-items flex-start, gap var(--space-3)
- .form-field--checkbox input: width auto, margin-top var(--space-1), flex-shrink 0
- .disclaimer: font-size var(--font-size-sm), color var(--color-muted), line-height var(--line-height-normal), margin-bottom var(--space-4), padding var(--space-4), background var(--color-card-warm), border-radius var(--radius-md)
- .btn-submit: background var(--color-accent-gold), color var(--color-fg-forest), border none, border-radius var(--radius-full), padding var(--space-3) var(--space-8), font-size var(--font-size-base), font-weight var(--font-weight-bold), cursor pointer, min-height 44px
- .cal-placeholder: font-size var(--font-size-sm), color var(--color-muted), font-style italic
- @media (min-width: 1024px): section: padding-block var(--space-16)
  </action>
  <verify>
    <automated>npm run check 2>&1 | tail -10</automated>
  </verify>
  <done>ContactSection.svelte compiles with 0 errors. Section id=contact. H2 verbatim. 5 form inputs with <label for> associations. AVG consent checkbox unchecked. Toggle group with js-enabled progressive enhancement. cal-mount div present. No third-party scripts. Token-only CSS.</done>
</task>

<task type="auto">
  <name>Task 2: Mount ContactSection in +page.svelte</name>
  <files>src/routes/+page.svelte</files>
  <action>
Import ContactSection from '$lib/components/ContactSection.svelte'. Add `<ContactSection />` after `<BehandelingenSection />`.

Verify page order: HeroSection → WerkwijzeSection → AboutSection → BehandelingenSection → ContactSection → (recency block at bottom).

Run npm run build. Check index.html for id="contact", "Een eerste stap hoeft niet groot te zijn.", form inputs with for attributes, id="cal-mount".
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>npm run build succeeds. index.html: id=contact, H2 present, form fields with labels, cal-mount placeholder. npm run check: 0 errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contact form | Form renders in Phase 1 but has no action endpoint — Phase 3 wires server-side validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Contact form action="" | accept | No server-side processing in Phase 1; form action wired in Phase 3 with Zod validation + honeypot |
| T-06-02 | Information Disclosure | AVG consent checkbox | mitigate | Checkbox unchecked by default per LGL-07; Phase 3 validates on server that consent was given |
| T-06-03 | Tampering | Cal.com mount placeholder | accept | Empty div in Phase 1; Phase 3 loads Cal.com only on toggle activation (PRF-07) |
| T-06-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- npm run check: 0 errors
- npm run build succeeds
- index.html: 5 form inputs with associated <label for> elements
- id="cal-mount" present in initial HTML
- No <script src="https://..." > in index.html (PRF-07)
- Toggle group hidden without JS (css :global rule)
- npm test: green
</verification>

<success_criteria>
ContactSection mounted. Email form with labeled inputs in initial HTML. AVG consent checkbox. Cal.com placeholder. Toggle progressive enhancement. No third-party JS. 0 TypeScript errors.
</success_criteria>

<output>
Create .planning/phases/01-landing-sections/01-06-SUMMARY.md when done.
</output>
