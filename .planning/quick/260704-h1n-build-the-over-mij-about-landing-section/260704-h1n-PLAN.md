---
phase: quick-260704-h1n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/global.css
  - src/lib/components/ui/interactions/TextLink.svelte
  - src/lib/components/ui/interactions/ButtonLink.svelte
  - src/lib/components/ui/AboutStat.svelte
  - src/lib/components/ui/AboutFeature.svelte
  - src/lib/components/ui/index.ts
  - src/lib/components/global/OverMij.svelte
  - src/lib/components/global/index.ts
  - src/routes/+page.svelte
autonomous: false
requirements: [LND-04, LND-09]

must_haves:
  truths:
    - "Landing page shows an 'Over mij' eyebrow, H2 heading, and body paragraph directly after the Werkwijze section"
    - "Desktop (≥1024px) shows two side-by-side portrait cards, two icon+title+body feature rows (Vanuit eigen ervaring / Vakkundig opgeleid), and a 'Lees meer over mij' link to /over-mij"
    - "Mobile (<1024px) shows a single full-bleed portrait card with a 'Plan een kennismaking' outline pill linking to /contact, and a bottom scrim containing the body paragraph + 'Lees meer over mij' link in the new gold-soft tone"
    - "Both breakpoints show 3 stat badges (8+ Jaren ervaring, 65+ Klachten verholpen, ∞ Mogelijkheden voor herstel) sourced from BRAND.stats, not hardcoded"
    - "No TypeScript/svelte-check errors introduced"
  artifacts:
    - path: "src/lib/components/global/OverMij.svelte"
      provides: "About section: header, portrait cards (mobile+desktop), feature rows, stats row"
      min_lines: 80
    - path: "src/lib/components/ui/AboutStat.svelte"
      provides: "Reusable number/icon-in-circle + label component (used 3x)"
      min_lines: 20
    - path: "src/lib/components/ui/AboutFeature.svelte"
      provides: "Reusable icon-in-circle + title + body component (used 2x, desktop-only)"
      min_lines: 15
    - path: "static/global.css"
      provides: "New --color-accent-gold-soft token"
      contains: "--color-accent-gold-soft"
  key_links:
    - from: "src/lib/components/global/OverMij.svelte"
      to: "src/lib/constants/brand.ts"
      via: "import { BRAND } from '$lib/constants/brand'"
      pattern: "BRAND\\.stats"
    - from: "src/lib/components/global/OverMij.svelte"
      to: "src/lib/components/ui/AboutStat.svelte"
      via: "component usage, 3x"
      pattern: "<AboutStat"
    - from: "src/lib/components/global/OverMij.svelte"
      to: "src/lib/components/ui/AboutFeature.svelte"
      via: "component usage, 2x, desktop-only"
      pattern: "<AboutFeature"
    - from: "src/lib/components/global/OverMij.svelte"
      to: "/over-mij"
      via: "TextLink href"
      pattern: "over-mij"
    - from: "src/lib/components/global/OverMij.svelte"
      to: "/contact"
      via: "ButtonLink href"
      pattern: "href=\"/contact\""
    - from: "src/routes/+page.svelte"
      to: "src/lib/components/global/OverMij.svelte"
      via: "<OverMij /> rendered after <Werkwijze />"
      pattern: "<OverMij"
---

<objective>
Build the "Over mij" (About) landing section from Figma (desktop node 318:210, mobile node 512:46) and insert it into the landing page directly after Werkwijze.

Purpose: Deliver requirement LND-04 (About + stats section) — the section carrying the practitioner's personal-experience trust signal and the 3 stat "citation/proof blocks" (8+/65+/∞) that support E-E-A-T per CLAUDE.md constraints.

Output: Two new reusable UI primitives (`AboutStat.svelte`, `AboutFeature.svelte`), one new section component (`OverMij.svelte`), one new CSS color token, extended `TextLink`/`ButtonLink` with new tone/variant options, and the section wired into `+page.svelte`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260704-h1n-build-the-over-mij-about-landing-section/260704-h1n-CONTEXT.md
@src/lib/components/global/Werkwijze.svelte
@src/lib/components/ui/WerkwijzeCard.svelte
@src/lib/components/ui/interactions/TextLink.svelte
@src/lib/components/ui/interactions/ButtonLink.svelte
@src/lib/constants/brand.ts
@static/global.css
@src/routes/+page.svelte
</context>

<interfaces>
<!-- Extracted from codebase — use directly, no further exploration needed. -->

From src/lib/constants/brand.ts:
```typescript
export const BRAND = {
  // ...
  stats: {
    yearsExperience: '8+',
    clientsHelped: '65+',
    satisfaction: '∞'
  } as const
} as const;
```

From src/lib/components/ui/interactions/TextLink.svelte (current):
```typescript
interface Props {
  label: string;
  href: string;
  inverted?: boolean;
  showArrow?: boolean;
  size?: 'lg' | 'sm';
}
```
CSS classes: `.text-link`, `.text-link__label`, `.text-link__arrow`, `.text-link--inverted`, `.text-link--sm`. Base color `var(--color-fg-forest)` / border-bottom same.

From src/lib/components/ui/interactions/ButtonLink.svelte (current):
```typescript
interface Props {
  label: string;
  href: string;
  withArrow?: boolean;
  onclick?: () => void;
}
```
`.btn-link__label` always gets `background: var(--color-border); color: var(--color-bg-sand);` — this is the "filled" look every current caller (Nav.svelte, WerkwijzeCard.svelte) relies on implicitly.

From src/lib/components/ui/WerkwijzeCard.svelte — card sizing convention to reuse verbatim for the desktop portrait cards:
`width: 17.625rem;` (282px) `height: 28.688rem;` (459px) `border-radius: 1.563rem;` (25px — exact Figma value, no token match) `object-fit: cover` on the image, `alt=""` `aria-hidden="true"` for decorative art.

From static/global.css color tokens block (lines 8-15):
```css
--color-bg-sand: #FAF0E6;
--color-fg-forest: #3D4A35;
--color-accent-gold: #D4A968;
--color-card-warm: #EDE8D0;
--color-muted: #7A8C6E;
--color-text-subtle: #848981;
--color-border: #B68A6B;
```

From src/lib/components/global/index.ts and src/lib/components/ui/index.ts — barrel export pattern:
```typescript
export { default as Werkwijze } from './Werkwijze.svelte';
export { default as WerkwijzeCard } from './WerkwijzeCard.svelte';
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add gold-soft color token, extend TextLink with tone prop, extend ButtonLink with outline variant</name>
  <files>static/global.css, src/lib/components/ui/interactions/TextLink.svelte, src/lib/components/ui/interactions/ButtonLink.svelte</files>
  <action>
In static/global.css, add `--color-accent-gold-soft: #C7A27A;` to the `:root` color tokens block (near the other `--color-*` tokens, after `--color-border`), with a one-line comment: `/* Figma-sourced — Over mij mobile CTA/link accents on dark overlay */`. Do not touch any other token.

In TextLink.svelte, add an optional prop `tone?: 'muted' | 'gold-soft'` alongside the existing `inverted` prop (do not remove or rename `inverted` — Footer.svelte depends on it as-is). When `tone === 'muted'`, apply `color: var(--color-muted); border-bottom-color: var(--color-muted);` via a new `.text-link--muted` class. When `tone === 'gold-soft'`, apply `color: var(--color-accent-gold-soft); border-bottom-color: var(--color-accent-gold-soft);` via `.text-link--gold-soft`. Wire the classes with `class:text-link--muted={tone === 'muted'}` / `class:text-link--gold-soft={tone === 'gold-soft'}` next to the existing `class:text-link--inverted` binding. Leave default (no tone, no inverted) behavior untouched.

In ButtonLink.svelte, add an optional prop `variant?: 'filled' | 'outline' = 'filled'`. Bind `class:btn-link--outline={variant === 'outline'}` on the `<a>` alongside the existing `class:btn-link--arrow`. Add CSS: `.btn-link--outline .btn-link__label { background: transparent; border: 2px solid var(--color-accent-gold-soft); color: var(--color-bg-sand); }`. Do not change the default (filled) styles — existing callers (Nav.svelte mobile menu CTA, WerkwijzeCard's "Maak een afspraak"/"Plan een kennismaking" CTA) must render identically since they never pass `variant`.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>--color-accent-gold-soft exists in global.css; TextLink accepts tone="muted"|"gold-soft" without altering inverted behavior; ButtonLink accepts variant="outline" without altering default filled output; svelte-check reports 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build AboutStat and AboutFeature reusable UI components</name>
  <files>src/lib/components/ui/AboutStat.svelte, src/lib/components/ui/AboutFeature.svelte, src/lib/components/ui/index.ts</files>
  <action>
Create `AboutStat.svelte` (Svelte 5 runes, props via `$props()`): props `{ value?: string; iconSrc?: string; label: string }`. Renders a circle badge followed by label text. Mobile-first layout: `display: flex; align-items: center; gap: var(--space-4);` with the circle at `4.813rem` (77px — Figma spec, no token match) and the label to the right. Inside the circle: if `iconSrc` is set, render `<img src={iconSrc} alt="" aria-hidden="true" class="stat__icon" />` (used for the ∞ stat, sized to ~60% of the circle, `object-fit: contain`); else render `<span class="stat__value">{value}</span>` (number text, `font-family: var(--font-display); font-weight: var(--font-weight-bold);`, sized to fit the circle). Circle background `var(--color-card-warm)`, `border-radius: 50%`, `flex-shrink: 0`. At `@media (min-width: 1024px)`, switch the badge to `7.813rem` (125px — Figma spec) and switch the wrapper to `flex-direction: column; align-items: center; text-align: center;` per the desktop row-of-3 layout in Figma. Label styling: `font-family: var(--font-body); font-size: var(--fs-body); color: var(--color-fg-forest);`.

Create `AboutFeature.svelte`: props `{ iconSrc: string; title: string; body: string }`. Desktop-only component (visibility is controlled entirely by the parent `OverMij.svelte` — this component has no internal responsive hiding). Layout: `display: flex; align-items: flex-start; gap: var(--space-4);` — icon circle (`4.688rem` / 75px — Figma spec, matches the note in CONTEXT.md) containing `<img src={iconSrc} alt="" aria-hidden="true" class="feature__icon" />` (`object-fit: contain`, ~60% of circle), circle background `var(--color-card-warm)`, `border-radius: 50%`, `flex-shrink: 0`; adjacent text stack with `<h3 class="feature__title">{title}</h3>` (`font-family: var(--font-display); font-size: var(--fs-h3); font-weight: var(--font-weight-medium); color: var(--color-fg-forest);`) and `<p class="feature__body">{body}</p>` (`font-family: var(--font-body); font-size: var(--fs-body); color: var(--color-text-subtle);`).

Add both to `src/lib/components/ui/index.ts`: `export { default as AboutStat } from './AboutStat.svelte';` and `export { default as AboutFeature } from './AboutFeature.svelte';`, alongside the existing exports.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>AboutStat.svelte renders either a text value or an icon image inside a responsive circle + label; AboutFeature.svelte renders an icon circle + title + body row; both exported from ui/index.ts; 0 TS errors.</done>
</task>

<task type="auto">
  <name>Task 3: Build OverMij section component and integrate into the landing page</name>
  <files>src/lib/components/global/OverMij.svelte, src/lib/components/global/index.ts, src/routes/+page.svelte</files>
  <action>
Create `OverMij.svelte` under `src/lib/components/global/`, following Werkwijze.svelte's structural convention (`<section>` wrapper, eyebrow+heading header block, mobile-first CSS with a single `@media (min-width: 1024px)` desktop override — no other breakpoint). Import `BRAND` from `$lib/constants/brand`, `AboutStat` and `AboutFeature` from `$lib/components/ui`, `TextLink` and `ButtonLink` from `$lib/components/ui/interactions`.

Markup structure (all nodes present in initial HTML at every breakpoint; visibility toggled via CSS `display`, never via `{#if}` gating on viewport — matches this codebase's existing pattern of shipping full content for crawlers and letting CSS handle responsive layout):

1. `<section class="about" id="over-mij">`
2. Header block: `<p class="about__eyebrow">Over mij</p>` + `<h2 class="about__heading">Vanuit eigen ervaring weet ik wat jij doormaakt.</h2>` (copy verbatim per LND-09/CONTEXT.md — do not paraphrase).
3. Media block `<div class="about__media">` containing three card `<div>`s:
   - `.about__card--mobile` (visible `<1024px` only): `<img src="/images/about-portrait-2.png" alt="" aria-hidden="true" class="about__card-img" />` filling the card (`355×579` render, `object-fit: cover`, rounded per the same `1.563rem` radius WerkwijzeCard uses); `<ButtonLink label="Plan een kennismaking" href="/contact" variant="outline" class="about__pill" />` absolutely positioned top-right overlay; `<div class="about__scrim">` absolutely positioned at the bottom with `background: linear-gradient(to top, rgba(61,74,53,0.92) 0%, transparent 55%)` (tune to taste, must keep text legible) containing `<p class="about__scrim-body">Ik ben 53 jaar en weet uit eigen ervaring hoe het voelt om vast te lopen. Mijn aanpak is geen theorie, het is wat mij zelf heeft geholpen. Ik werk vanuit rust, veiligheid en echte ervaring.</p>` and `<TextLink label="Lees meer over mij" href="/over-mij" tone="gold-soft" />`.
   - `.about__card--desktop-1` (visible `≥1024px` only): `<img src="/images/about-portrait-1.png" alt="" aria-hidden="true" />`, `17.625rem × 28.688rem`, `border-radius: 1.563rem`, `object-fit: cover`.
   - `.about__card--desktop-2` (visible `≥1024px` only): same sizing, `<img src="/images/about-portrait-2.png" alt="" aria-hidden="true" />`.
4. Content column `<div class="about__content">` (visible `≥1024px` only): `<p class="about__body">` — same body paragraph text as the mobile scrim, verbatim; `<div class="about__features">` containing two `<AboutFeature>` instances — `iconSrc="/images/about-icon-heart.jpg"` title="Vanuit eigen ervaring" body="Geen aangeleerde theorie, maar een aanpak die ik zelf heb doorleefd." and `iconSrc="/images/about-icon-sprout.jpg"` title="Vakkundig opgeleid" body="Geen aangeleerde theorie, maar een aanpak die ik zelf heb doorleefd." (identical body text is intentional per locked CONTEXT.md decision — ship verbatim, do not alter); `<TextLink label="Lees meer over mij" href="/over-mij" tone="muted" />`.
5. Stats row `<div class="about__stats">` (visible at both breakpoints — mobile: `flex-direction: column`; desktop: `flex-direction: row; justify-content: center;`): three `<AboutStat>` instances built from `BRAND.stats` — `value={BRAND.stats.yearsExperience}` label="Jaren ervaring"; `value={BRAND.stats.clientsHelped}` label="Klachten verholpen"; `iconSrc="/images/about-icon-infinity.jpg"` label="Mogelijkheden voor herstel" (no `value` passed — BRAND.stats.satisfaction is intentionally not rendered as text since Figma uses the infinity glyph image instead).

CSS notes: use `--space-*` tokens for all gaps/padding that map cleanly; use exact-rem values with a `/* Figma spec */` comment (matching WerkwijzeCard's convention) for the two Figma measurements that don't map to the token scale (77px/125px stat circles are inside AboutStat, not here; the 282×459 and 355×579 card dimensions belong here). Section background: `var(--color-bg-sand)`, matching Werkwijze.

Add `export { default as OverMij } from './OverMij.svelte';` to `src/lib/components/global/index.ts`.

In `src/routes/+page.svelte`, import `OverMij` alongside the existing `Hero, Werkwijze` import from `$lib/components`, and render `<OverMij />` directly after `<Werkwijze />` and before the existing `{#if data.meta.dateModified}` block.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>OverMij.svelte exists, renders header+media+content+stats per the structure above, uses BRAND.stats (no hardcoded stat strings), reuses AboutStat/AboutFeature/TextLink/ButtonLink; exported from global/index.ts; rendered in +page.svelte after Werkwijze; svelte-check reports 0 errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Over mij section: 2 new shared UI components (AboutStat, AboutFeature), 1 new section component (OverMij), 1 new color token, TextLink/ButtonLink extensions — wired into the landing page after Werkwijze.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the landing page in a browser.
    2. At desktop width (≥1024px): confirm two side-by-side portrait cards, eyebrow "Over mij", heading "Vanuit eigen ervaring weet ik wat jij doormaakt.", body paragraph, two feature rows ("Vanuit eigen ervaring" / "Vakkundig opgeleid" — same body text under both, this is expected), "Lees meer over mij" link (muted-green tone) that navigates to /over-mij, and a row of 3 stat badges (8+ / 65+ / ∞).
    3. Resize to mobile width (<1024px): confirm centered eyebrow+heading, a single full-bleed portrait card with a "Plan een kennismaking" outline pill (gold-soft border, cream text) in the top-right that navigates to /contact, a legible bottom scrim with body paragraph + gold-soft "Lees meer over mij" link, and the 3 stat badges stacked vertically below the card.
    5. Compare against Figma frames desktop node 318:210 and mobile node 512:46 for reasonable visual fidelity (exact pixel match not required, but layout/spacing/colors should be recognizably the same design).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Purely presentational, statically-rendered landing content — no user input, no new external requests, no new npm packages. Existing `/over-mij` and `/contact` routes are internal navigation only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-h1n-01 | Tampering | Decorative image assets in `static/images/` | accept | Assets already downloaded and reviewed by user (per CONTEXT.md); no runtime fetch, no user-supplied content; low-value target for a static marketing image. |
| T-quick-h1n-02 | Information Disclosure | None | accept | No new data collection, forms, or PII surfaces introduced by this section. |
</threat_model>

<verification>
1. `npm run check` passes with 0 errors after each task.
2. Grep confirms `BRAND.stats` (not hardcoded strings) feeds the two text-based stat badges: `grep -n "BRAND.stats" src/lib/components/global/OverMij.svelte`.
3. Grep confirms both CTA hrefs are present: `grep -n "over-mij\|/contact" src/lib/components/global/OverMij.svelte`.
4. Human visual check (checkpoint task) confirms Figma fidelity at both breakpoints.
</verification>

<success_criteria>
- `OverMij.svelte` renders after `Werkwijze` on the landing page with all locked copy verbatim.
- Desktop shows 2 portrait cards + 2 feature rows + text link; mobile shows 1 full-bleed card + outline pill + scrim + text link; both show 3 stat badges from `BRAND.stats`.
- `AboutStat` and `AboutFeature` are genuinely reusable components (not one-off inline markup), matching the explicit user instruction.
- `TextLink` and `ButtonLink` are extended (not forked) with new tone/variant options; existing callers (Footer, Nav, WerkwijzeCard) are visually unaffected.
- `--color-accent-gold-soft` token exists in `static/global.css`.
- `npm run check` reports 0 errors.
- Human checkpoint approved.
</success_criteria>

<output>
Create `.planning/quick/260704-h1n-build-the-over-mij-about-landing-section/260704-h1n-SUMMARY.md` when done
</output>
