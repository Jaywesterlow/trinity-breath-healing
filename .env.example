# PUBLIC_SITE_URL is required — set per environment in Vercel dashboard.
# No localhost default: if unset, the build fails loudly (FND-07).
# Local dev: copy this file to .env and set the value explicitly.
PUBLIC_SITE_URL=
